# Implementation Plans

Generated by the improve skill on 2026-08-06 for the **Working Papers** module
(`migration/src/view_wp.tsx`, `wp_signoff.tsx`, `view_workspace.tsx`).
Planned against commit `366eb37`. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Ekstrak lapisan murni WP ke `src/wp_canon.ts` + tes karakterisasi | P1 | M | — | DONE (`fdaa1f5`, branch `advisor/001-wp-canon-extract`) |
| 002  | Unifikasi derivasi status WP (exec-aware) di `wp_canon.ts` | P1 | S | 001 | DONE (`3c6a9c2`, branch `advisor/002-wp-derivation-unify`) |
| 003  | Semantik slot preparer + tanggal live + identitas sesi (bukti/catatan) | P2 | S | 001 | DONE (`55594d9`, branch `advisor/003-wp-signoff-chain-identity`) |
| 004  | Gate SoD + identitas sesi pada aksi sign-off drill WP | P1 | M | 001, 002, 003 | DONE (`8fc1e1d`, branch `advisor/004-wp-signoff-sod`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **002 requires 001**: the derivation functions must live in `src/wp_canon.ts`
  (pure, node-testable) before they can be unified and re-tested.
- **003 requires 001**: it edits `deriveWpStatus` inside `wp_canon.ts` (moved by 001).
- **004 requires 001, 002, 003**: it edits `SignoffTab`/`WPFooter` in
  `view_wp.tsx` on top of the corrected chain semantics from 003, and relies on
  the unified derivation from 002 staying green.
- Plans 002, 003, 004 all append to `src/wp_canon.test.ts`; run them in order
  and do not leave the suite red between plans.

## Verification baseline (all green at plan time)

- `cd migration && npm run typecheck` → exit 0, no errors
- `cd migration && npm run lint` → exit 0
- `cd migration && npm run test` → all pass
- `cd migration && npm run build` → no resolution failures

## Findings considered and rejected

- **WP "New"/"Unggah"/"Lihat" dead controls (audit #7)**: not planned — wiring
  real upload is a product decision (two evidence stores exist); defer.
- **Phase-gate readers bypass server state (`opinionFinalized`/`eqrReviewsLS`,
  audit #9)**: not planned — orthogonal to the WP layer, medium effort/risk;
  revisit when the gate/opinion modules are next touched.
- **`wpCompletenessFor`/`engagementGate` unit tests**: `finalisationGateCriteria`
  and `checkWtbIntegrity` (the real threshold logic) are already tested in
  `engagement_phase_gate.test.ts` / `wtb_integrity.test.ts`; the remaining
  aggregation in `wp_signoff.tsx` is thin and not worth a dedicated plan now.
- **Two WP registries (`WP_INDEX` vs `AMS.WORKPAPERS`, audit #1)**: intentionally
  out of scope for these plans — consolidation is a data-layer change affecting
  cockpit/mytasks/ai-insights; flagged as a future plan.

## Keputusan temuan yang di-defer (2026-08-06)

| Temuan | Keputusan | Status |
|---|---|---|
| **#7 — kontrol mati di WP** (`WP Baru` / `Unggah` / `Lihat`, tidak ada `onClick`) | **DITARIK dari `view_wp.tsx`** — affordance yang berbohong dihapus (minimal, nol-perilaku; `Unggah` redundan dengan "Tambah Bukti" + kontrol Bukti SubBar kanonik). Upload nyata & pembuatan WP baru dari template = **keputusan produk, tetap defer** (dua evidence-store ada). | RESOLVED (honest-minimal) |
| **#9 — pembaca gerbang fase bypass server state** (`opinionFinalized`/`eqrReviewsLS` di `wp_signoff.tsx`) | **TETAP DEFER.** Bacaanya cache-klien yang *hanya* mengisi gerbang *graduated & overridable* (warn/confirm); server tetap menegakkan RBAC pada tulis dan transisi fase persisten di server. Cache ditulis oleh `useServerState` (set + hydrate) jadi nyaris konsisten. Fix = ubah `engagementGate` (pure fn) jadi async server-backed di call-site cockpit/firm/opinion — medium effort, orthogonal ke WP; **revisit saat modul gate/opini disentuh.** | KEEP DEFERRED |
| **#1 — dua registry WP** (`WP_INDEX` vs `AMS.WORKPAPERS`) | **TETAP DEFER.** `WP_INDEX` = registry drill-down kaya (seksi, status-default, prosedur via `WP_PROCS`); `AMS.WORKPAPERS` = konsumen cockpit/mytasks/ai-insights. **Derivasi sudah SSOT via `wp_canon` (plan 002)**; unifikasi registry = refactor lapisan-data lintas-konsumen, arc terpisah. | KEEP DEFERRED |

## Smoke role-based sign-off (2026-08-06) — 27/27 PASS

Headless-Chrome (CDP) end-to-end terhadap dev server (`:5180` + `:5181`): login per peran →
Working Papers → drill ref `A` → tab **Sign-off & Riwayat** + **footer**.

| Peran | Reviewer | Partner | EQR | Footer |
|---|---|---|---|---|
| Junior (Fajar N.) | tombol **nonaktif** + tooltip "Peran Anda tidak berwenang" | nonaktif | nonaktif (PIE) | hint **"Sign-off review hanya oleh Reviewer berwenang"** |
| Senior (Dimas R.) | nonaktif | nonaktif | nonaktif | hint yang sama |
| Manager (Anindya P.) | **aktif** → ter-record `Anindya P.` | nonaktif | nonaktif | tombol Sign-off Review aktif, tanpa hint |
| Partner (Hartono W.) | aktif (melihat tanda tangan Manager) | **aktif** → ter-record `Hartono W.` | **aktif** → ter-record `Hartono W.` | aktif, tanpa hint |

- **Identitas penanda tangan = nama sesi** (bukan hardcode): preparer `Fajar N.` (sesi Junior),
  reviewer `Anindya P.` (sesi Manager), partner & EQR `Hartono W.` (sesi Partner); semua
  di-persist server (diverifikasi reload/hydrate).
- **0 console error / exception** di seluruh alur.
- **Temuan minor (pra-ada, BUKAN bagian 4 plan):** race kecil `useServerState` di
  `contexts.tsx` — respons hydration `state.get` yang masih in-flight saat user menulis dapat
  menimpa tampilan optimistis kembali ke snapshot lama (data aman: flush server tetap masuk;
  self-heal saat reload/state-get berikutnya). Tidak disentuh: `contexts.tsx` = WIP Confirmation
  Hub. Opsi perbaikan (guard "ada tulis lokal sejak hydration" sebelum `setValRaw(res.value)`)
  untuk diputuskan saat `contexts.tsx` di-commit.
