{
  "name": "asseris-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tahap 7 — Playwright end-to-end atas stack Postgres (API tRPC + SPA hasil build). Lima perjalanan: login cookie HttpOnly, isolasi engagement lintas-user, edit+pindah engagement, sign-off SoD berurutan, dan StateDocHistory+audit.",
  "scripts": {
    "stack:api": "node scripts/start-api.mjs",
    "stack:web": "node scripts/start-web.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.49.0"
  }
}
