# dependencies
node_modules/
build/node_modules/
migration/node_modules/

# generated build artifacts (regenerable — not the source of truth)
app/compiled/
app/vendor/
NeoSuite AMS (prod).html
migration/dist/
migration/dist-wedge/
migration/coverage/

# NOTE (W3 Phase 2): migration/src/ + migration/index.html are now the
# CANONICAL hand-maintained ESM source — committed, no longer codemod-generated.

# generated lint report
migration/.eslint-report.json

# archived originals (24 MB zip + duplicate PDFs + old inlined build)
_archive/

# secrets / env (NEVER commit — compose & server read these; W10 deploy .env carries
# POSTGRES_PASSWORD + APP_ENCRYPTION_KEY). server/.env (sqlite DATABASE_URL only) is already
# tracked from W6 and intentionally left as-is; these rules stop NEW secret files leaking.
.env
.env.*
!.env.example
server/.env.local

# misc
.thumbnail
.DS_Store
Thumbs.db

# Claude Code per-machine config (personal — never commit). settings.local.json
# holds accumulated tool-permission grants; settings.json holds a local model override.
.claude/settings.json
.claude/settings.local.json

# business-development / evaluation deliverables: kept on disk for reference but
# NOT app source (pitch deck, business plan, eval report/dashboard, financial
# model, gap matrix, test registers, user manual, WP template, dummy data + its
# generator). Root-anchored so tracked "NeoSuite AMS*.html" / "Evaluasi NeoSuite
# AMS - *.html" deliverables are unaffected.
/ASSERIS_Model_Keuangan_3thn.xlsx
/Asseris - Data Dummy Pengujian Modul.xlsx
/Asseris - Manual Pengguna.docx
/Asseris - Matriks Gap SAK SPAP ISQM.xlsx
/Asseris - Register Uji Modul.xlsx
/Asseris - Template Kertas Kerja Audit.xlsx
/Evaluasi Asseris - Dashboard.html
/Laporan Evaluasi Asseris.docx
/Pitch Deck Asseris.pptx
/Rencana Bisnis Asseris.docx
/_build_dummy.py
