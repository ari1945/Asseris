# Serah-Terima Pengembangan Asseris — 2026-09-07

> Ditulis saat Ari berhenti memakai Claude Code dan berpindah ke LLM/agen lain. Tujuannya
> satu: **agen mana pun yang meng-clone repo ini harus bisa melanjutkan pengembangan tanpa
> memori, percakapan, atau alat dari sesi-sesi sebelumnya.** Semua fakta di sini
> diverifikasi pada tanggal di atas terhadap `origin/master` `052d6c9`, lalu **dimutakhirkan
> pada hari yang sama ke `8f434de`** setelah antrean PR ditutup; angka dan nomor baris
> menua — verifikasi ulang sebelum mengandalkannya.

Urutan baca untuk agen baru: **[`AGENTS.md`](../../AGENTS.md)** (aturan kerja & arsitektur)
→ berkas ini → [`memory/00-INDEKS.md`](memory/00-INDEKS.md) (peta pelajaran) →
[`../prompts-perbaikan/00-LANJUTKAN.md`](../prompts-perbaikan/00-LANJUTKAN.md) (arc aktif
terakhir) → [`../PRD-REGISTRY.md`](../PRD-REGISTRY.md). Prompt siap-salin untuk sesi
pertama: [`00-PROMPT-MULAI.md`](00-PROMPT-MULAI.md).

---

## 1. Keadaan repo saat serah-terima (terverifikasi)

| Hal | Nilai |
|---|---|
| Remote | `https://github.com/ari1945/Asseris.git` |
| `origin/master` | **`8f434de`** (2026-09-07). Rantai hari ini di atas `052d6c9` (#334, 2026-08-29): `1af7c1e` #341 serah-terima ini → `09eb979` #335 docs W2 → `2884106` #323 → `dc2ebf5` #337 → `2d0c697` #338 → `62a443e` #339 → `8f434de` #340. |
| Gerbang master | Keempat workflow (`ci` · `e2e` · `deploy-smoke` · `dependency-audit`) **hijau pada `8f434de`** dan pada tiap commit dependabot di atas. Periksa ulang: `gh run list --branch master --limit 12`. |
| Kerja belum-commit di checkout utama | **NOL.** Semua yang sebelumnya menganggur (4 termodifikasi + 16 untracked, terakhir disentuh 2026-08-22) di-commit ke cabang `wip/parkir-mesin-nol-pemanggil-2026-09-07` (`c3c5b20`, sudah di-push). Lihat §4. |
| Checkout utama | Di `master` = `origin/master`, pohon bersih. |
| Toolchain | Node **22** (`.github/workflows/ci.yml`), npm workspaces: root · `migration/` · `server/` · `e2e/` (lockfile ada di keempatnya). `migration` masih **terkunci di Vite 5 / Vitest 2** (lihat `docs/UPGRADE-BACKLOG.md`). |

### PR terbuka (per 2026-09-07, sesudah antrean ditutup)

| PR | Cabang | Isi | Keputusan yang dibutuhkan |
|---|---|---|---|
| **#336** | `claude/asseris-saas-evaluation-afx4af` | Evaluasi kesiapan SaaS (`docs/EVALUASI-SAAS-2026-08-31.md`) + **kode**: D3 firmId gagal-tertutup, D2 konektor milik firma (2 migrasi Prisma), B1/B2 undangan & reset password via SMTP. 49 berkas, `MERGEABLE` saat ditulis — master sudah bergerak 7 commit sejak itu, jadi `gh pr update-branch 336` dulu dan tunggu CI ulang. | **Reviu Ari.** Ini perubahan skema + auth; bukan docs-only. Jangan digabung tanpa membaca §"Koreksi terhadap roadmap" (SES → SMTP karena residensi data Jakarta). **Satu-satunya PR yang tersisa.** |
| (tanpa PR) | `wip/parkir-mesin-nol-pemanggil-2026-09-07` | Snapshot kerja terparkir. | **Jangan di-merge.** Lihat §4. |
| (tanpa PR) | `claude/app-development-plan-aws-rztqas` (remote saja, sesi cloud 2026-08-31, 3 commit docs di atas `052d6c9`) | **Kerja nyata yang belum mendarat**: `docs/RENCANA-PENYELESAIAN-AI-CLOUD-AWS.md` (program penyelesaian: eksekusi AI di cloud + hosting AWS), `docs/URUTAN-PENYELESAIAN-TABEL-PROMPT.md` (76 pekerjaan + prompt siap-tempel), `docs/TRIASE-DRAFT-2026.md` (triase 53 PRD Draft, 16 keputusan terkonsolidasi). | **Buka PR-nya** (docs-only). Isinya justru peta jalan yang dibutuhkan agen berikutnya — baca bersama §5 berkas ini; bila keduanya berselisih, yang lebih baru (berkas itu, 2026-08-31) menang untuk urutan pekerjaan. |
| (tanpa PR) | `claude/dreamy-keller-xczrnh` (remote saja, sesi cloud 2026-08-31, 2 commit di atas `052d6c9`) | `docs/EVALUASI-GAP-2026-08-29.md` (sapuan gap segar; lima cacat prioritas yang ternyata sudah mati) + **kode**: `fix(dms)` pelaku log akses SA 230 dari sesi, tiga tombol yang berhenti mengarang nama — `session_actor.ts` + 2 uji baru. | `update-branch` ke master lalu **buka PR**; CI harus hijau dulu (menyentuh `view_dms.tsx`). Kelas cacatnya = pola 3 (`AMS.USER` sebagai pelaku). |

Sudah mendarat hari ini, jangan dibuka ulang: **#335** docs W2 `DEFAULT_ENG_ID` (`09eb979`;
prompt kerja W2-1 kini ada di `docs/prompts-perbaikan/W2-1-DEFAULT-ENG-ID.md`) dan lima
dependabot **#323 #337 #338 #339 #340** (`zod` 4.4→4.5 lulus semua uji server; #339 sempat
konflik lockfile setelah tiga merge sebelumnya, sembuh lewat komentar `@dependabot rebase`).

### Cabang & worktree — SESUDAH pembersihan 2026-09-07 ("Proceed." Ari)

Pembersihan §7 **sudah dijalankan**. Hasilnya:

| Yang tersisa | Keterangan |
|---|---|
| `master` (lokal + remote) | satu-satunya worktree; pohon bersih |
| `wip/parkir-mesin-nol-pemanggil-2026-09-07` (lokal + remote) | kerja terparkir, §4 — simpan sampai keputusan M2 / W1 / arc home |
| `wip/parkir-worktree-tools-prisma-verify-2026-09-07` (lokal + remote, `755322c`) | snapshot kerja belum-commit worktree `awesome-torvalds` (2026-08-15): `tools/ensure-prisma-client.mjs` +226/−32, `tools/verify.mjs` +12/−4. **Kemungkinan besar sudah mendarat lewat #327** (perbandingan bentuk skema Prisma) — verifikasi blob sebelum memakainya. |
| `wip/parkir-worktree-timebudget-isolasi-2026-09-07` (lokal + remote, `3a41231`) | snapshot worktree `exciting-noyce` (2026-08-15): `view_timebudget.tsx` +108/−48 + `timebudget_engagement_isolation.test.ts` (absen di master). Arc isolasi Time & Budget mendarat lewat #266/#282 dengan bentuk lain — kemungkinan varian tersalip. |
| `wip/parkir-worktree-ppl-cpe-licensing-2026-09-07` (lokal + remote, `8e7e63c`) | snapshot worktree `nifty-kowalevski` (2026-08-22): `canon_ppl.ts` +82, `ppl_single_engine.test.ts` +251, `cpe_training.ts`, `data_licensing.ts`, `view_pc_hcm/pc_talent/people/personal.tsx`. Fungsi yang ditambahkannya (`pplYearOf`, `skpInYear`, `pplPeriod`) **sudah ada di master** dan `ppl_single_engine.test.ts` sudah 293 baris di master ⇒ hampir pasti tersalip (#319/#320). |
| `claude/asseris-saas-evaluation-afx4af` (remote) | = PR #336, menunggu reviu |
| `claude/app-development-plan-aws-rztqas` · `claude/dreamy-keller-xczrnh` (remote) | **kerja nyata dari sesi cloud 2026-08-31, belum ada PR** — lihat tabel PR di atas |

Yang dihapus: **39 cabang lokal** (semuanya nol berkas yang hanya ada di cabang; 9 di
antaranya akan meregresi master bila di-merge) dan **5 worktree** (`.claude/worktrees/*` ×4
+ `D:\Claude AI\_wt\pppk`). Tiga worktree ternyata masih memikul kerja belum-commit yang
tidak tercatat di mana pun — itulah tiga cabang `wip/parkir-worktree-*` di atas; tak ada
yang dibuang tanpa snapshot. Junction `node_modules` dilepas lewat `cmd /c rmdir` sebelum
`git worktree remove`; `node_modules` utama diverifikasi utuh sesudahnya.

---

## 2. Cara bekerja (ringkas; rinciannya di `AGENTS.md` §2)

```bash
npm run verify            # dari root — cermin persis CI; WAJIB hijau sebelum push
```

- `master` **selalu hijau** (BUILD.md §R-7). Cacat yang direpro tapi belum ditutup dikirim
  sebagai `it.fails()` / `it.skip()` + `// KARANTINA s/d <tanggal>`.
- Alur PR: buat cabang → `npm run verify` → push → PR → bila master bergerak `gh pr
  update-branch` dan tunggu CI ULANG → `gh pr merge --squash` → hapus cabang remote manual
  → tunggu 4 workflow master. CI bisa **tidak pernah start** walau PR `CLEAN` (GitHub
  menjatuhkan dispatch); obatnya `gh pr close` + `gh pr reopen`.
- Sebelum menyentuh cabang/berkas apa pun: `git status --short` dan sensus **blob**
  (`AGENTS.md` §8, butir squash). `git cherry`/"N ahead" menyesatkan di repo ini.
- Ratchet `:any` memerah **dua arah** (menambah → `check-any-ratchet`; menghapus →
  suppression basi, `eslint` exit 2). Penulis tunggal: `npm run lint:any-baseline`.
- Uji `.ts` wajib bebas `any` (`typecheck:test` adalah gerbang).
- UI Bahasa Indonesia; skala tipografi 8 ukuran, lantai 11px (`AGENTS.md` §5); token
  warna, bukan hex; kontrol form native.

---

## 3. Pengetahuan yang dipindahkan

**`docs/handover/memory/`** = 191 catatan memori agen (2026-07 … 2026-08-29), salinan
verbatim. Mulai dari [`00-INDEKS.md`](memory/00-INDEKS.md). Yang paling sering menggigit,
sebagai daftar periksa sebelum bekerja:

1. **Klaim "absen"/"belum ada" sistematis salah** — grep dulu; konverter/mesin yang
   "belum ada" sering sudah ada (`asseris-gap-matrix-eval`, `asseris-acceptance-engagement-flow`).
2. **Gerbang yang belum pernah merah tidak membuktikan apa pun**; gerbang regex bisa
   membaca komentar sebagai kode, lolos vakum lewat heredoc, atau buta pada ternari
   (`asseris-prisma-client-worktree-trap`, `asseris-tipografi-gerbang-buta-ternari`,
   `asseris-spr2400-pengiriman-dan-premis-salah`).
3. **Angka karangan** — fallback `|| 'ENG-2025-014'`, `|| 'KAP Wijaya Hartono & Rekan'`,
   `AMS.USER` sebagai pelaku, `new Date()` alih-alih `AMS.TODAY`, id dari `list.length+1`
   (`asseris-eng-fallback-kelas-tulis`, `asseris-arc-prompt-perbaikan-modul` §pola 1–9).
4. **Pemanggil tak mengirim ctx** → mesin `ctx.x || seed` diam-diam memakai seed; cacatnya
   di pemanggil (`asseris-apar-subbuku-hidup`).
5. **Kunci firm-scope tanpa cabang `capForWrite`** = jatuh ke `FIRM_ADMIN` = tulisan
   ditolak **senyap** (FORBIDDEN masuk cabang "offline" tanpa toast) —
   `asseris-mytasks-user-scope`, `asseris-p2pk-spm-readiness-arc`.
6. **Keputusan Ari yang mengikat:** migrasi lingkup firma→perikatan, **data lama HANGUS**
   (jangan tawarkan read-through) — `asseris-migrasi-lingkup-hangus`.
7. **`git checkout -- <berkas>` menghapus kerja belum-commit** (terjadi ≥2×);
   penanda konflik bisa ter-commit; `ln -s` di shell agen MENYALIN, bukan menaut.
8. **Sesi paralel pernah berbagi satu direktori kerja** — `verify` merah bisa milik arc
   lain; baseline dulu (`asseris-sesi-paralel-satu-worktree`).
9. **Segel Ed25519 sampai #334 tidak pernah menutupi isi tabel** (replacer rekursif),
   dan tombol "Verifikasi" tidak menghitung ulang hash (`asseris-segel-buta-isi-replacer`).
10. **CRLF/LF campuran per direktori & per berkas** — mutasi string bisa no-op senyap;
    cetak `changed: True/False`.

Dokumen sistemik lain: `BUILD.md` (sejarah W0–W15, Tahap 7–9), `docs/PROMPT-PERBAIKAN-MODUL.md`
(template prompt perbaikan per modul), `docs/PRD-RINGKASAN-KEDALAMAN-E9.md` (taksonomi
kedalaman L0–L5), `docs/EVALUASI-SAAS-2026-08-31.md` (di PR #336), `docs/DEPLOY.md`.

---

## 4. Kerja terparkir di `wip/parkir-mesin-nol-pemanggil-2026-09-07`

Diuji 2026-09-07 dengan menyalin berkas-berkasnya ke atas `origin/master` `052d6c9`:

| Berkas | Keadaan di atas master | Penghalang |
|---|---|---|
| `migration/src/mytasks_derive.ts` + `.test.ts` · `server/src/__tests__/mytasks_scope.test.ts` | **typecheck MERAH** (`DeadlineRow` tak punya `id`, 2 error) · uji server 5 gagal / 4 lulus (`stateAccess.ts` masih mendaftar `mt.meta`/`mt.personal` di lingkup firma — M1 tak pernah mendarat) | Keputusan **`docs/usulan-M2-mytasks-sumber-kebenaran.md`** (Opsi A server SSOT vs B klien) |
| `migration/src/wip_adj.ts` + `wip_writedown_authority.test.ts` | uji gagal (W4 SLA; `use_firm_wip`/`aje_approval` di master tak punya `APPROVAL_STEP_CAP` yang diandalkannya) | Keputusan **`docs/usulan-W1-wip-writedown-otorisasi.md`** (efek dulu vs otorisasi dulu) |
| `migration/src/home_composition.ts` + `.test.ts` | lulus, tapi **nol pemanggil**; `HM_FIRMOPS_AREAS` masih dideklarasikan inline di `view_home.tsx` ⇒ dua register untuk satu konsep | Arc `01-home` (`docs/prompts-perbaikan/01-home.md`) belum diselesaikan; P2 butuh axe + smoke papan-ketik (lokal) |
| `migration/src/a11y_anchor_href.test.ts` | gerbang sungguhan, MERAH pada master karena 1 pelanggaran hidup: `view_home.tsx` `<a onClick>` fallback offline "buka My Tasks" | Kirim bersama perbaikan satu barisnya (`<button type="button" className="linkbtn">`), buktikan merah dulu |
| `migration/src/use_firm_subledger.ts` | nol pemanggil; A1 apar sudah ditutup #318 dengan cara lain (`apOf(ctx)`/`coaOf(ctx)`) | Kemungkinan besar **buang**; verifikasi dulu apakah `view_firmgl`/`view_firmfinance` di master masih membaca register `invoices`+`firmap` lewat dua pintu |
| `data_firmfin.ts`, `view_firmfinance.tsx`, `view_firmgl.tsx`, `view_pipeline.tsx`, `apar_*`, `firm_gl_export*` | varian pra-#318; 11 hunk konflik bila di-apply; master sudah memuat versi yang lebih baru | **Buang.** |
| `docs/prompts-perbaikan/103-spr2400.md`, `130-invprop.md` | absen di master; keduanya prompt yang arc-nya SUDAH mendarat (#303, #314) | Arsip; boleh dimasukkan ke master lewat PR docs kalau ingin catatan lengkap |
| `docs/prompts-perbaikan/W0-*`, `W1-*`, `142-mgmtletter.md` (versi cabang) | draf lama; versi master (#326/#328/#330/#331) yang berlaku | Abaikan versi cabang. |

Repo **tidak punya gerbang variabel mati** (`no-unused-vars` mati; `docs/usulan-S1`), jadi
mesin tanpa pemanggil tidak akan pernah memerah — jangan di-commit ke master "untuk nanti".

---

## 5. Pekerjaan terbuka, diprioritaskan

Urutan ini rekomendasi penulis serah-terima, bukan keputusan Ari. Yang bertanda 🔴 adalah
cacat keamanan/integritas yang masih hidup di master.

### A. Menutup antrean (kecil, segera)
1. Buka PR docs dari `claude/app-development-plan-aws-rztqas` (peta jalan 2026-08-31) dan
   gabungkan — lalu selaraskan §5 berkas ini dengannya.
2. `update-branch` + buka PR dari `claude/dreamy-keller-xczrnh` (fix DMS pelaku sesi);
   gabung bila CI hijau.
3. Reviu & putuskan **#336** (`update-branch` dulu).
#335 dan lima dependabot sudah mendarat 2026-09-07.

### B. 🔴 Arc identitas & segel ekspor — `docs/prd-export-seal-identity-ssot.md` (In Progress)
F-1/F-2 (#332) dan F-3 (#334) mendarat. Sisa:
- **F-4** penolakan ekspor terlihat di UI (`emitExportRefusal` sudah disiarkan; toaster
  belum ditinjau visual; axe hijau). **Terkopel dengan keputusan `DEFAULT_ENG_ID`** —
  PRD §"Amandemen B": jangan diputuskan terpisah.
- **F-5** 20 situs `engLabel` di `refNo`/`meta` ⇒ SC-2 + gerbang repo-lebar. ⚠ #333 §13:
  jangan pakai angka §1 PRD sebagai baseline gerbang — hitung ulang.
- **F-6** registri → Implemented.
- **Keputusan Ari (Q-3 trust-model):** baris segel belum menyimpan `firm` padahal `firm`
  ikut di-hash sejak V2 — rekam **klaim klien** atau **fakta server** (`ctx.user.firmId`)?
  Dicatat di §12 PRD.
- **Temuan kedua F-3, belum dijadwalkan:** ketiga pemanggil `exportVerifySeal`
  (`view_onboarding:314` · `view_onboarding2:205` · `view_continuance:291`) menyerahkan hash
  yang tersimpan sejak penyegelan ⇒ verifikasi tak pernah menghitung ulang ⇒ tombol
  "Verifikasi" hanya membuktikan baris segel ada. Butuh verifier yang menghitung ulang
  payload per `sealFormat`. Ini PRD tersendiri.

### C. 🔴 W2 — `DEFAULT_ENG_ID` (prompt W2-1 ada di PR #335)
Empat peran, satu cacat: (1) `contexts.tsx` fallback lingkup persist → **cabut** (jebakan
laten, praktis tak pernah dijalani karena peran 2); (2) nilai awal perikatan terpilih →
**ditahan**, pilihan produk, mencabutnya menjadikan F-4 prasyarat + PRD sendiri; (3)
`app.tsx` definisi kedua literal yang sama → dedupe; (4) `view_newdisc.tsx` penanda
kepemilikan → ganti nama saja. Server `router.ts` `z.string().min(1)` menolak scope kosong
— pahami sebelum mencabut (1).

### D. 🔴 Sisa kelas `|| 'ENG-2025-014'` jalur TULIS
Sensus 2026-08-27 (`asseris-eng-fallback-kelas-tulis`): 17 situs WRITE-PATH, didominasi 16
`view_psak*.tsx` (`scopeId: eng?.id` → `exporter.seal.mutate` + `logEvent.mutate`) +
`view_clientportal.tsx:369` (menulis PBC). #332 mencabut `scopeId` dari **model ekspor**,
tapi `logEvent`/`pbc.v2` bukan ekspor — **sensus ulang wajib** sebelum menyatakan tertutup.
Grep satu baris memberi NOL hasil karena fallback dan konsumsinya beda baris; telusuri
variabelnya.

### E. Usulan yang menunggu keputusan Ari (tidak boleh diputuskan agen)
`docs/usulan-*.md` — 19 berkas. Yang memblokir kerja terparkir/arc: **M2** (My Tasks SSOT),
**W1** (write-down WIP), **O1b** (saldo awal jadi kertas kerja), **A3** (pembayaran utang
memposting jurnal), **S1** (gerbang variabel mati), **TB3** (bobot fase), **J** (impor GL
untuk JET), **IA6/IA7** (audit internal), **FA2**, **B4/B6**, **P2**, **R3/R6**, **SPR** ×2,
**mgmtletter-seed-identitas**. Format jawaban yang diharapkan: satu opsi per usulan, lalu
agen mengeksekusi.

### F. Arc yang sudah disetujui ("Proceed.") tapi belum dikerjakan
- `docs/prd-firm-erp-deepening.md` — PR-1 mendarat (#258); **PR-2 (restrukturisasi COA
  aset tetap; angka siap di `memory/asseris-firm-erp-deepening-arc.md`) → PR-4; PR-3/5/6**
  independen setelah PR-2. Q-2: gerbang merah MEMBLOKIR ekspor LK. Q-5: `revenue`/
  `fixedassets` tetap read-only.
- `docs/prd-delivery-milestones-deepening.md` — PR-4..PR-6 belum.
- Cockpit PR-C-8 (aksi tulis ber-RBAC) dipisah, belum (`PRD-REGISTRY`).
- P2PK PR-5 (menunggu Q5 substansi POJK 9/2023) dan PR-7.

### G. Utang aksesibilitas (Tahap 9)
35 kontrol tanpa label · 71 tombol tanpa handler (sensus 2026-08-27) · 4 permukaan drag
hanya-tetikus: `view_dashboard.tsx` (lewat `<Portlet>` di `ui.tsx` — kena semua pemakai),
`view_firm.tsx`, `view_mytasks.tsx`, `view_pipeline.tsx` · anchor `view_home.tsx` (§4).
Aturan: menamai kontrol MATI lebih buruk daripada membiarkannya tak bernama — cabut atau
sambungkan (`asseris-icon-button-names`). jsdom tidak mensintesis Enter→click; bukti
papan-ketik butuh Playwright.

### H. Modul yang belum dibuatkan prompt perbaikan
Dari 158 modul (`migration/src/icons.tsx`), 25 sudah diprompt dan 21 mendarat. Kandidat
berikut menurut kedalaman E-9: `relatedsvc` (stub 12 baris), `sakep` (nol view), `sa200`,
`sa800/sa805/sa810/spr2400`, `sa501`, `sjah3000` (cangkang display-only — pertanyaannya
produk, bukan teknis), `records`, `audittrail`, `firmfinance`, grup PSAK #110–136 (27 modul,
pola seragam). Metode: `docs/PROMPT-PERBAIKAN-MODUL.md` + §A–F di `00-LANJUTKAN.md`.

### I. Upgrade & platform
`docs/UPGRADE-BACKLOG.md`: React 18→19, Prisma 6→7 (config `url` pindah ke
`prisma.config.ts`), Vite 5→7 / Vitest 2→3 (`migration` terkunci). Masing-masing cabang
sendiri, gerbang penuh + verifikasi live; lepas `ignore` dependabot saat mulai.

### J. Data yang hanya Ari yang punya
Lampiran PMK 168 (PPh 21 TER) · cuti bersama SKB 2026 · `jpCap` (batas upah JP BPJS
tahun berjalan). Tanpa ini, regref set terkait tetap memakai nilai bertanda "belum
dikonfirmasi".

### K. Kebersihan yang DITUNDA atas keputusan Ari (2026-08-28)
Memindahkan 61 PRD dari root ke `docs/prd/` (`memory/asseris-prd-root-pindah-docs.md`) —
ditunda "sampai cabang lokal mendarat"; cabang lokal kini sudah bersih (§1), jadi syaratnya
terpenuhi. `prd_registry.test.ts` memindai `''` dan `docs` — pemindahan harus memperbarui
pemindai dan registri sekaligus.

---

## 6. Yang khusus Claude Code dan padanannya

Sudah dipetakan di **`AGENTS.md` §9**. Ringkasnya: instruksi → `AGENTS.md`; memori →
`docs/handover/memory/`; panel Browser & `launch.json` → perintah manual + Playwright;
`.claude/worktrees` → `git worktree`; `.claude/*.mjs` → skrip sejarah, biarkan.
`D:\Claude AI\CLAUDE.md` (instruksi Ari, di luar repo) → `AGENTS.md` §0.

Dua hal yang **tidak** bisa dipindahkan ke repo dan harus diterima hilang: (a) riwayat
percakapan sesi-sesi lama (kesimpulannya sudah di memori dan dokumen), (b) keadaan
peramban/login dev yang dipakai verifikasi visual — pakai akun seed di BUILD.md §W7.

---

## 7. Resep kebersihan cabang (SUDAH dijalankan 2026-09-07 — disimpan sebagai prosedur)

Sensus 2026-08-27 (`memory/asseris-sensus-cabang-2026-08-27.md`): dari ±40 cabang lokal,
hanya yang di §1 yang berarti; **sembilan** di antaranya akan **MEREGRESI** master bila
di-merge (mengembalikan hex mentah pra-#311/#313, menghapus 88 baris uji e2e a11y,
melonggarkan ratchet `:any`). Jangan merge apa pun dari daftar debris; hapus saja.
Pelajaran saat menjalankannya: **periksa `git status` tiap worktree sebelum
`worktree remove`** — tiga dari lima ternyata memikul kerja belum-commit; snapshot dulu ke
cabang `wip/`, baru hapus.

```bash
# 1) worktree dulu (lepas junction node_modules lebih dulu — lihat §1)
git worktree list
# 2) cabang yang upstream-nya sudah hilang ([gone]) dan pohonnya sama dengan master
git for-each-ref --format='%(refname:short) %(upstream:track)' refs/heads | grep gone
# 3) untuk tiap kandidat: buktikan tak ada berkas yang HANYA ada di sana
git diff --name-status origin/master <cabang> | grep '^A'
# 4) baru hapus
git branch -D <cabang>
```

Cabang yang WAJIB disimpan: `master`, keempat `wip/parkir-*`, dan dua cabang remote sesi
cloud (`claude/app-development-plan-aws-rztqas`, `claude/dreamy-keller-xczrnh`) sampai
PR-nya mendarat.

---

## 8. Checkpoint akhir

- **Current context:** master hijau (4 workflow); nol kerja belum-commit; satu worktree;
  satu PR terbuka (#336, butuh reviu) + dua cabang cloud 2026-08-31 tanpa PR (peta jalan &
  fix DMS); arc aktif = identitas/segel ekspor (F-4..F-6) dan W2 `DEFAULT_ENG_ID`.
- **Decisions made (oleh Ari, mengikat):** PRD dulu + "Proceed."; data lama migrasi lingkup
  HANGUS; hanya W1-E yang dikerjakan dari W1, sisanya ditutup #332; peran 2 `DEFAULT_ENG_ID`
  ditahan; firm-erp Q-1..Q-5; delivery Q-1..Q-5.
- **Open questions:** 19 usulan (§5.E) + Q-3 trust-model segel + verifier hitung-ulang +
  #336.
- **Next actions:** §5.A lalu §5.B/C (terkopel), sambil menunggu keputusan §5.E.
