---
name: neosuite-ams-p2-wp-signoff
description: "NeoSuite AMS feature stream P2 — evidence + sign-off lintas-WP: status, arsitektur, next = Fase 3"
metadata: 
  node_type: memory
  type: project
  originSessionId: 89dc65e3-ed56-41d4-9f38-0dd0a0cb4485
---

Feature build **P2** (dari [[neosuite-ams-arc]] evaluasi gap fitur): jadikan modul "kertas kerja auditable" = sebar **sign-off + tautan bukti** lintas-WP. Eval read-only menemukan: hanya 6/161 view tier auditable; suite PSAK 0% persist. PRD = `Audit System/PRD - Evidence & Sign-off Lintas-WP.md`; eval = `Audit System/Evaluasi Fitur NeoSuite AMS - Gap & Pendalaman.md`. **Keputusan user (terkunci):** localStorage (bukan W6) · 2-tingkat (preparer/reviewer) · lock LUNAK (boleh buka kembali) · ~30 WP (suite PSAK + SA 500/540/501 + Core Execution) · `requiredEvidence` seed dari SA.

**2 refinement arsitektur atas PRD (PENTING):**
1. **SSOT — reuse `wpState` kanonik, BUKAN store paralel.** `wpState` = `usePersisted('wpState',{})` di AuditProvider (`contexts.jsx`) → persisted `ams.v1.wpState`, keyed per ref; tulis via `setWp(ref, patch)` (merge); baca status via `window.deriveWpStatus(ref,audit,firm)` (def di `view_wp.jsx`). Sign-off modul = tanda tangan WP-ref kanoniknya → muncul juga di register Kertas Kerja.
2. **Spread via SubBar global, bukan 30 edit view.** Tiru `EvidenceControl` (di `shell.jsx` SubBar, render `<WpSubBarControl moduleId/>` SEBELUM `<EvidenceControl/>`); registry-driven `WP_MODULE_MAP` → satu edit shell mencakup semua modul terpetakan.

**Fase 0-1 = DONE & committed** (`ff16099`; docs `e8b85a4`). Komponen di **`migration/src/wp_signoff.jsx`**: `WpSignoff` (2-tingkat, soft lock), `WpEvidenceLink` (required vs attached), `WpStatusBadge`, `WpPanel` (drop-in), `WpSubBarControl` (kontrol SubBar, null bila modul tak di map), `wpCompletenessFor(audit,moduleIds)` (rekap Fase 4). `WP_MODULE_MAP` (~24 modul): key = ref huruf kanonik bila ada (psak14→C, psak16→E, psak71→B, psak73/lease→F, psak24→H, revenue→R, sad→810, opinion→900, calc→300), selain itu = moduleId. Signer = `useAuth().user.name`. Evidence global keyed moduleId (`amsEvidenceCount`, store `ams.v1.evidence`). **Verified Vite :5180:** PSAK16 SubBar tampil "Kertas Kerja"+"Bukti"; preparer→reviewer persist `ams.v1.wpState.E` (Draf→In Review→Reviewed); 0 console err. lint/typecheck/build hijau; canon tak tersentuh (49 test).

**Gotcha:** hook guard `(typeof useX==='function')?…` MEMICU lint `rules-of-hooks` (sudah error-gate sejak W3 P4) — panggil hook langsung. Nav untuk test: set `localStorage['ams.route']=<moduleId>` + reload (tak ada nav global).

**Fase 3 = DONE & committed** (`70405a6`). `WP_MODULE_MAP` 23→**34 modul unik** (35 key incl. alias `lease`). +11: PSAK `psak72`(→ref `R`, alias `revenue` spt `lease`/`psak73`→F), `psak19`/`psak1`/`psak2` (ref=moduleId); SA-500 `sa520`/`sa530`/`sa580`; Core Execution `aje`/`analytical`/`sampling`/`jet`. **Sengaja DIKECUALIKAN:** `psak117`+`isak35` (punya bespoke p117/I35Signoff → jangan tambah duplikasi baru, biar Fase 2 yg satukan); SA 700/800 + SPR/SJAH asurans (di luar scope PRD terkunci); `wtb`/`workpapers` (bukan satu WP yg ditandatangani). **Verified Vite :5180:** sa520 SubBar tampil "Kertas Kerja", preparer→`ams.v1.wpState.sa520` (status "In Review"), 0 console err; lint/build hijau; canon tak tersentuh. **Gotcha test:** route = string POLOS `localStorage['ams.route']='sa520'` (JANGAN JSON.stringify — kutip jadi bagian moduleId → tak match map); popover `.ev-pop` render di tick React berikutnya (klik di satu eval, baca di eval terpisah).

**Fase 4 = DONE & committed** (`d986b33`). Komponen baru `WpCompletenessRecap` (di `wp_signoff.jsx`, import `Progress`): 2 bar `Progress` — sign-off% (hijau, reviewer-signed) + bukti% (teal) — atas semua modul auditable (default `Object.keys(WP_MODULE_MAP)`, baca `useAudit().wpState` + store evidence). Dipasang di panel **"Kesiapan Opini & EQR"** (`view_cockpit2.jsx` `TabRisiko`, atas Gauge gate). **Hardening:** `wpCompletenessFor` kini DEDUPE per ref kanonik (`Set` of wpKey) → modul ber-alias (`lease`/`psak73`→F, `revenue`/`psak72`→R) dihitung SEKALI; perbaiki double-count laten dari alias Fase 3. **Verified Vite :5180:** recap render 2 bar, **total=33** (35 key − 2 ref dup), live (seed sa520 Reviewed → 1/33 sign-off = 3%), 0 console err; lint/build hijau; canon tak tersentuh. Catatan: cockpit gate lama pakai data `workpapers` legacy; recap pakai `wpState` kanonik — dua sumber beda, sengaja.

**Fase 2 = DONE & committed** (`4576ec3`) — **P2 SELESAI**. Migrasi 6 bespoke nyata (SignoffTab di `view_wp.jsx` ternyata sudah kanonik, bukan target). Tiga pola:
1. **Statis cetak** (`p71/p117/sySignoff` → `*Defaults()`): blok tanda tangan kertas kerja CETAK baca chain via helper MURNI baru `wpSignersFor(audit,moduleId,defaults)` (di `wp_signoff.jsx`): preparer←chain.preparer, reviewer←chain.reviewer, approver←chain.partner, fallback ke default statis. `psak117`+`syariah` MASUK map (ref=moduleId); komponen tambah `useAudit()` di WorkPaper.
2. **Interaktif 2-tingkat** (`FSSignoff`/`I35Signoff`): rewrite → `useWpSignoff(moduleId)`; parent (`view_fsgen`/`view_isak35`) derive bentuk `{prepared,reviewed}` dari wpState utk KPI/footnote; store `fsgen.signoff`/`isak35.signoff` DILEPAS (key LS lama jadi yatim, OK). `fsgen`+`isak35` masuk map.
3. **Opini = mirror** (keputusan user terkunci): `OpinionSignoff` tetap utuh (3-tingkat manager/partner/eqr + checklist SA700 + finalize → store `opinionDoc.signoff`), TAPI `sign`/`finalize` MIRROR ke `wpState['900']` (manager→reviewer, partner→partner, eqr→eqr, auto-preparer "Generator Laporan", finalize→status Reviewed). `WP_SUBBAR_HIDE={opinion:true}` sembunyikan chip "Kertas Kerja" SubBar di opini (satu kontrol).

**Perbaikan bonus:** `useWpSignoff` kini `locked` dari `useFirm().locked` (sebelumnya `audit.locked` SELALU undefined → lock arsip tak pernah aktif di seluruh lapisan kanonik; kini aktif). WP_MODULE_MAP 34→**38 ref unik**.

**Verified Vite :5180** (route = string polos `localStorage['ams.route']='isak35'` + reload; eval render butuh 1 tick — baca DOM di eval TERPISAH dari reload/click): isak35 preparer→reviewer persist `wpState.isak35` (Reviewed); opini sign→`wpState['900'].chain.reviewer`+preparer auto; psak71 cetak baca `wpState['B']` (seed nama khas tampil); psak117/syariah fallback default + chip map; chip opini hilang (hanya "Bukti"); **0 console err**. lint/typecheck/build hijau; canon 49 test utuh. Gotcha: tab "Kertas Kerja A-1/S-1" beda dari chip SubBar "Kertas Kerja" — regex match harus spesifik.
