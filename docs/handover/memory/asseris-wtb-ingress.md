---
name: asseris-wtb-ingress
description: "W-WTB·1..·4 ingress/integritas/mapping/ledger + A1/A2/A3/B — ✅ MERGED ke master via PR #40 (c89b047)"
metadata: 
  node_type: memory
  type: project
  originSessionId: f5947fa0-b38d-4062-89f6-56d6c4b2f23a
---

Evaluasi modul Working Trial Balance (2026-06-25) menemukan ingress data **fiktif**: "Sync GL" no-op, `AMS.WTB` literal 28-baris hardcode satu engagement, label "tersinkron 2 jam lalu" palsu. Hilir (materialitas/GC/PSAK/FS via `figuresFromWTB`→`AMS_CANON`) arsitektur SSOT-nya BENAR & kuat — tak perlu dibongkar.

**W-WTB·1 (ingress paste/CSV) — SHIPPED, commit `00d0729`, PR #36 (https://github.com/ari1945/Asseris/pull/36) open vs master.** PRD: `PRD - W-WTB-1 Ingress TB Nyata (Paste-CSV).md`. Fase 0–3 tuntas, semua gate hijau (typecheck 0, lint ratchet aman, build ok, 318 migration + 137 server test), live-verified sebagai Manager.
- **Fase 0** `wtb_import.ts` (+`.test.ts`, 15 uji): parser/validator PURE — deteksi delimiter, header sinonim id/en + fallback posisional, kolom Debit/Kredit, parse angka id-ID (`1.850.000.000`, kurung negatif, Rp, —), infer grup dari prefix kode, gerbang balance bertoleransi (0,01% total aset, lantai Rp 1.000), `computeCoverage()` (kejujuran engine PSAK via `WTB_MAP` SSOT-import).
- **Fase 1** wiring: `contexts.tsx` StateDoc `wtbImport` (engagement-scope) + base-WTB swap (`baseWtb = wtbImport?.rows?.length ? rows : D.WTB`) di atas lapisan override/AJE; `rbac.ts` `capForWrite` case `wtbImport`→`WP_EDIT`; CONFLICT_LABELS.
- **Fase 2** UI `WtbImportDrawer` di `view_execution.tsx`: tombol "Sync GL"→"Impor TB" (+"Add Account" dihapus), drawer tempel→pratinjau→validasi+coverage→Terapkan (gated `can(CAP.WP_EDIT)`), tombol "Kembali ke demo", label provenance JUJUR (seed demo vs "Sumber: impor paste-csv · <ts> · seimbang ✓"). `SAMPLE_TB` impas (Σ=0 & aset=liab+ekuitas).
- **Fase 3** live: impor→WTB KPI 316,6M→27,5M; persist lintas reload (server); **bukti lintas-modul: FS Generator TOTAL ASET 27.500** dari `FSGEN.buildModel(wtb)`; revert→demo. (Going Concern & FS Gen baca `useAudit().wtb` reaktif.)

**Gotcha penting (ratchet `no-explicit-any` di .tsx lama):** kode baru WAJIB bebas-`any`. React types TAK tersedia (`React.MouseEvent/ChangeEvent` = error) → pakai tipe struktural (`(e:{target:{value:string}})`). `useMemoX`/`useServerState` bertipe `any` → JANGAN type-arg generic; anotasi const (`const parsed: ParseResult|null = useMemoX(...)`) agar param map ter-infer (hindari implicit-any cascade). Param map JSX anotasi eksplisit tipe impor (WtbIssue/CoverageEngine/ImportedWtbRow). `I.info` TIDAK ADA (cek ikon dulu, salah → layar putih).

**Batas jujur W-WTB·1:** `figuresFromWTB` kunci kode tetap → bagan akun klien beda TAK menyalakan engine PSAK (di-surface via indikator cakupan, BUKAN disembunyikan). Drill sub-ledger masih sintetik.

**W-WTB·2 (gerbang integritas) — SHIPPED, commit `0624443`, PR #37 (stacked di atas #36, base `feat/wtb-ingress-paste-csv`).** `wtb_integrity.ts` (+9 uji): `checkWtbIntegrity()` PURE — footing Σadj INFO sadar-laba (Σ≈−laba=normal pra-tutup), rekon neraca (aset=liab+ekuitas, selisih≈laba wajar), adj=unadj+aje, rekon AJE (Σkolom=0 & tie ke `ajeRegisterByAccount` proyeksi register dr/cr+lines[]); status graduated ok|attention, TAK memblok. `view_execution.tsx`: chip status nyata gantikan "Balanced" kosmetik + `WtbIntegrityPanel` (3 tile + pesan + tabel selisih AJE per akun). **TEMUAN: data seed demo LONGGAR** — footing Σ=−14,66 M (=−laba, RE demo ganda-hitung laba), Σ AJE WTB=−3,12 M & 4 akun tak tie register (mis. 4-1100 sisi debit AJE-03 tak terbukukan). Sesuai keputusan Ari: nol perubahan seed → gerbang surfacekan jujur sbg "Perlu perhatian" (graduated). TB impor seimbang → hijau.

**W-WTB·3 (editor pemetaan CoA, Tier 1+2) — SHIPPED, commit `1d0d65c`, PR #38 (stacked di atas #37).** `wtb_mapping.ts` (+10 uji): `STANDARD_COA` (28 akun, canonKey reverse-`WTB_MAP`), `autoMap` (identitas kode + cocok nama ternormalisasi), `applyMapping` (relabel + MERGE baris menuju kode sama; tak-terpetakan dipertahankan), `mappingCoverage` (reuse `computeCoverage`). `contexts.tsx`: StateDoc `wtbMapping` + `baseWtb` terapkan `applyMapping` pada baris terimpor (di bawah override/AJE). `rbac.ts` case `wtbMapping`→WP_EDIT. `view_execution.tsx`: `WtbMappingDrawer` (tombol "Petakan Akun" saat ada impor) — strip cakupan live + Saran otomatis + select CoA ber-grup ◆=pemicu PSAK. **Mekanisme = alias kode → nol perubahan canon.** Live: impor TB kode-klien (cakupan 0%) → auto → 14/14 akun, 14/28 FS, 4/6 PSAK → terapkan → FS Gen Aset tetap neto 14.000 / Piutang neto 7.500 (CKPN tercermin). Gotcha: merge baris ubah `key` → wtbOverrides lama bisa yatim (edge, set mapping sebelum dokumentasi analitis).

**W-WTB·4 (sub-ledger drill nyata) — SHIPPED, commit `5640001`, PR #39 (stacked di atas #38).** `wtb_ledger.ts` (+10 uji): `parseLedger()` (delimiter/header id-en/posisional/Debit-Kredit, reuse `parseAmount`)→`byCode`; `ledgerForRow()` tie-out Σ GL vs `unadj` SADAR `srcCodes`. `contexts.tsx`: StateDoc `wtbLedger`. `rbac.ts` case `wtbLedger`→WP_EDIT. `view_execution.tsx`: `WtbLedgerDrawer` (tombol "Impor GL") + `WtbDrill` tab Buku Besar → baris GL nyata + badge tie-out bila ada, else sintetik berlabel jujur "ilustratif" (ganti klaim palsu "tie-out ✓" statis). Live: drill Kas 3 baris nyata TIE-OUT COCOK ✓; Persediaan→sintetik. CATATAN: demo kini punya SAMPLE_GL persist utk Kas(tie)/Piutang(untied) — display-only, tak ada UI clear (re-impor utk ubah).

**ROADMAP W-WTB TUNTAS (·1..·4) — ✅ MERGED ke master (2026-06-26).** KOREKSI: stack #36→#37→#38→#39 TAK pernah di-merge berurutan; semuanya OPEN sampai **PR #40 (di-root langsung ke master, berisi commit ber-oid identik ·1..·4 + A1/A2/A3/B) di-merge → master `c89b047`**. #36 auto-MERGED (oid sama), #37/#38/#39 CLOSED sebagai diserap #40, semua branch remote dihapus. (Pelajaran: jangan tulis "merged" sebelum `gh pr list --state all` mengonfirmasi.) Opsi lanjut: bersihkan data seed demo agar TB ter-foot ketat + AJE tie (terpisah, sentuh snapshot canon); ingress XLSX upload; konektor GL nyata (W9). Gotcha umum semua fase: ratchet `no-explicit-any` di .tsx (React types absen → tipe struktural event; jangan generic di `useMemoX`/`useServerState`, anotasi const), cek ikon `I.*` ada sebelum pakai. Lanjutan ingress: XLSX upload, konektor GL (W9). Lihat [[asseris-module-evaluation]] [[neosuite-ams-arc]].
