---
name: asseris-personal-data-isolation
description: "Isolasi data personal berjenjang (self/unit/firm) + granular per-jenis + modul \"Data Personal Saya\" — PRD 2026-07-05, IMPLEMENTED+live-verified, MERGED ke master via PR #61 (merge commit 2dc48c6, 2026-07-18)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 05e06fe6-1c0d-44cd-9a73-9c2dad0b3b2d
---

> ✅ **MERGED (2026-07-18).** SELURUH pekerjaan 5 ronde di bawah **IMPLEMENTED + gerbang hijau + live-verified**, kini **SUDAH MERGED ke `master`**: branch `feat/personal-data-isolation` (commit `35768a9` + `b36f6a9` rbac admin console) via **PR #61** (https://github.com/ari1945/Asseris/pull/61), merge commit **`2dc48c6`**. 6 CI check PASS sebelum merge. Snapshot `canon_regression...snap` (EOL-only, pre-existing) SENGAJA tak di-commit. **⚠ AKSI SESI DEPAN:** (1) sinkron lokal: `git checkout master && git pull` (master lokal dulu punya `b36f6a9` unpushed—kini di origin via PR, jadi FF bersih); (2) **`npm run seed`** WAJIB (seed nulis payrollData StateDoc, agar EMP-501/601 & data personal masuk); (3) branch `feat/personal-data-isolation` sudah merged—boleh dihapus. Ronde-5 = "Detil" jadi halaman + self-service — lihat butir (5). Branch saat ini `master`. **Aksi pertama:** `git status`/`git diff --stat` untuk lihat cakupan, lalu **buat branch** (mis. `feat/personal-data-isolation`) sebelum commit (jangan commit di master). Usul commit granular: (a) RBAC+server core, (b) view leak-fixes + modul baru, (c) kunci modul SDM, (d) penyederhanaan karyawan+firm-ops+Detil-modal — atau satu commit besar bila Ari mau. **Sebelum commit:** re-run gerbang (mig `npm run typecheck && npm run lint && npm test && npm run build`; server `npm run typecheck && npm test`) + `npm run seed` (seed nulis payrollData StateDoc; DB perlu fresh). Server preview sudah dihentikan — restart via preview_start `dev-all` atau `cd migration && npm run dev:all`. **Residual opsional** (bukan permintaan Ari): `STAFF.rating/util` roster HCM statis (HCM sudah ter-gate); FIRM_STAFF tak muncul di roster payroll audit. Detail teknis lengkap tiap ronde di bawah ↓.

**PRD "Isolasi Data Personal (Privacy) & Halaman Data Personal Saya"** (2026-07-05, sign-off "Proceed."). Setiap pegawai hanya lihat data personalnya sendiri di modul grup **Firma**; pengecualian berjenjang + granular. **IMPLEMENTASI SELESAI, seluruh gerbang hijau, live-verified via API+browser. SUDAH di-commit + push (commit `35768a9`), PR #61 OPEN + CI hijau.**

**Temuan awal (koreksi Agent-3 yang KELIRU):** fondasi `personal.get` self-scoping SUDAH ada sejak 2026-07-01 ([[asseris-nav-beranda-restructure]]) untuk 10 key — `contexts.tsx:~180` sudah route personal keys ke `api.personal.get`. Yang bocor: view baca `AMS.*` STATIS (bypass): `HR_CASES`(sanksi), `CPE_LOG`(PPL base), `STAFF_PROFILE`(PII), `AML_SCREENING`, `LEAVE_BALANCE`, `PERF_CYCLE.goals`. Plus lubang **version-0**: `contexts.tsx` dulu hanya adopsi hasil server bila `version>0` → fallback ke seed penuh `AMS.*` di DB baru.

**Model = 2 sumbu: KATEGORI(7) × CAKUPAN(self→unit→firm).** 14 cap baru di `rbac.ts` `personal.<kategori>.viewUnit|viewFirm` (payroll/leave/perf/cpe/conduct/indep/profile). **DECOUPLING kunci:** jalur baca `personal.get` TAK lagi cek `HR_MANAGE`/`FIRM_ADMIN` — hanya cap personal. (HR_MANAGE tetap gate TULIS via `capForWrite`; FIRM_ADMIN tetap admin.) 2 peran baru: **`Rekan Pemimpin`** (Managing Partner, semua `.viewFirm`) & **`Rekan`** (partner otonom, `.viewUnit`). `Engagement Partner` tetap = partner default **self-only** (mode tersentralisasi; otonomi = opt-in assign `Rekan`). Grant: Finance=`payroll.viewFirm` saja; Admin&HR=semua `.viewFirm`.

**Unit (rumah tangga partner):** `STAFF[].unit` + `AMS.UNITS{id:{name,lead}}` (data_part1.ts; 3 unit: U-KOM/Rudi, U-JK/Sari, U-LEAD/Hartono). `server/personalScope.ts unitSubtree()` = eksplisit UNITS→**fallback ORG reports-to** (OQ5=keduanya).

**File kunci disentuh:** `rbac.ts`(+14cap/+2role/PARTNER_BASE/capForWrite+6key) · `server/personalScope.ts`(REWRITE: +6 PERSONAL_KEYS=16 total, `KEY_CATEGORY`/`CATEGORY_CAP`, `personalPopulation`, `filterPersonalByPopulation`, `unitSubtree`, `seedForKey` fallback) · `server/router.ts`(personal.get pakai populasi+seed-fallback) · `contexts.tsx`(PERSONAL_STATE_KEYS+6, personal keys SELALU adopsi server walau version 0, jangan cache firm-scope bersama—hindari bocor lintas-user, mulai kosong-aman) · view leaks: `view_pc_conduct`(hrCases/amlScreening+myStaff filter), `view_people`(cpeLog+canFirm gate+guard), `view_pc_hcm`(Profile360Drawer 7 key via personal.get), `view_hrops`(leaveBalance/perfGoals+guard) · **NEW `view_personal.tsx`** modul `personal` grup "Saya" (icons/app/main). Seed: Hartono→Rekan Pemimpin, Rudi→Rekan, Sari→Engagement Partner; login dev Rudi `Rekan#2025!`.

**GOTCHA server:** `loadAmsSeed` (seedData.ts) dulu HANYA impor `data.js` → `ORG`/`STAFF_PROFILE`/`HR_CASES`/`AML_SCREENING`/`ETHICS_DECL`/`GIFTS_REGISTER` (augmentasi IIFE `data_people.js`) **undefined di server** → unitSubtree ORG-fallback & seedForKey kosong. FIX: tambah `await import('../../migration/src/data_people.js')` (side-effect augment AMS singleton). Uji `unitSubtree` fallback yang menangkapnya.

**GOTCHA lint:** edit file ber-`:any`-baseline menggeser nomor baris → suppressions stale (248 err). FIX: `npm run lint:any-baseline` (regenerasi). Modul `.tsx` BARU wajib bebas-any (view_personal pakai tipe eksplisit + `rec<T,>`/`arr<T,>` trailing-comma).

**Gerbang:** typecheck 0 (mig+srv), lint 0, **server 267/267**, **migration 429/429 (fingerprint canon identik—nol regresi)**, build OK. Tes diperbarui: `rbac.test.ts`(matriks + blok cap personal berjenjang), `personal_scope.test.ts`(REWRITE: self/unit/firm/decoupling/version-0-seed), `bootstrap.test.ts`(6→8 role).

**Live-verified (API tRPC + browser :5180, DB-backed roleStore pasca reseed+restart):** payroll/hrCases/staffProfile/leaveReqs baris terlihat — RekanPemimpin 10/4/3/5(firm), Rudi 4/2/1/3(unit U-KOM saja, BUKAN U-LEAD), Engagement Partner(Sari) 1/0/0/0(self meski FIRM_ADMIN), Senior(Dimas) 1/0/1/1(self), Admin&HR 10/4/3/5, **Finance 10/0/0/0 (gaji-semua TANPA SDM=granular)**. Modul "Data Personal Saya" render 8 seksi self-only (browser, Dimas). 0 console error.

**LANJUTAN sesi sama — penguncian MODUL (bukan filter baris):** Ari minta modul manajemen SDM firma **Rekrutmen & Onboarding**(`recruitment`), **Pelatihan & Kompetensi**(`learning`), **Suksesi & Karier**(`succession`) hanya untuk **Partner + HRD** — pegawai lain diblokir lihat keseluruhan. Ini **module-level gate**, beda dari row-filter. Implementasi: cap baru `CAP.HR_MODULE_VIEW='hr.moduleView'` di PARTNER_BASE + Admin&HR (bukan Finance/auditor). Komponen reusable **`AccessDenied`** di `ui.tsx`. Gate `can(HR_MODULE_VIEW)` di view: `view_pc_talent.tsx`(Recruitment+Learning), `view_pc_org.tsx`(SuccessionPlanning) — return `<AccessDenied>` (menggantikan isi, data tak tersusun). Kurasi nav: `MODULE_CAP={recruitment/learning/succession:'hr.moduleView'}` di `icons.tsx` + filter `group.items` di `shell.tsx` `canOpenModule()`. **Client `can()` = static GRANTS bundle** (`contexts.tsx:328` `rbacCan(me.role,cap)`) → gate murni klien, TAK perlu reseed (server tak meng-gate modul ini; view=authoritative, nav=UI). Gerbang: typecheck 0, lint 0 (regen baseline), **migration 437/437**, server 267/267, build OK. rbac.test +baris HR_MODULE_VIEW. **Live-verified:** Senior(Dimas) sidebar SEMBUNYIkan 3 modul + force-nav via fiber onNavigate→"Akses terbatas", 0 data bocor; Admin&HR(Yuni) sidebar TAMPILkan + Rekrutmen render penuh. Login switch via `window.AMS_API.auth.login.mutate`+reload (cookie-restore).

**RONDE-3 sesi sama (2026-07-06) — penyederhanaan tampilan karyawan + firm-ops personal:**
(1) **Yuni/firm-ops karyawan KAP juga**: mereka TAK di `AMS.STAFF` (roster audit-shaped, crash by-grade) → dulu `resolveEmpId`=null → dashboard kosong. FIX: roster terpisah **`FIRM_STAFF`** (`data_part1.ts`: EMP-501 Yuni, EMP-601 Teguh) + seed `PAYROLL`/`LEAVE_BALANCE` mereka (`data_part2.ts`) + `resolveEmpId` klien(`ethics_compliance.ts`)+server(`personalScope.ts loadStaff`) cari **STAFF ∪ FIRM_STAFF** + `view_personal.tsx` lookup gabungan. `view_home.tsx`: panel **"Data Personal Saya"** kini tampil utk SEMUA peran (buang guard `!isFirmOps`, grid 2-kol selalu, konsolidasi 4 QuickLink→1 tombol `nav('personal')`). ⚠ seed `seed.ts:264` MENULIS `payrollData` StateDoc dari AMS.PAYROLL → **reseed WAJIB** agar EMP-501/601 masuk (kalau tidak Yuni tak lihat gajinya). FIRM_STAFF TAK muncul di roster audit (view HCM/payroll iterasi AMS.STAFF) — sengaja, hindari crash by-grade.
(2) **Trim view karyawan**: modul **`hcm`** ditambah ke `MODULE_CAP`→HR_MODULE_VIEW + gate `AccessDenied` di `view_people.tsx HCM`. **Payroll**: tab "Jurnal Penggajian" hanya `isFull` (`view_payroll.tsx PR_TABS` conditional). **Kalender kehadiran** (`view_hrops.tsx`): rows `staff.filter(s=>BAL[s.id])` (BAL=leaveBalance self-scoped) → non-privileged hanya dirinya.
(3) **Declutter nav**: grup **"People & Compliance"** disembunyikan dari sidebar utk non-HR_MODULE_VIEW via **`GROUP_CAP`** baru (`icons.tsx`) + filter di `shell.tsx groups`. Senior/Junior `ROLE_SIDEBAR_GROUPS` firm→`['Saya','Portal & Dokumen']`. Karyawan akses data personal HANYA lewat "Data Personal Saya"; deep-link ke modul personal (payroll self dll) tetap jalan (grup aktif tetap tampil utk orientasi). `view_personal` Profil buang deep-link→hcm (kini ter-gate).
Gerbang: typecheck 0, lint 0 (regen baseline), migration 437/437, server 267/267, build OK. **Live-verified (reseed+preview restart):** Yuni dashboard→"Data Personal Saya" panel→render EMP-501 gaji Rp24,5jt (bukan empty-state); Dimas Firma-sidebar=hanya Saya+Portal (People&Compliance & HCM absen), force-nav hcm→"Akses terbatas" tanpa bocor, payroll tanpa tab Jurnal ("1 karyawan"), kalender 1 baris (dirinya). Login switch `AMS_API.auth.login.mutate`+reload; nav paksa via React-fiber `onNavigate` (palette tak indeks modul firma).

(4) **`view_personal.tsx` tombol "Buka modul"→"Detil"** (2026-07-06): kini buka **MODAL in-page** (`DetailModal`+`detailBody()` switch per-seksi: slip gaji lengkap+potongan BPJS/PPh, daftar SKP, riwayat cuti, independensi, butir etika, kasus disiplin+desc, kinerja+KPI, profil+PII) — BUKAN `nav()` ke modul, jadi sidebar tetap ramping. `nav`/`useNav` dihapus dari file (tak dipakai). GOTCHA tanpa @types/react: `useState` tak boleh type-arg (`useStatePS(null)`, inferred any OK); param handler ketik struktural (`(e:{stopPropagation:()=>void})`), bukan `:any`. Live-verified: klik Detil→modal muncul, `stillOnPersonalPage:true` (tak navigasi), tutup bersih.

(5) **RONDE-5 (2026-07-06) — "Detil" jadi HALAMAN + SELF-SERVICE** (mengganti modal ronde-4). `view_personal.tsx` kini master-detail: `page` state → halaman detail in-module (SubBar "Kembali", bukan modal, bukan nav ke modul admin). Dari halaman detail pegawai **self-service**: **ajukan cuti** (form) & **deklarasi independensi/etik** (tombol). **Jalur tulis BARU aman** (bukan lewat state.set yang HR_MANAGE): `server/src/personalSelfService.ts` `submitLeaveRequest`/`declareSelf` + endpoint `personal.submitLeave`/`personal.declare` (protectedProcedure) — server resolve empId dari SESI, `writeOwn` read-modify-write HANYA baris caller (append cuti sendiri status Menunggu / set declared / signed), audit `SELF_SERVICE` (union `AuditAction` +1). Deklarasi independensi **buat baris bila belum ada** (staf non-partner tak di seed INDEPENDENCE). Klien: `api.ts` `personalSubmitLeave`/`personalDeclare`; view pakai override lokal (`reqOv`/`indepOv`/`ethOv`) pasca-mutasi agar segar tanpa reload. Non-staf (empId null) → FORBIDDEN. Tes: `server/src/__tests__/personal_selfservice.test.ts` (5 tes: append-own, isolasi Fajar tak lihat cuti Dimas, non-staf ditolak, declare indep/etik). Gerbang: server 272/272, mig typecheck 0/lint 0/build OK. **Live-verified (Dimas Senior):** Detil Cuti=HALAMAN (bukan modal, `isPage:true`)+form→submit→"terkirim, menunggu HRD"+baris Menunggu muncul; Detil Independensi→tombol→"tersimpan"+status Diterima. GOTCHA: `submitLeave` id = `LV-<empNum>-<count>` (tanpa Date.now); tanpa @types/react handler event ketik struktural `(e:{target:{value:string}})`.

**Residual jujur (belum ditutup, bukan yang Ari sebut):** `STAFF.rating/util` di direktori HCM masih roster-level statis (tapi HCM kini ter-gate Partner+HRD, jadi tak terekspos ke karyawan). FIRM_STAFF (Yuni/Teguh) tak muncul di roster payroll audit (view iterasi AMS.STAFF) — data personal mereka via personal.get saja. `AMS.*` seed tetap di bundle browser (batas single-tenant; proteksi di lapisan API/derivasi/gate klien). Command-palette tak indeks modul firma (jadi tak perlu di-gate; toh view gating). Next: pertimbangkan commit + tutup residual bila diminta.
