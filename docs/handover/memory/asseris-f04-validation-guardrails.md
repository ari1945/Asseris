---
name: asseris-f04-validation-guardrails
description: Fase 4 stream — validasi silang & guardrail input (canon_validation.ts PURE); PR-B..G SEMUA MERGED master (SA265/450/705/530/505), Fase 4 cross-check TUNTAS
metadata: 
  node_type: memory
  type: project
  originSessionId: e28d0d03-f17e-4e86-aa1d-95d80f507676
  modified: 2026-07-21T10:35:56.825Z
---

Fase 4 Asseris = "validasi & guardrail input" (menutup gap P6 validasi tipis, lihat [[asseris-input-mechanism-eval]]). Semua guardrail = fungsi PURE di `migration/src/canon_validation.ts` (pola `canon_*`, tak sentuh AMS_CANON) + di-surface sebagai banner amber di view + unit test di `canon_validation.test.ts`. Branch: `feat/f04-validation-guardrails`.

**STATUS 2026-07-21 — 🎉 FASE 4 CROSS-CHECK TUNTAS, repo bersih (0 PR terbuka):** PR-F & PR-G ✅ **MERGED master** — **PR #112** (PR-F SA 530, squash `f51d260`, 6/6 CI) + **PR #114** (PR-G SA 505 rebased, squash `254395d`, 6/6 CI). Master final: typecheck 0, 544 test hijau. Guardrail kini di SELURUH rantai: risiko→prosedur→ukuran sampel(F)→cakupan bukti(G)→salah saji(D)→opini(E). Semua kandidat cross-check (SA 505 & SA 530) SELESAI.

⚠️ **GOTCHA STACKED-PR + SQUASH (mahal, catat!):** PR-G dibuat sbg stacked PR (base=cabang PR-F). Saat #112 di-merge `--delete-branch`, GitHub meng-**auto-CLOSE** #113 (base branch-nya terhapus) — TAK bisa di-reopen (base ref hilang) & TAK bisa retarget (closed). Squash #112 juga membuat commit PR-F asli (`207fdf0`) di cabang #113 jadi REDUNDAN vs versi squash di master → CONFLICTING. FIX: `git rebase --onto origin/master <old-base-sha=207fdf0> <sa505-branch>` (replay HANYA commit PR-G, buang commit PR-F), force-push, buat **PR BARU** #114 ke master. **PELAJARAN: untuk PR independen yang kebetulan menumpuk, JANGAN stacked+squash — rebase onto master SEJAK AWAL, atau merge base-PR dulu lalu langsung rebase (jangan andalkan auto-retarget; --delete-branch mematikannya).**

**Stream sebelumnya #109/#110/#111 ✅ MERGED master.**

**3 PR pra-sesi DITINJAU, DIRAPIKAN, & ✅ SEMUA MERGED master (2026-07-21, squash): #108 (`36b1617`), #106 (`8df0f25`), #107 (`91bda34`).** Integrated master diverifikasi: typecheck 0, full lint 0, 565 test. Repo BERSIH (0 PR terbuka). CATATAN: #106 & #107 sama-sama sentuh rbac.ts/rbac.test.ts → auto-merge GitHub bersih (baris berbeda: payrollSent.v1 array vs capacityPlan.v1 branch) — TETAP verifikasi gate penuh pasca-merge (jangan andalkan MERGEABLE=CLEAN saja). Ringkasan tiap PR:
- **#106 Fase 3 honest-affordances** (`feat/f03-honest-affordances`, `70096d6`): VERDICT SHIP. 14 export nyata (`amsExportXlsx` + segel), tombol didemosi jujur, nol :any baru. Nits diperbaiki: `payrollSent.v1` ditambah ke enumerasi test HR_MANAGE (rbac.test.ts); tombol payroll "Kirim ke Karyawan"→"Tandai Terkirim" (atestasi distribusi, kirim di luar app). SENGAJA tak diubah: nama firma hardcode 20× (bukan nit kecil; `AMS.FIRM.short`='WHR' salah, harusnya `.name`) → follow-up.
- **#107 Fase 4 derivasi kapasitas** (`feat/f04-derivation-capacity`, `111e284`): VERDICT (semula NEEDS-WORK) → FIXED. `canon_capacity.ts` PURE, SSOT genuine (week-0 dari `schedule`, forward dari `capacityPlan.v1`), test invarian SSOT dua-arah bagus. **BUG NYATA diperbaiki:** `capacityPlan.v1` firm-scope tanpa cabang capForWrite → FIRM_ADMIN (Partner-only) → suntingan Manajer GAGAL SENYAP. Fix: cabang `→ ENGAGEMENT_MANAGE` di rbac.ts (pola priorYear) + test + gate UI dua-lapis (tombol/panel `can(ENGAGEMENT_MANAGE)`) + guard bagi-nol ratio di view_dashboard2.
- **#108 Fase 4 derivasi delivery** (`feat/f04-derivation-delivery`, `5d0eb1c`): VERDICT SHIP. Status milestone dihitung dari `{done,date}×AMS.TODAY`, nol field status tersimpan, deterministik, tested. Nits diperbaiki: guard tanggal kosong di setMsDate (cegah "Invalid Date"); test ambang ≤7 inklusif (+7 due, +8 upcoming).

**GOTCHA REVIEW:** subagent (general-purpose, effort high) STALL mid-stream 2× di PR besar (#107) — "Response stalled mid-stream". Fallback: tinjau sendiri dari diff (lebih andal utk PR <10 file). `asGrade`/CAP_GRADES cocok dgn role SCHEDULE nyata (`Partner/Manager/Senior/Junior` di data_part1.ts:245 — BUKAN `'Audit Manager'` di roster util L134).

**PR-G tambahan `3962062` — CF_AREA.pop reaktif dari WTB (SSOT):** dulu 3 literal seed duplikat WTB → kini `wtbUnadj(code)` tarik `AMS.WTB` kolom `unadj` (Bank `1-1100`/Piutang `1-1200`/Utang `2-1100` abs). Nilai identik seed (nol perubahan perilaku), verified via node. Pihak Berelasi (7.450jt) TETAP literal — agregat pengungkapan SA 550, TAK ada akun WTB tersendiri (melekat AR/AP). WTB row = `{code,name,ly,unadj,adj,aje}` di data_part1.ts:53 (`WtbRow` di canon_types); `unadj`=saldo buku dilaporkan (objek konfirmasi), `adj`=audited. Lookup `(AMS.WTB||[]) as WtbRow[]` (hindari :any).

**PR-G SA 505 — cakupan konfirmasi vs saldo (OPEN #113):** `reconcileConfirmationCoverage()` PURE. Satu SSOT: `items` (confirmState.v1 seed+override); **status mengkodekan resolusi** (resolveRecon/resolveAlt flip → 'Received'), nol flag manual. 4+1 cek: non-respons POSITIF 'No Reply' tanpa prosedur alternatif (berat, SA 505.12; negatif dikecualikan .15/A23) · diskrepansi belum selesai (warn, .11→SAD) · respons tapi keandalan belum divalidasi (warn, .10-11) · cakupan nilai <50% saldo populasi per area (warn, SA 330; `CONF_COVERAGE_MIN` konvensi) · finalized+terbuka (berat). Saldo pop dari CF_AREA (WTB). Banner tab Ringkasan (atas panel cakupan) + tombol deep-link worklist. Seed ENG-014 memicu: alt-missing CF-006/012, disc-open CF-005/010, coverage-low Piutang 20%/Utang 23%. **GOTCHA ratchet (kena lagi):** menambah `(c: any)`/`(a: any)` di map() → eslint un-suppress SELURUH view_confirm.tsx (82 error). Fix: definisikan `type CfRow` & `(items as CfRow[]).map((c)=>…)` — TANPA `:any`. Gate 544 test.

**PR-F SA 530 — TM sampel vs PM (OPEN #112):** `reconcileSamplingTolerance()` PURE di canon_validation.ts. Dua SSOT: TM (`sampling.v1.params.tm`, slider persisten di-seed dari PM sekali lalu geser independen) vs PM kanon (`materialityFor().pm`). 5 cek: TM>PM (berat, SA 320.9/530.7 sampel under-powered) · proyeksi≥TM tapi "diterima" (berat, SA 530.A22/450) · TM≥15% di bawah PM (`SAMPLING_UNDER_PM_TOL`, non-berat inefisiensi) · PM absen (non-berat) · finalized+berat (berat). View: banner tab Kalkulator + baris "PM kanon vs TM aktif" di bawah slider TM. **BUG lama diperbaiki:** view_sa530 dulu hitung PM hardcode `×0.75` (baris 67) — kini tarik `materialityFor().pm` yg menghormati `pmPct` workspace. TM tetap editable (hanya warn, tak auto-reset — keputusan auditor). Gate: typecheck 0, eslint 0, 535 test (9 baru). Verifikasi live TAK jalan (login butuh password → classifier blok; Ari login sendiri bila mau). Seed cred Manager: `anindya.p@whr-cpa.id`/`Manager#2025!` (data_part1.ts).

⚠️ **3 PR LAIN masih OPEN** (stream berbeda, pra-sesi ini — BUKAN punyaku): #106 (Fase 3 honest-affordances, `feat/f03-honest-affordances`), #107 (PR-A1 derivasi kapasitas, `feat/f04-derivation-capacity`), #108 (PR-A2 derivasi delivery, `feat/f04-derivation-delivery`). Repo TIDAK bersih-PR.

**LANJUT SESI BARU — kandidat cross-check validasi tersisa:** (1) SA 505 — cakupan konfirmasi terkirim/diterima vs saldo AR/AP (`confirmState.v1` × WTB, lihat [[asseris-confirm-hub-sa505]]); (2) SA 530 — ukuran sampel vs populasi & TM/PM di modul sa530 (lihat [[asseris-sa530-consolidation]]). Pola sama: fungsi PURE di canon_validation.ts + banner amber + deep-link + unit test.

---
PR-B+PR-C ✅ **MERGED master** via **PR #109** (`caec9d3`, squash). 6/6 CI pass.

**PR-D SA 450 — rekonsiliasi 3-arah ✅ MERGED master (`287f761`, PR #110, squash, 6/6 CI):** TEMUAN besar = split-brain RANGKAP TIGA yang terputus: (1) `AMS.AJE` (Posted/Proposed) (2) ledger SAD `sadItems.v1` (disp corrected/uncorrected/passed, populasi terlengkap +Projected/PY) (3) opini `opinionDoc.v1.type` — mesin opini bahkan baca salinan `UNCORR` BEKU di view_opinion_parts. Ari pilih scope "rekonsiliasi penuh 3-arah". Fungsi PURE `reconcileUncorrectedMisstatements` + `ajeRefKey` (normalisasi PAJE-NN/AJE-NN→AJE-NN; SA 530/SUM-PY/CTT→null) di canon_validation.ts: 3 cek = completeness (Proposed tanpa item SAD) + staleness (disp SAD vs status posting AJE) + SA 450→705 (agregat SAD>OM tapi opini unmodified). Banner di view_sad + deep-link nav('aje')/nav('opinion'). Agregat opini dari SAD (bukan AJE — AJE lebih miskin). GOTCHA baru: (a) `opinionDoc.v1` dibaca READ-ONLY via useAmsPersist — aman krn `useServerState` TAK menulis saat mount (cuma hydrate), jadi tak merusak dokumen opini; default minimal `{type:'unmodified'}` cukup. (b) `AMS.AJE` sudah bertipe → akses langsung TANPA `(AMS as any)` (itu +1 any = fail ratchet). (c) ketik `recon: UncorrResult` eksplisit (useMemo di shim return any → `.map((s))` implicit-any TS7006). Live-verified Rekan Pemimpin ENG-2025-014: 2 drift seed (M-02↔AJE-05, M-04↔AJE-02), agregat 3.270jt=77% OM. LOGIN gotcha: aku TAK BOLEH ketik password (classifier blok) — Ari login sendiri di Browser pane (bukan Chrome lain); seed cred di server/src/seed.ts (Hartono `hartono.w@whr-cpa.id`/`Partner#2025!`). Gate 518 test.

- **PR-B** (`345165b`): probError/clampPct (0–100), dueBeforeIssued, capacityProjection. Dipakai view_pipeline (OppForm prob, InvForm tanggal terbit), view_scheduler (over-booking), view_risk (surface reconcileRiskResponse RoMM→prosedur).
- **PR-C** (`c1f8b4d`): `reconcileDeficiencyComm` — validasi silang SA 265 → SA 260. Derivasi MURNI dari 2 SSOT (deficiencies.v1 `sig`+`status` × baris SA 260 ¶16 di tcwg.v1) — **bukan** flag `communicated` manual (keputusan Ari: SSOT, nol drift). Deteksi: sig di-set "Lisan" (langgar ¶9) + sig ada tapi SA 260 ¶16 belum "Selesai". Banner di tab Register SA265View + deep-link nav('sa260'). Konstanta DEFIC_WRITTEN/TCWG_FINDINGS_DONE sinkron dgn `DEFIC_COMM` di view_sa2comm.

**PR-E SA 705 — konsistensi opini ✅ MERGED master (`5242be2`, PR #111, squash, 6/6 CI):** guardrail level-modul di view_opinion — opini tersimpan `opinionDoc.v1.type` vs rekomendasi determinan. Fungsi PURE `reconcileOpinionConsistency` di canon_validation.ts (+ OPINION_LEVEL/OPINION_LABEL): 4 cek = under/over/mismatch (under-mod=severe SA 705.7-10) + basis (modifikasian tanpa paragraf Basis SA 705.20) + gc-missing/gc-stray (SA 570.22) + finalized (difinalisasi tapi inkonsisten=gerbang mutu). KUNCI desain: `rec` dihitung di view via `window.AMSOpinion` (O.recommendOpinion/classifyMis/aggUncorr — SAMA seperti DeterminationPanel/OpinionFlowBar) lalu di-INPUT ke pure fn → NOL duplikasi matriks SA 705, selaras rekomendasi on-screen; TAK me-refactor mesin opini (konsisten keputusan PR-D tolak "satukan SSOT #3"). Banner merah bila severe + deep-link setTab('determine'). GOTCHA: mesin opini `recommendOpinion`/`classifyMis`/`aggUncorr` tak diekspor modul—hanya via `window.AMSOpinion` (O). OpinionFlowBar hardcode OM 4.25M; DeterminationPanel pakai activeEngagement.materiality — banner ikut Panel (4.25M fallback). Live: seed under-mod WTP vs WDP (severe). Gate 527 test. Sesi login: cookie httpOnly bertahan lintas restart server (tak perlu login ulang).

**Pola guardrail (ulangi utk cross-check berikut):** fungsi PURE reconcile → hitung `issues` → banner `{issues>0 && ...}` amber (`var(--amber-bg)`, `I.alert`) + deep-link `nav(target,{from})`. Cetak biru: banner risk→programme di [view_risk.tsx](migration/src/view_risk.tsx) ~L103.

**GOTCHA verifikasi live** (modul SA di HIDDEN_GROUP, mis. sa265/sa260 grup "SA · Tanggung Jawab (200)", `wsForModule('sa265')===null`): ⌘K palette **menggantung browser-tool** (bukan cuma flaky — `type` lalu `screenshot` timeout 30s berulang; app sendiri sehat, konsol bersih). Rute andal: sidebar Perikatan → **Matriks Kepatuhan** (compmatrix) → klik baris SA via JS (`element.click()` pada ancestor cursor:pointer). Baca hasil via `document.body.innerText` regex, **bukan** screenshot (timeout). `mcp__Claude_Browser__computer{screenshot}` tak reliable sesi ini.
