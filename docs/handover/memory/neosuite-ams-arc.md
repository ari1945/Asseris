---
name: neosuite-ams-arc
description: "NeoSuite AMS W0–W10 productionization — current wave, repo state, key gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 88a1bb2f-7085-4b76-a555-f5d5e5535fe5
---

Engagement: harden NeoSuite AMS (Indonesian KAP audit SPA) from buildless prototype to product via the 11-wave backlog (W0–W10) in `Evaluasi NeoSuite AMS - Kesiapan Pengembangan Claude Code.html`. Schema (24 tables, 5 domains) + per-seam map in `Peta Jalan Teknis - Handoff Claude Code.html`. Work wave-by-wave; show plan + files before code; gate each wave on its DoD; zero numeric regression vs baseline.

**Repo state (set up 2026-06-17):** full source was inside `Audit System.zip`; extracted and **promoted to project root** (matches CLAUDE.md layout). Originals archived in `_archive/` (gitignored). **git initialized** — baseline `9f5b095`, W0 `c37453b`, W1 `dbc31be`. Generated artifacts (`app/compiled`, `app/vendor`, prod html, `migration/src`, `.eslint-report.json`) gitignored. Build workflow documented in `BUILD.md`.

**W0 = DONE.** prod precompile (`build/`) green: `NeoSuite AMS (prod).html` boots/renders/navigable. Key figures locked in `W0-BASELINE.md` (materiality OM Rp 4,26 M; PSAK 46/71/73) — use as regression anchor. Vite/ESM (`migration/`) toolchain verified (codemod + 242-module build + dev boot) but full render deferred to W3.

**Gotcha — dev vs prod Babel differ:** buildless `NeoSuite AMS.html` loads `@babel/standalone` (preset env → `const`→`var`, tolerates redeclaration); `build/precompile.mjs` uses preset-react only (keeps `const`). So latent golden-rule #1 violations (duplicate per-file React hook aliases) only crash the **prod** path. Fixed 5 file-pairs in W0. ESM is immune (per-module scope).

**W1 = DONE** (`dbc31be`). Vite (`migration/`) is the build SSOT (single `main.jsx`, 242-module build); ESLint flat-config gate added (`npm run lint`). Error gate green (no-undef 0, no-dupe-keys 0); fixed 2 real dup-key bugs (icons.jsx 'internalaudit', view_bo1 'Diproses'). Wired 4 symbols (shell owns `useStateSH`; PROGRAMME/PEVT/ML_FINDINGS_SEED window-exported).

**W3 worklist (now precisely measured via lint warnings, 607):** `react/jsx-no-undef` = 3 unpublished helper components **`KvBox`(254), `RowKv`(142), `Kv`(2)** + `PSAK48View`/`PSAK68View`, plus rule false-positive on `window` in `<window.X/>` (44, related_modules.jsx). `react-hooks/rules-of-hooks` (163) = intentional buildless guards (`typeof useNav==='function'?…`, `window.useAmsPersist`). Plus real dup `AJEView` (view_execution+view_aje). W3 DoD: wire components, promote both rules to error.

**W2 = DONE** (`3b11497`, `96ed161`). Split the 2 JSX giants: view_groupaudit.jsx 1016→478 (+view_groupaudit_parts.jsx 551); related_modules.jsx 1200→193 (+related_modules_data.js 540 +related_modules_data2.js 480, AST-accurate extraction of interleaved LINEAGE graph). All outputs <600; zero numeric regression; components render 0-error. styles.css already modular; copilot.jsx 725 (<900). **canon.js/data.js engine splits DEFERRED to W3** (they're IIFEs — splitting safe only under ESM; user-approved). Split helper scripts in `.claude/w2-*.mjs`; AST mapper `.claude/w2-ast-map.mjs` (uses migration's @babel/parser).

**W3 IN PROGRESS — decision: GO ESM-ONLY** (user-approved). migration/src becomes canonical hand-maintained source; codemod regeneration stops; app/ + NeoSuite AMS.html retired as frozen reference. Phases: 1✅ complete import graph · 2 make src canonical (un-gitignore+commit src, stop codemod) · 3 canon.js/data.js engine splits (ESM) · 4 strip window per-workspace (keep runtime bus) + rewrite `<window.X/>` · 5 promote jsx-no-undef+rules-of-hooks to error & green.

**W3 Phase 1 = DONE** (`d01310b`). Wired 5 unpublished components to owner window-exports (KvBox→view_analytical, Kv+RowKv→view_calc, PSAK48View→view_psak48, PSAK68View→view_psak68). Dedup'd AJEView: view_aje overrides view_execution's (renamed view_execution's → AJEViewLegacy, dropped from export); window.AJEView stays view_aje's (verified). jsx-no-undef 444→44 (only `<window.X/>` left). no-undef 0; build 245; zero numeric regression.

**W3 Phase 2 = DONE** (`d60199d`). migration/src + index.html are now CANONICAL hand-maintained ESM source (un-gitignored, committed); codemod retired (npm script `_codemod_RETIRED`); buildless app/ + NeoSuite AMS.html + build/ are FROZEN reference. **Edit migration/src now, NOT app/.** Verifying Vite DEV render (never done before — prior waves used prod) surfaced + fixed 2 real codemod bugs: (1) LINEAGE circular-import TDZ in related_modules_data2.js (now imports+re-exports LINEAGE from related_modules_data.js); (2) only styles_base.css was carried — copied+imported the other 4 CSS (chrome/ai/work/modules). ESM app verified: fully styled (matches prod), numbers identical, build 249, lint error-gate green (207 warns = worklist). BUILD.md rewritten; CLAUDE.md has ESM-only banner.

**W3 Phase 3 = DONE** (`dc35a93` canon, `3c8c3b6` data). Split both IIFE monoliths into <600-line ESM modules, oracle-verified zero regression. canon.js 2026L→canon_base+canon_part1..4+index (AMS_CANON **74/74 match**); data.js 2002L→data_base+data_part1..4+index (window.AMS **142/142 match**). Lint 0-err (207 warns), build green, renders identically. The reverted first attempt (`538678d`) broke 6 orchestrators by NOT importing across parts; **fix:** generator computes every external ref per part via @babel AST and emits grouped cross-module imports (`.claude/w3-split-{canon,data}.mjs`, `w3-canon-dag.mjs` generalized). **Live oracle** (supersedes old prod-baseline): inject `<script>` into Vite page (preview eval is isolated-world, can't read page globals directly) → per-key len:djb2 fingerprint → diff `w3-{canon,data}-live-{pre,post}.json`. **3 key learnings (in `.claude/W3-PHASE3-NOTES.md`):** (1) gate on runtime oracle NOT vite build (rollup green on missing in-fn refs); (2) ESM cycles safe iff no load-time initializer reads across them (canon part3↔part4 cycle is harmless — only SRC/FIG compute at load, both in canon_base); (3) trailing BARE statements (data.js's 3 seed-normalization forEach passes) go in the INDEX after imports, not swept into a part. Oracle harness also seeds W4 Vitest.

**W3 Phase 4 = DONE** (`90202d7`) — **Phase 5 folded in** (both target lint rules now ERROR & green). Re-scoped on findings (user-approved "lint-gate scope"): the arc's "strip window" line is FAR bigger than implied — codemod left ~1,500+ window READS across ~189 files (592 `window.AMS`, 143 `window.I`, 113 `window.AMS_CANON`, +BO/LEGAL/FSGEN/…); dual-publish writes still load-bearing; **none needed for the lint gate**. So Phase 4 = two mechanical rewrites + the promotion: (1) collapsed 152 `(typeof useX==='function')?useX():fb` guards → bare `useX()` (dead false-branch; hooks already ESM-imported — verified); (2) rewrote 38 `<window.X/>` dynamic JSX → imported refs (`window.I`→`I` w/ word-boundary regex so `window.IMPORT`/`IRM` survive; `<window.TrendBars/>`→import) fixing all 44 jsx-no-undef; (3) 11 residual rules-of-hooks incl. 2 REAL bugs (hooks-after-early-return in GAPackages+UserMenu) + `useAmsPersist` renamed/imported. Lint 207 warns→**0 problems**, rules→error. Zero numeric regression proven structurally (no canon_*/data_*/forensic engine in diff; live materiality OM=4.260/PM=3.195=W0 baseline; canon 74/AMS 142). Render verified Vite :5180 (0 console errs, no error boundary). Codemods: `.claude/w3-p4-{guards,jsx,residual,check-imports}.mjs`; full notes `.claude/W3-PHASE4-NOTES.md`. Key learning: `react/jsx-no-undef` fires on `<window.X/>` (treats `window` as undefined JSX root) but NOT on computed `window.I[key]`.

**W4 = DONE** (`b1d6f36`, on `master`). Vitest net over the canon "number engines". Setup (`migration/src/__tests__/setup.js`) = node env + hand-rolled `window`/`localStorage`/`BENCHMARKS` stub, **dynamic** imports ordered data→canon (static imports hoist above the stub & crash on `window.AMS=…`; canon_base computes SRC/FIG from `window.AMS.WTB` at load). One `*.test.js` per canon module (canon_base, part1..4, forensic) hard-pinning W0-BASELINE figures (materiality OM4260/PM3195/CTT213, deferredTax taxExp10274/ETR21.18%/var−1914, psak71 ckpn1980→2600/ecl2603.014/gap623.014, leasePortfolio ROU/Liab/Net, reconcile no-err tie-out). `canon_regression.test.js` = snapshot fingerprint over all 19 zero-arg AMS_CANON engines (round3dp+sort) → the W2/3/6 regression gate (`src/__snapshots__/`, update via `npm run test -- -u`). Fixture `src/__fixtures__/wtb.js`. **49 tests green; coverage v8 ~99% lines/100% funcs/77% branches** (gate ≥80 lines/stmts/funcs, ≥70 branches in `vitest.config.mjs`). Installed `@vitest/coverage-v8@2.1.9`. lint green, build green. Note: materiality()/psak71() etc. read window.BENCHMARKS + window.AMS.WTB, so isolated unit tests need those stubs.

**W5 = DONE** (on `master`, gradual TypeScript — canon layer). 7 canon engines `git mv .js→.ts` (canon/canon_base/canon_part1..4/forensic_canon); permukaan diketik via new `canon_types.ts` (WTB/WtbRow/Figures/Fig/MaterialityResult/FsModel/AjeRow/Benchmark) + `src/types/globals.d.ts` (ambient `window` contract: AMS/AMS_CANON/BENCHMARKS/FSGEN/AMS_FORENSIC). `tsconfig.json` scope = canon files only; `strict:true` **except `strictNullChecks:false`** (the gradual ramp — DoD says "strict bertahap"). `noImplicitAny` on → **0 `any` on canon surface** (verified by emitting `.d.ts` for all 8 modules + grep). New `npm run typecheck`=`tsc --noEmit`=**0 err** (the .ts gate; ESLint does NOT lint `.ts` — flat config matches only `.js/.jsx`, tsc is stronger there). DoD "≥3 view use WTB/FIG types": new `canon_selectors.ts` (typed `figures()/fig()/materialityFor()`) consumed by 3 views (view_psak14/spr2410/sad) replacing `window.AMS_CANON.materiality(...)` calls + JSDoc `@type` — value-identical. **Zero numeric regression**: 49 vitest green (snapshot+W0 pins), Vite render verified (:5180 boots, OM4260/PM3195/CTT213/FIG.ckpn1980/taxExpense10274 = baseline, edited view renders 0 console-err). **Key gotchas:** (1) renaming `.js→.ts` breaks `./x.js` specifiers — rewrote to extensionless in canon files + tests + setup.js + main.jsx (Vite/Vitest resolve `.ts`); (2) `vite.config.mjs` esbuild `loader:'jsx'`+`include:/\.(jsx|js)$/` left `.ts` un-stripped → changed loader→`'tsx'` (TS+JSX superset) over all 4 exts; bare generic `<T>`→`<T,>` to avoid JSX-parse in tsx mode; (3) under `strictNullChecks:false`, heterogeneous seed arrays infer common-object-with-optionals (not unions) so mostly only `null`-first-element (TS7018) + dynamic-index (`Record<string,…>`) + `.find().prop` need fixing; psak66 jv/jo result needed an explicit `JointArrResult` union-merge interface.

**Fase B (product) underway:** **W6 COMPLETE** (backend SSOT — see [[neosuite-ams-w6-backend]]).
**W7 COMPLETE** (auth/sesi/RBAC — see [[neosuite-ams-w7-auth]]): `603e771` server auth · `35a3048`
server RBAC enforce + neg-authz · `21d10b2` client login seam · `26fa6fd` UI↔RBAC gates + real
SecKeamanan/SecAkses (act-as fully removed). All live-proven, 0 console err. Deferred from W7→W10:
httpOnly cookies, IP-allowlist, email alerts/reset, cross-device revoke; per-engagement data
isolation → W7.5/W9.
**W8 COMPLETE** (LLM-proxy — see [[neosuite-ams-w8-llm-proxy]]): `8f2932a` server proxy (key in
env, RBAC `CAP.LLM_USE`, rate-limit, egress-redaction, `LlmEvent` audit; Anthropic + OpenAI-compat
via `fetch`, nol-vendor; 44 server vitest) · `8195929` client status + **P4 Fase 3 narration**
(diagnostics "Jelaskan N temuan" button, honest disclaimer, graceful not-configured). Live-proven
vs mock upstream + later REAL-proven vs DeepSeek (redaction holds, NARRATE audited), 0 console err.
Deferred W8→later: streaming, copilot/ai_insights wiring, per-user BYO key.
**W7.5 COMPLETE** (per-engagement data isolation — see [[neosuite-ams-w7-5-isolation]]): `3653add`
server (EngagementMember + `ENGAGEMENT_VIEW_ALL` Partner/Manager + assertEngagementAccess on
bootstrap/state/list + seed + 15 tests → 59 server vitest) · `19286ea` client (FirmProvider accessible
set + guarded setActiveEngagementId + switcher/palette filter). Live-proven 3 roles (Junior→1 eng,
ENG-031 FORBIDDEN; Senior→2; Manager→all 7), 0 console err.
**W10 slice 1 COMPLETE** (hardening — see [[neosuite-ams-w10-hardening]]): `4a33fd5` server-side
append-only hash-chained audit trail (replaces client pseudo-hash demo; CAP.AUDIT_VIEW) · `b7801b4`
auth/secret hardening (httpOnly cookie, AES-256-GCM totpSecret-at-rest, IP-allowlist) · `b93bd01`
observability (/healthz + /metrics + JSON logs) + deploy-readiness (Dockerfile/compose/CI,
Postgres-flip — UNTESTED-from-here). 84 server vitest + 59 migration vitest (zero numeric
regression), live-proven, 0 console err.
**W9 COMPLETE** (konektor data / integrasi nyata — see [[neosuite-ams-w9-connectors]]): server-side
connector framework (`server/src/integrations/`), provider asli tak dapat dikreditensialkan dari sini
→ pola W8 (fixture adapter, HTTP drop-in). `7c3f62d` Fase 0 schema(Connector/SyncJob/ConnectorToken)
+seed-from-blueprint+RBAC(INTEGRATION_VIEW all/MANAGE Partner+Manager)+router list/status · `33dee91`
Fase 1 runner `runBankSync` pull→map→**control-total gate**→idempotent-post-to-SSOT→reconcile+SYNC
audit (Bank Feed→cashbank) · `50b4b36` Fase 2 webhook(HMAC public)+HTTP adapter(not-configured anggun)
· `36cbf9c` Fase 3 klien read-model (`data_import.js` overlay server utk konektor ter-wire, fallback
simulatif)+tombol Sinkronkan. Live-proven (Partner): wired 0→1, posted=5/consumed=5/tied=true,
idempoten, 0 console err. 116 server + 59 migration vitest, canon fingerprint identik. Deferred W9·2+:
konektor ke-2..8, OAuth flow, scheduler, smoke provider berbayar.
**Next: W10.5 selesai; window-namespace strip legacy track COMPLETE (`9d14ad8`, all 11 namespaces ESM-only — see [[neosuite-ams-window-strip]]).**
**W11 COMPLETE — widen TS kanon→lapisan data (`data*.js→.ts`), see [[neosuite-ams-w11-typescript-data]].** LAPISAN DATA SELESAI (pilot + slice 2–8 + boss `data.js`→`.ts` `be996c2`); tak ada `src/data*.js` tersisa. GOTCHA resolver: extensionless `./x` resolve, `./x.js`→`.ts` tidak.
**W12 IN PROGRESS — widen TS lapisan data→view (`view_*.jsx→.tsx`), see [[neosuite-ams-w12-typescript-view]].** 173 view, app-tier relaks (view BUKAN canon-reachable, tak ada 🔒). Keputusan D1–D4 terkunci (urut #importer-kecil-dulu · `_parts` by-count · ratchet=terminal W13+ · scope=173 view, fondasi W13+). **Fase 0 (beachhead) SELESAI** (`5f0f4fd`): pilot 3 (subsequent/sad/timebudget) + **infra penentu**: `ui.d.ts` foundation-shim (sibling .d.ts dihormati walau import `./ui.jsx` eksplisit — spike; konversi naif ~64 err/view 85% dari prop-required ui.jsx tak-berketik) + `AmsData.fmt/rp` + `view_calc.d.ts` provider-shim (hapus saat provider→.tsx W13). All gates green, live-proven Partner 0 console err. Resep+slice-list BUILD.md §W12. Sisa: Fase 1 (~151 leaf 1-imp, batch ~15–25) → Fase 2 (2-3 imp + penyedia-bersama terakhir). Alternatif lain: W9·2+ konektor / new feature.

**W5 ramp = DONE: `strictNullChecks` flipped ON** (canon now FULL strict). 86 null errors fixed (TS18048/TS2532/TS2322) via `!` non-null assertions on domain-guaranteed `.find().prop` lookups (type-only/erased — 49 vitest still green, 0 `any` on surface, build green). Codemod `.find(<pred>)!.…` for immediate-access sites (15 in part3); part4 `psak66` jv/jo + `a.<optional>` seed fields hand-`!`'d; field-level `!` where the prop itself is `number|null` (`dt.items[].car`) or `number|undefined` (`JointArrResult` fields). Committed on `master`. **Next = (a) widen TS** (data.js / a few views to `.tsx`) and/or **(b) DEFERRED full window-namespace strip** (own multi-session arc item): convert ~1,500 `window.AMS/I/CANON/…` reads → ESM imports workspace-by-workspace, drop dual-publish `Object.assign(window,…)` writes, KEEP imperative bus (`__amsOpen*`/`amsApplyPrefs`/`compliancePct`/`__amsNav`). Oracle-gate per workspace (now ALSO vitest+tsc-gated). See [[neosuite-ams-w0-w10-conventions]] for invariants.
