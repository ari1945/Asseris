---
name: asseris-session-2026-07-19-checkpoint
description: "START HERE — checkpoint; Fase 2 TUNTAS (master 7afb9fc); Fase 3 (PR #106); Fase 4 PR-A1 kapasitas (#107) + PR-A2 delivery (#108) + PR-B validasi/guardrail (#109) TERBUKA"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5520978f-41bf-4833-a66e-be6bc5e102f3
  modified: 2026-07-20T06:25:34.738Z
---

> 🚀 **FASE 4 PR-B (Validasi silang & guardrail) — PR [#109](https://github.com/ari1945/Asseris/pull/109) TERBUKA (2026-07-20), branch `feat/f04-validation-guardrails` dari master (`345165b`), independen.** Menutup gap P6 untuk 4 titik + 1 linkage. Baru: **`canon_validation.ts`** (PURE): `probError`/`clampPct` (0–100), `dueBeforeIssued` (jatuh tempo ≥ terbit), `capacityProjection` (jam vs kapasitas). Wiring: OppForm prob 0–100 (min/max+error+disable); InvForm tambah input Tanggal Terbit (dulu hardcode) + validasi due≥issued; BookingForm proyeksi over-booking di form (soft); view_risk register surface **`reconcileRiskResponse`** (canon_audit_plan, SUDAH ADA & teruji — dulu cuma di Memo Strategi) sebagai banner RoMM→prosedur (gapRisks/cakupan%). MATERIALITAS sudah tervalidasi (range slider min/max + warning deviasi >10%) → TAK disentuh. Gate: typecheck 0, lint 0 (net −1 any), build, 502 mig(+9 canon_validation.test)+283 srv. Live: OppForm prob=150→error+disabled, 60→enabled; risk reconcile jalan bersih (banner conditional, tersembunyi saat seed cakupan 100%). GOTCHA: RESERVE — cross-validation logic risiko→prosedur SUDAH ADA di `canon_audit_plan.reconcileRiskResponse` (grep dulu sebelum bikin baru). **Sisa Fase 4: sa265→sa260 communicated cross-check (butuh flag `communicated` baru di shape `Deficiency` view_sa2comm — sengaja dipecah dari PR-B); lalu PR-C jejak audit who/when + sweep klok AMS.TODAY app-wide.**
>
> 🚀 **FASE 4 PR-A2 (Derivasi delivery/audittimeline) — PR [#108](https://github.com/ari1945/Asseris/pull/108) TERBUKA (2026-07-20), branch `feat/f04-derivation-delivery` dari master (`d210df4`), independen dari PR-A1.** Baru: **`AMS.TODAY`** = klok demo SSOT tunggal (data_part4→data.ts→AmsData `TODAY:string`); DELIVERY_WINDOW+delivery+audittimeline merujuk satu konstanta (modul data lain masih anchor '2026-03-09' sendiri — sweep app-wide = PR-C). **`canon_delivery.ts`** (PURE): `seedDeliveryPlan` (status seed→flag `done` tersimpan), `milestoneStatus(m,today)` (done/due/upcoming DITURUNKAN, bukan label beku; ≤7h→due termasuk lewat-tempo), `withMilestoneStatus`, `deliveryDaysTo`. **`deliveryPlan.v1`** (firm-scope, seed AMS.DELIVERY) — view_delivery editor di drawer (toggle selesai/urung + sunting tanggal), view_audittimeline konsumen BACA-SAJA (edit di Delivery tercermin). Gate: typecheck 0, lint 0 (net −3 any), build, 501 mig(+8 canon_delivery.test)+283 srv. Live: milestone "dalam Nh"/"selesai" turun AMS.TODAY; toggle→deliveryPlan.v1 v0→1; audittimeline "Hari ini · 09 Mar" + unifikasi terbukti. GOTCHA PR-A2: `engById()=ENGAGEMENTS.find()||{}` PECAH saat ENGAGEMENTS bertipe (union `EngagementRow|{}` → akses `.status` gagal) → fallback `||({} as EngagementRow)` (cast bertipe, bukan :any); prune-suppressions bingung saat file punya any BARU + line-shift → hapus any baru dulu, `git checkout eslint-suppressions.json`, baru prune ulang. **Sesi depan: PR-B validasi silang & guardrail; PR-C jejak audit who/when + sweep klok AMS.TODAY app-wide.**
>
> 🚀 **FASE 4 PR-A1 (Derivasi kapasitas) — PR [#107](https://github.com/ari1945/Asseris/pull/107) TERBUKA (2026-07-20), branch `feat/f04-derivation-capacity` dari master (`5135ac5`).** Menutup Kelas G "dua model kapasitas terputus". Baru: **`canon_capacity.ts`** (PURE, pola canon_assertions — `capacityModel`/`currentWeekFromSchedule`/`seedForwardPlan`/`pipelineDemand`). Minggu-berjalan DITURUNKAN dari `'schedule'` (SSOT booking, key sama scheduler); minggu ke-depan = **`capacityPlan.v1`** (firm-scope, editable, editor 56-input di view_capacity + jejak who/when). **Minggu-0 tak pernah disimpan di plan** (struktur menjamin nol model tandingan). Rewire konsumen seed: view_capacity + view_dashboard2 + soqmPull(view_isqm_parts) baca helper, bukan `AMS.CAPACITY.grades/.staff`. pipelineDemand=heuristik value→jam (Q2-a, kecualikan Lost/Won). Gate: typecheck 0, lint 0 (NET −10 any), build, 505 mig(+12 canon_capacity.test)+283 srv. Live: heatmap week-0 = util scheduler persis (Dimas 95%/Bayu 55%/Hartono 35% vs seed lama 80%); capacityPlan.v1 persist v0→1 (Partner, FIRM_ADMIN default — nol perubahan rbac). PRD: `09-AI-OS/Briefs/2026-07-20-PRD-asseris-fase4-derivation-crossvalidation.md`. **Sesi depan: PR-A2 derivasi delivery/audittimeline + `AMS.TODAY` SSOT (klok beku '2026-03-09'); lalu PR-B validasi silang, PR-C jejak audit.** GOTCHA W15/ratchet baru: `useAmsPersist` RETURN `any` + `AmsData` index-sig `[k]:unknown` (SCHEDULE/CAPACITY unknown, PIPELINE/STAFF/USER bertipe) → (1) `.map/.filter` atas receiver `any`/unknown TAK beri tipe kontekstual → param callback WAJIB anotasi struktural (`(s: {status?:string})`, `(w: string, i: number)`), BUKAN `:any`; (2) akses koleksi tak-bertipe → cast `AMS.CAPACITY as CapacitySeed` (tipe, BUKAN `as any`) — hindari `(AMS as any)` yang nambah explicit-any. Verif UI via palette ⌘K: `type` keystroke lalu **element.click() baris hasil** (label modul = INGGRIS "Capacity Planning", bukan "Kapasitas"); Enter TAK memilih; screenshot flaky (timeout) → baca `document.body.innerText`; React onClick re-render async → cek state di call TERPISAH.
>
> 🚧 **FASE 3 (Honest Affordances) — PR [#106](https://github.com/ari1945/Asseris/pull/106) TERBUKA (belum merge), branch `feat/f03-honest-affordances` (3 commit `ca92695`/`7e6d6ce`/`f6b9bf6`).** Sapu ~31 tombol-mati back-office/HR (gap P5). Keputusan Ari = **Honest-affordances**: PR-A 14 tombol Export→`amsExportXlsx` NYATA (sealed+audit); PR-B 3 HR (leave→nav('personal'), payroll→persist `payrollSent.v1` [key baru di cabang HR_MANAGE `capForWrite` rbac.ts], ethics→`pc.ethics` requested+chip); PR-C 14 tombol entri ERP → demosi jujur (hapus + chip muted "Read-only", tooltip CoreSys/roadmap). Gate tiap PR: typecheck 0, lint 0 (nol :any baru — for-of atas any[]/tipe lokal PSent,EthDecl/cast struktural), build, 493+283. Live: export round-trip sealed:true; payrollSent.v1 v0→1. PRD: `09-AI-OS/Briefs/2026-07-20-PRD-asseris-fase3-honest-affordances.md`. **Sesi depan: tunggu review/merge #106, lalu Fase 4** (derivasi delivery/capacity dari scheduler+time; validasi silang). GOTCHA baru: demosi = hapus Btn + chip `<span className="chip tiny muted">`; util export = `amsExportXlsx({fileName,firm,title,meta,sheets:[{name,columns,rows,totals}]})` seal+audit otomatis; rbac firm-key baru WAJIB masuk array cabang (payrollRun/pc.ethics dst) atau Manajer 403 senyap; `nav` context-only (tak ada global nav) → verif UI via AMS_API state round-trip bukan drive-UI.
>
> 🎉 **FASE 2 TUNTAS (2026-07-20). master `7afb9fc`.** Keenam modul SA pelaporan kini kertas kerja tersimpan+auditable: PR-A/PR-B `#101`(sesi lalu) · PR-C `841a93f`(#102) · PR-E `aa04c3e`(#103) · PR-D `46ab236`(#104) · PR-F `7afb9fc`(#105) SEMUA MERGED. **Sesi depan = Fase 3 (aktifkan tombol mati back-office/HR) atau Fase 4 (derivasi delivery/capacity).** Keys StateDoc baru Fase 2: rep580.v1, comp710.v1, oi720.v1, sa230Doc.v1 (+ perluasan opinionDoc.v1.comp). Semua engagement-scope di AMS_PERSIST_SCOPE.

## Fase 2 — PR-F (sesi 2026-07-20, PR #105 TERBUKA) — MENUTUP FASE 2
sa230 Dokumentasi Audit: konstanta perikatan (tgl laporan/batas perakitan ¶A21/retensi) dulu HARDCODE modul → tersimpan+editable **`sa230Doc.v1`** (engagement-scope); + **atestasi kelengkapan** (penanda tangan/tgl/memo, tandatangani/cabut, peringatan bila agg.blocking>0). `useDocCanon` (rollup % dari WP kanonik) TAK disentuh — SSOT dipertahankan (teladan). Konstanta modul → seed default. Gate: typecheck 0, lint 0 (NOL :any baru → suppressions sa230 tak berubah), build, 493+283. Live: ubah tgl laporan 2026-04-01 + retensi 7th → sa230Doc.v1 v0→1 + lini masa/hero mengikuti; atestasi ditandatangani (Hartono Wijaya+memo) tersimpan & terender; probe dihapus (2 tabel).

## Fase 2 — PR-D (sesi 2026-07-20, PR #104 TERBUKA)
sa720 Informasi Lain: display-only → register tersimpan **`oi720.v1`** (engagement-scope). CRUD dokumen (nama/waktu sebelum-sesudah tgl laporan/flag diperoleh); telaah per-dok (konsistensi: konsisten/inkonsisten-LK/inkonsisten-pengetahuan/salah-saji-material + disposisi + catatan + lampiran F0.1 collection 'sa720'). Bagian "Informasi Lain" laporan DITURUNKAN: salah saji tak-terselesaikan → "terdapat kesalahan penyajian material" (¶18). Sign-off PALSU → NYATA (SASignoffMini stdId=sa720 + SA_WP_MAP refs 900 + WP_MODULE_MAP). Gate: typecheck 0, lint 0 (sa720 1→0), build, 493+283. Live: OI-3→salah saji → oi720.v1 v0→1 + paragraf berubah; CRUD tambah OI-6 persist (v2); sign-off nyata WP 900; probe dihapus (2 tabel).

## Fase 2 — PR-E (sesi 2026-07-20, PR #103 TERBUKA)
sa710 Informasi Komparatif: display-only → lensa + kertas kerja. **SSOT paragraf auditor pendahulu (¶13)**: fakta (nama/jenis opini lalu/tgl/periode) dulu HARDCODE TRIPLIKAT (view_opinion OP_TXT.comparative + sa705 + sa710) → kini SATU sumber `opinionDoc.v1.comp`; helper murni **`comparativeParagraph(client,mode,comp)`** (diekspor view_opinion) dibaca ketiganya; OP_TXT.comparative dihapus. sa710 = permukaan tulis (compMode+opts.comparative+comp), opini+sa705 = konsumen. Kertas kerja sa710 sendiri = **`comp710.v1`** (prosedur ¶7–9 + situasi ¶11–14, engagement-scope). Sign-off PALSU (nama staf hardcode) → NYATA via SASignoffMini stdId=sa710 (+entri SA_WP_MAP refs 900 di sa_canonical + WP_MODULE_MAP di wp_signoff). Gate: typecheck 0, lint 0 (sa710 7→0, opinion 34→32 NET turun), build, 493+283 test. Live: edit fakta di sa710 → opinionDoc.v1.comp v0→1 → paragraf IDENTIK di sa710+Generator Opini(laporan)+sa705 (triplikasi hilang); comp710.v1 v0→1; sign-off nyata WP 900; probe 2 key dihapus (masing 2 tabel).
POLA SSOT (dipakai ulang): fakta konten-laporan → `opinionDoc.v1` (Constraint §6), BUKAN doc modul; modul SA jadi lensa (baca/tulis doc bersama) + doc-sendiri hanya utk data non-laporan (checklist/prosedur). Helper teks murni diekspor dari pemilik SSOT, dibaca konsumen — nol duplikasi string.

## Fase 2 — PR-C (sesi 2026-07-20, PR #102 TERBUKA)
sa580 Representasi Tertulis: display-only → kertas kerja tersimpan `rep580.v1` (engagement-scope, di `AMS_PERSIST_SCOPE`). Per-representasi editable (status diminta/diterima/N-A + tgl diterima + teks pengecualian + flag penolakan ¶20); metadata surat (tgl + 2 penanda tangan); lampiran surat bertandatangan via Attachment **F0.1** (`amsAttachmentUpload` scope engagement/collection 'sa580', byte nyata+SHA-256, unduh/hapus). Evaluasi ¶18–20 & dampak-opini DITURUNKAN dari status (bukan hijau hardcode). sa580 sudah di `WP_MODULE_MAP` (SASignoffMini tetap). Gate: typecheck 0, lint 0 (ratchet sa580 3→0 NET turun), build hijau, 493 mig+283 srv test. Live: edit→server v0→1 engagement-scope, bertahan reload (cache `ams.v1.engagement.ENG-2025-014.rep580.v1`+server), chip SubBar `1 PENOLAKAN (¶20)`/`8/10 DIPEROLEH`, probe dihapus (2 tabel).
GOTCHA sesi ini: palette ⌘K filter TAK respons ke native-setter+input → buka via **computer type real keystroke** ke input `[placeholder*="Cari modul"]` lalu **element.click()** baris hasil (`div` teks "SA 580 · Representasi Tertulis"); Enter tak memilih. SubBar chip Badge non-interaktif TAK terbaca di `body.innerText` sesekali → baca `.pagehead-actions`.innerText langsung. `amsAttachment*` bertipe di `src/app-globals.d.ts` (bukan globals.d.ts). Icon `minus`/`paperclip` TAK ADA di `I` → pakai `x`/`link2`.

## Status repo
- **PR #100 (F0.1 penyimpanan berkas + F1 migrasi persistensi) ✅ MERGED** master `ac3915d`.
- **PR #101 (Fase 2 PR-A+PR-B) ✅ MERGED** master `ee0a5a4` (2026-07-19). Branch dihapus. PRD: `09-AI-OS/Briefs/2026-07-19-PRD-asseris-fase2-sa-reporting-editors.md`.

## Fase 2 — selesai sesi ini (PR #101)
Tema: modul SA pelaporan display-only → kertas kerja tersimpan, **SSOT tunggal** (lensa, bukan penyimpan tandingan). TEMUAN kunci: modul `opinion` sudah punya SSOT `opinionDoc.v1` (type/basisText/opts/kams/signoff) + decider identik + `buildOpinionBlocks` → laporan. sa705 & sa701 dulu DUPLIKAT display-only.
- **PR-A `8f275a9`** — sa705/706 jadi lensa `opinionDoc.v1`: hero "WTP" hardcode + decider ephemeral → diturunkan/tersimpan (mod705↔type; basisText; opts.eom/om/comparative). Sign-off opini tetap tunggal di modul opini (sa705 refleksi + nav). Export seed bersama `DEFAULT_DOC_O` dari view_opinion.
- **PR-B `adc8e10`** — sa701 KAM jadi lensa `opinionDoc.v1.kams`: Register **memakai ulang `AMSOpinion.KAMWorkshop`** (satu editor 2 permukaan); funnel/laporan/keterkaitan diturunkan; `doc.kamExcluded` (¶18). Perkaya shape KAM bersama: `fsRef`+`include` (backward-compat) + wiring buildOpinionBlocks/preview/kamCount + KAMWorkshop. Sign-off kanonik `SA_WP_MAP.sa701`.

Gate: typecheck 0 (mig+srv); lint 0 `:any` baru (prune, net turun); **493 mig + 283 srv test hijau**. Live-verified browser+DB: SSOT bidirectional (edit sa705/sa701 → tercermin Generator Opini); tulis sampai server StateDoc (version naik); nol error konsol.

## Fase 2 — SISA (belum, butuh keputusan lanjut Ari; urutan PRD)
- **PR-C sa580** (representasi: status/tgl/pengecualian/penolakan + lampir surat via Attachment F0.1) · **PR-D sa720** (register info-lain + ganti sign-off PALSU + Attachment) · **PR-E sa710** (komparatif + paragraf auditor pendahulu SUMBER-TUNGGAL [kini triplikat opinion/sa705/sa710] + ganti sign-off PALSU) · **PR-F sa230** (atestasi + tgl tersimpan; rollup dipertahankan).
- Fase 3 (tombol mati back-office/HR) · Fase 4 (derivasi delivery/capacity).

## GOTCHA operasional (hemat waktu)
- **RESET probe StateDoc WAJIB DUA TABEL**: `stateDoc` **DAN** `stateDocHistory` (`deleteMany where key`). Hapus stateDoc SAJA → orphan history version-1 → create baseVersion0 (path `router.ts:751`) tabrakan unique di tx history → rollback → **CONFLICT "already-exists" ABADI** untuk key itu (writes app gagal senyap, server tetap version 0). Gejala: `state.get`→0 tapi `state.set`→"already-exists". [[asseris-input-mechanism-eval]]
- **Verifikasi live**: `preview_start` config **`dev-all`** (vite :5180 + backend :5181). Login **hartono.w@whr-cpa.id / Partner#2025!**. `encryptionKey MISSING` normal.
- **Palette/DOM MCP flaky**: form_input tak picu React onChange → isi input via **native setter + dispatch `input` event** (`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set`); klik baris sidebar/modul via `element.click()` (clickByText), BUKAN koordinat. Screenshot flaky → verif via `AMS_API` round-trip (`window.AMS_API.state.get/set`) + cache `ams.v1.engagement.<engId>.<key>` + DB prisma. `read_console_messages` jalan. Cache scopeId = **`ENG-2025-014`** (display id, cocok server).
- **Ratchet `no-explicit-any`**: `:any` baru (walau meniru gaya file sekitarnya!) un-suppress SELURUH file → 100+ error. JANGAN tulis `:any` baru; pakai tipe struktural (`(e: { target: { value: string } })`), `Record<string, unknown>`, tipe lokal (`ODoc = typeof DEFAULT_DOC_O`), `keyof`. Callback ke setter useAmsPersist: `setDoc((d: ODoc) => …)` — param WAJIB bertipe (setter `(next:any)=>void` tak propagasi konteks). Setelah hapus `:any`/alias yatim → suppressions stale → `npx eslint src --prune-suppressions`.
- **Server**: `prisma db push`; `npm run prisma:generate` bila ubah schema; backend :5181 tak hot-reload rbac→restart. Node prisma reset: `cd server && node -e "..."`.
- **Commit/gh**: Bash=POSIX sh (jangan `@'...'@`; `git commit -F file`; JANGAN `&` PowerShell). `gh` via PowerShell `& "C:\Program Files\GitHub CLI\gh.exe"` ([[asseris-tooling-gh]]).

**Pola:** PRD-first + sign-off; SSOT (lensa, grep-dulu, jangan bikin doc kedua); gate hijau + verif live sebelum lapor.
