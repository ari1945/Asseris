---
name: asseris-module-evaluation
description: "Evaluasi modul Asseris 2026-06-25 (Firm vs Engagement, flow, kecukupan SPAP) + status remediasi 5 isu"
metadata: 
  node_type: memory
  type: project
  originSessionId: e60868bc-9ca1-4080-b625-015cd25a8970
---

Evaluasi modul Asseris (2026-06-25) atas 3 pertanyaan Ari: (1) penempatan Firm vs Engagement, (2) flow engagement vs penugasan riil, (3) kecukupan prosedur per SPAP + keterhubungan. Arsitektur 2-workspace (`Perikatan`/`Firma`, `icons.tsx` WORKSPACES/GROUP_WS) sehat; 5 isu remediasi:

1. **EQR salah workspace** — ✅ **SELESAI PR #29** (`feat/eqr-engagement-workspace`). EQR (ISQM 2) gerbang arsip per-engagement tapi di grup firma "Mutu, Risiko & Regulasi"→ dipindah ke grup "Finalisasi & Pelaporan" (workspace Perikatan, samping opini), tag ISQM 2. governance/soqm/pppk tetap firma.
2. **Onboarding↛Engagement + SA 210 tanpa gerbang** — ✅ **SELESAI PR #27** (gerbang Perencanaan→Eksekusi akseptasi+surat) **+ PR #28** (override Partner-only Q4). Detail: [[asseris-acceptance-engagement-flow]]. KOREKSI: konverter `StepConvert` ternyata SUDAH ada (grep dulu!).
3. **Gerbang fase buta progres prosedur** — ✅ **SELESAI PR #31** (`feat/finalisation-gate-execution`). Gerbang →Finalisasi kini sadar-progres: util murni `engagement_phase_gate.ts` (`finalisationGateCriteria`) + `wpCompletenessFor` ditambah `notStarted`. Kriteria: kesimpulan SA 230 **≥80%** + **0 WP belum-dimulai** (nol bukti & nol kesimpulan) + 0 catatan high. **Keputusan Ari:** metrik `conclusionPct`, ambang ≥80%+0-notStarted, severity **tetap 'warn'** (override mungkin). 13 unit test; 213 hijau; live-verified bundle nyata (eksekusi 0%→2 blocker; allMet saat semua berkesimpulan). Arsip ('confirm' lock) TAK diubah; lompatan Perencanaan→Finalisasi sudah tertangkap `toIdx>fromIdx`. Gotcha: `engagementGate` di `.tsx` (impor React) → test hanya util murni, integrasi diverifikasi live via `window.engagementGate`.
4. **Modul SPAP tipis/terputus** — ✅ **TUNTAS**: **SA 550 #32** + **SA 560 #33** + **mgmtletter SA 260/265 #34** + **strategy SA 300/330 ✅PR#35 MERGED** (`ebf7871` di master, `7b2a226`; `canon_audit_plan.ts` `reconcileRiskResponse` RoMM→respons→prosedur→KK + gap/coverage, dikonsumsi `StrategyMemo` view_misc1, 23 uji; verified 2026-06-26 — sebelumnya keliru dicatat "belum push/PR"). SELURUH 5 pick selesai.
   - **`strategy` SA 300/330** (done): **KOREKSI framing — modul KAYA (4 tab) bukan tipis; penyakitnya TERPUTUS** (sama pola mgmtletter). `canon_audit_plan.ts` MURNI (SSOT) — angkat `APPROACHES`/`defaultApproach` (dulu `SM_APPROACHES`/`smDefaultApproach` inline `view_misc1`) + `adequateApproach` (min memadai SA 330: signifikan/fraud→ext, L×I≥9→ctrl) + `reconcileRiskResponse({risks,overrides,wpStatusByRef})` → gap per-RoMM (`under-response`/`no-response`/`no-proc`/`no-wp`/`wp-not-started`) + roll-up cakupan. Status KK **diinjeksi** view (deriveWpStatus) agar kanon bebas-global (pola `reconcileGovernanceComms`). 23 test. `SmApproach` konsumsi kanon (de-dup) + panel "Pemetaan Risiko → Prosedur (SA 330)" (roll-up+daftar gap+nav) + kolom SA 330. **Gotcha penting:** risk.wp = sub-ref (`B-3`,`JE-1`) sedangkan WP_INDEX = section (`B`,`JE`✗); map `wp.split('-')[0]`→section, ref di luar lead-schedule (JE-1/RP-1)→`''`=n/a (JANGAN flag, hindari false-positive). Live-verified Partner: default 100%/0 gap (jujur — semua sig default 'ext', section In Review/In Progress), R-05 toggle→Substantif = 88% + gap under-response. Suppressions view_misc1 24→23 (prune, BUKAN serap any baru). deriveWpStatus/WP_META/openCanonicalWp diimpor dari view_wp (tak ada circular).
   - **`mgmtletter` SA 260/265** (done, PR #34 `feat/mgmtletter-deficiency-reconciliation`): **KOREKSI framing — modul KAYA (6-bagian/thread/workflow/generator), bukan tipis; penyakitnya TERPUTUS** dari register defisiensi ICFR. `canon_deficiency.ts` MURNI (SSOT) — angkat model severity SA 265 (`classifyDeficiency` 2×2+kompensasi, `LEVELS`/`COMMS` 265.9/.10) dari inline `view_icfr` ke kanon + `DEFICIENCIES`/`DEF_SEED`/`DEFICIENCY_ML_LINK` eksplisit + `reconcileGovernanceComms()` (defisiensi→wajib-TCWG?→tertaut ML final?→gap; + temuan signifikan SA 260 pending). 12 test. `view_icfr` tab Evaluasi Defisiensi konsumsi kanon (de-dup behavior-preserving, persist `icfrDef.v1` utuh, baseline any 60→57). `view_final3` panel rekonsiliasi. **KOREKSI premis PRD (truth>agreement): ML-02 ternyata `stage:'final'` (bukan diskusi) → F-01 terkomunikasi, TAK fabrikasi gap; seed jujur 3/3 terkomunikasi + ML-04 pending SA 260.** Live-verified Partner. Gotchas: (a) hapus inline `any` → suppressions sisa → `eslint src` **exit 2** → WAJIB `eslint src --prune-suppressions` (prune-only, tak serap any baru); (b) ICFR refactor pertahankan derivasi `defs` dari matriks (jangan relokasi matriks — out of scope); canon `DEFICIENCIES` mirror utk konsumen tanpa-matriks (ML).
   - **SA 560 `subsequent`** (done, PR #33 `feat/subsequent-events-fs-reconciliation`): `canon_subsequent.ts` MURNI (SSOT) — `SE_EVENTS` dipindah ke kanon + field aditif `aje` (tautan AJE penyesuai) & `disclosed` (non-penyesuai) + `scanSubsequent()` merekonsiliasi ke perlakuan LK: penyesuai→status pembukuan dari `AMS.AJE` (posted/proposed/**unbooked**), non-penyesuai→pengungkapan (disclosed/undisclosed). Roll-up dampak + unbookedImpact. Tautan EKSPLISIT (bukan match teks); tanpa tautan=unbooked jujur. 11 test (+integrasi AMS.AJE). View: panel "Rekonsiliasi ke Laporan Keuangan", gap→nav aje/disclosure. Live-verified Partner: SE-01(PKPU 2,53M)+SE-04(provisi 0,85M) unbooked, SE-02(kebakaran 4,1M) undisclosed, klik gap→modul AJE. Gotcha: `AMS.AJE` = `AjeRow` dgn `status?: string` opsional → `AjeEntry.status` WAJIB opsional. Badge props `:any` di ui.tsx → `kind={meta.k}` tanpa cast. **Screenshot preview sering timeout (render OK, capture flaky) → andalkan dump innerText via preview_eval.** view_subsequent baseline any=15.
   - **SA 550 `related`** (done): `canon_related.ts` MURNI (SSOT) — RP_PARTIES/RP_TXN dipindah dari view ke kanon (+`rpId` aditif; dual-publish `window.RP_TXN/RP_PARTIES` dipertahankan utk kompat `view_forensic`) + `scanRelatedParties()` merekonsiliasi register RPT ↔ `AMS_FORENSIC.JOURNAL_POP` (entri ber-`rpId`). Flag `unrecorded` (gap kelengkapan ¶15-17) & `undisclosed` (PSAK 7); cocok via rpId+nilai (tol Rp1jt), fallback nama. 15 test (+integrasi data riil). View dapat panel "Rekonsiliasi Ledger ↔ Registri RPT". **KOREKSI temuan eval: `JOURNAL_POP` SUDAH punya `rpId`/`party` (grep dulu!)** — jadi sumber ledger sudah ada, modul cuma mengabaikannya. Live-verified Partner: JV-24-09001→T-06 undisclosed (CV Mitra Keluarga 2,78M) + JV-24-09002 unrecorded (advance Dir.Utama 1,2M). Gotcha: ratchet `any` view_related baseline=27; +3 any baru → bulk-suppress lepas SELURUH file (bukan overflow saja) → WAJIB tipekan (impor `LedgerGap`/`UnsupportedRegister`), jangan re-baseline.
   - **Persistensi toggle rekonsiliasi** — ✅ **SELESAI** (folded ke PR #33 & #32, commit baru, belum merge). Toggle yang menggerakkan scan dulu hanya `useState` lokal → hilang saat reload (judgment auditor tak durabel). Fix pola sama strategyApproach.v1: override per-id via `useAmsPersist` di atas seed canon (canon tetap murni); daftarkan key ENGAGEMENT-scope di `contexts.tsx` AMS_PERSIST_SCOPE (DEFAULT firm → bug RBAC FIRM_ADMIN; engagement → capForWrite WP_EDIT + isolasi W7.5). **#33 subsequent**: `subsequentClass.v1` (reklasifikasi penyesuai↔non) + `subsequentProcs.v1` (tick); any 15→7. **#32 related**: `relatedTxn.v1` (disclosed/market/conf patch per-id) + `relatedProcs.v1`; any 27→12. Live-verified Partner: SE-01 & T-04 toggle → reload → tetap (key `ams.v1.engagement.ENG-2025-014.<key>`); 0 console error. Gotcha: useAmsPersist returns `any` → tipekan setter callback (`(s: Record<...>)`) agar tak tambah any; prune-suppressions setelah hapus any lama.
5. **Orphan dock lineage** — ✅ **SELESAI PR #30** (`feat/lineage-backbone-docks`). Blok `BACKBONE_LINEAGE` (`related_modules_data.ts`) tambah 14 entri: WTB, Materiality, Working Papers, AJE, Asersi, Sampling, Diagnostic, FSGen, Disclosure, MgmtLetter, Related, GroupAudit, Expert, ServiceOrg. Renderer `ModuleLineage` tak diubah. Verified live window.LINEAGE (14 ada, semua id resolve). Gotcha: sidebar = icon-grid custom non-introspectable via preview tools → verifikasi pakai window.LINEAGE/MODULE_INDEX, bukan DOM-scrape.

Repo remote `ari1945/Asseris` (gh via PowerShell + `C:\Program Files\GitHub CLI\gh.exe`, [[asseris-tooling-gh]]). Verifikasi live: `dev-all` (:5180+:5181), login Manager/Partner (lihat akun di BUILD.md); sidebar = icon-grid (DOM text-scrape tak reliabel → verifikasi via `window.MODULE_INDEX`/`wsForModule`/`MODULES`). Pola PR: branch off master, PRD di `docs/prd-*`, gate typecheck/lint/test/build.
