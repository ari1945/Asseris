---
name: asseris-gap-matrix-eval
description: Evaluasi gap SAK/SPAP/ISQM Asseris (auditor 23 Jun) + status terverifikasi + remediasi quick-win yang sudah dikerjakan & roadmap sisa
metadata: 
  node_type: memory
  type: project
  originSessionId: 5405fe26-4609-45e6-b6ab-d1c2715578c5
---

Auditor menyusun **"Asseris - Matriks Gap SAK SPAP ISQM.xlsx"** (29 temuan: 10 Tinggi / 17 Sedang / 2 Rendah) di root repo. Basis evaluasinya = *migration/src pasca-W5, bukti 18 Jun* — **mendahului P1/P2/P5/W6**, sehingga beberapa temuan Tinggi **sudah usang**.

**Verifikasi saya (23 Jun 2026) vs matriks:**
- **USANG (sudah ditangani):** S-02 (W6 backend), S-03 (sign-off+bukti global via `WpSubBarControl` di `shell.tsx:340`), S-04 (P5 `engagementGate`), AK-02 (semua PSAK di `WP_MODULE_MAP` → kesimpulan SA 230 persist). Lihat [[neosuite-ams-p1-conclusions]] [[neosuite-ams-p2-wp-signoff]] [[neosuite-ams-p5-lifecycle-gates]] [[neosuite-ams-w6-backend]].
- **NYATA & terbuka (High):** SA-08 (going concern dangkal+tak persist), Q-01 (SOQM ISQM 1 level firma), Q-02 (gerbang EQR tak terikat penyelesaian substantif), Q-03 (register independensi/rotasi — kritis, Ari penandatangan).
- **⚠️ KOREKSI 23 Jun (sesi PR#7): klaim "absen" auditor SISTEMATIS SALAH.** Verifikasi langsung membuktikan SA-01, SA 250, SA 620 SEMUA SUDAH ADA sebagai modul kaya, hanya **display-only**. SA-01 surat perikatan = `StepLetter` (view_onboarding2.tsx, persisted) + acceptance/continuance `StepAcceptance` (view_onboarding.tsx) — BUKAN absen. SA 250 = `SA250View` (view_sa2comm.tsx), SA 620 = `UseOfExpert` (view_specifics2.tsx) — keduanya 3–5 tab kaya tapi hardcoded/local-state. **Gap nyata seragam = AUDITABILITY** (persist SSOT engagement-scoped + sign-off SA 230 + tombol fungsional), BUKAN "bikin modul baru". Pola perbaikan = sama Q-02 (WP_MODULE_MAP + useAmsPersist). **Pelajaran: JANGAN percaya label "absen" matriks — selalu grep view+route+definisi dulu.**
- **Modul absen lama (klaim usang):** `view_sa250`/`view_expert` tak ada SEBAGAI FILE, tapi komponennya (`SA250View`/`UseOfExpert`) ada di file lain + terdaftar MODULES + route. ITGC/ICFR (`InternalControl`) & serviceorg (`ServiceOrg`) juga ADA — kedalaman belum diverifikasi.
- **Nuansa:** sa520/530/540 SUDAH di `WP_MODULE_MAP` (punya lapisan generik) — sisa = persist data DOMAIN (sampel/range/ekspektasi), bukan 0 seperti klaim auditor.

**Remediasi quick-win SUDAH dikerjakan (commit belum dibuat):**
1. `wp_signoff.tsx` — tambah 5 entri `WP_MODULE_MAP`: `sa240, goingconcern, subsequent, related, confirm` → kini auditable (sign-off+bukti+kesimpulan SA 230 otomatis via SubBar global). Ini menaikkan denominator `WpCompletenessRecap`/gate Arsip (lebih ketat = benar).
2. `wp_signoff.tsx` — helper bertipe `eqrStatusFor(engId)` (baca `ams.v1.eqrReviews.v2`, pola sama `opinionFinalized`) + kriteria EQR di gerbang Arsip.
3. `view_opinion_parts.tsx` — `canFinalize` kini menuntut `eqrSubstantiveDone` (review EQR `cleared`, bukan sekadar centang rantai) + Pill "EQR (modul) lolos" + pesan blokir. Ini menutup lubang Q-02: opini PIE tak bisa terbit sebelum EQR substantif selesai.

Gate hijau: `npm run typecheck` + `npm run lint` (ratchet no-explicit-any utuh — helper baru NOL `:any`) + 82/82 vitest. UI smoke terhalang auth W7+TOTP; app boot bersih tanpa error konsol.

**Track Mutu Firma — Fase 0–1 SELESAI (commit `dc00589`, branch `gap-remediation-wp-eqr`, blm push).** PRD: `PRD - Mutu Firma (SOQM…).md`. Pola baru reusable: **`firm_attest.tsx`** (`useFirmAttest`/`FirmAttestCard`) = atestasi LEVEL-FIRMA bertipe penuh, persist `useAmsPersist('firmAttest.<key>')` scope firma (server-backed), sign-off RBAC-gated berlapis, lock LUNAK, sign terkunci s/d kesimpulan tertulis. Q-01: evaluasi tahunan SOQM (`SoqmAnnualEval`) kini attestable (kesimpulan pimpinan + sign-off `CAP.FIRM_ADMIN` + periode). **Koreksi penting:** Q-01 jauh lebih sempit dari klaim auditor — SOQM sudah persist register/remediasi/keluhan; `openRca` cuma state akordeon (RCA seed display-complete). Keputusan Ari: SOQM=pimpinan tunggal, deklarasi=self+partner; Q-03 mulai dari deklarasi.

**Q-03a SELESAI (Fase 2, commit `af0713a`).** RE-SCOPE penting: `Independence` (view_people.tsx:268) SUDAH persist deklarasi (`useAmsPersist('independence')`) + rantai persetujuan 3-tingkat (`useAmsPersist('indepAppr')`: self→manajer etika→partner) — persis model self+partner. Gap nyata = "tidak ber-jejak": `indepAppr` dulu cuma angka level. Diperbaiki → `{id:{level,steps:[{by,at}],period}}` (backward-compat number), tiap lapis rekam penandatangan+tanggal, `INDEP_PERIOD` ganti hardcode "TA 2026". Catatan: `firm_attest.tsx` TIDAK dipakai di sini (workflow per-orang sudah ada; enrich in-place).

**Q-03b/c SELESAI (Fase 3, commit `2bbbce7`).** Genuinely-absent → dibangun di `Independence` drawer: (b) register ancaman/pengamanan persist `indepThreats` (seed dari konflik INDEPENDENCE; tambah/edit kategori IESBA + keparahan + pengamanan + tandai-dimitigasi ber-jejak) · (c) pengakuan rotasi/cooling-off persist `indepRotAck` (akui + tindak-lanjut ber-jejak untuk personel ≥ batas rotasi). Tipe `IndepThreat`/`RotAck`. **Q-03 TUNTAS.**

**PR#6 kini berisi Q-02 + Q-01 + Q-03 (a/b/c) — 5 commit.** baseline `:any` view_people 68→66 (mengetat 2×).

**PR#6 (Q-01/Q-02/Q-03) MERGED 23 Jun → master `012c42d`.**

**SA 250 (NOCLAR) SELESAI → [PR#7](https://github.com/ari1945/Asseris/pull/7)** (branch `sa250-noclar-auditable`, off master, 3 commit + PRD). Track "jadikan display-only auditable": (1) `sa250`→`WP_MODULE_MAP` (sign-off+bukti+kesimpulan SA 230 via SubBar global) · (2) register NOCLAR `useAmsPersist('noclar.<engId>')` engagement-scoped, tambah/edit/hapus ber-jejak · (3) jenjang pelaporan ¶22–29 status terekam + tombol "Memo NOCLAR"→`amsExportPdf`, AI Assist disembunyikan. Tipe penuh `NoclarItem/ReportTier/NoclarState/Ev`; ratchet `view_sa2comm` 5→4; semua gate hijau (migration 82, server 116). **Pola reusable** untuk modul display-only lain: lift state→`useAmsPersist('<key>.<engId>')` + tambah ke `WP_MODULE_MAP` + ketik `Ev`={target:{value:string}} struktural untuk hindari `:any` baru.

**SA 620 (Pakar) SELESAI → [PR#8](https://github.com/ari1945/Asseris/pull/8)** (branch `sa620-expert-auditable`, CI 3/3 hijau, 3 commit+PRD). Pola PR#7 lagi: `expert`→`WP_MODULE_MAP` · `useStateSP2(EXPERTS_SEED)`→`useAmsPersist('experts.<engId>')` (skor ¶9 slider + evaluasi ¶12 bertahan) · tab Kesimpulan: narasi hardcoded→dinamis + **panel sign-off FIKTIF (nama statis)→`WpPanel` kanonik** (tutup utang bespoke-signoff, sejalan P2) · tombol "Tambah Pakar"→`blankExpert` skeleton + "Memo"→`amsExportPdf`. Tipe penuh `ExpertRow`; 0 `:any` baru (baseline `view_specifics2` 43 utuh). **Catatan reuse penting:** `WpPanel moduleId=...` bisa di-drop-in INLINE di view (bukan cuma SubBar) — ideal untuk MENGGANTI sign-off bespoke/fiktif dengan SSOT.

**SA 260 & 265 SELESAI → [PR#9](https://github.com/ari1945/Asseris/pull/9)** (branch `sa260-265-auditable`, 3 commit+PRD). Pola PR#7/#8 ke-3×: `sa260`+`sa265`→`WP_MODULE_MAP` · SA260 `useAmsPersist('tcwg.<engId>')` (matriks status + temuan, **sign-off fiktif Anindya/Hartono→`WpPanel`**) · SA265 `useAmsPersist('deficiencies.<engId>')` (register + klasifikasi, **nama firma hardcode "KAP Wijaya..."→`AMS.FIRM`** dinamis, `WpPanel` di tab Komunikasi) · tombol export wire + AI Assist disembunyikan. **`view_sa2comm` kini 0 explicit-any** (ratchet entri suppression DIHAPUS — SA250+260+265 semua bertipe). **Selesai: seluruh seri SA 250/260/265 + SA 620.**

**SA 402 (Organisasi Jasa) SELESAI → [PR#10](https://github.com/ari1945/Asseris/pull/10)** (branch `sa402-serviceorg-auditable`, 3 commit+PRD, CI hijau). Pola PR#7-9 ke-5×: `serviceorg`→`WP_MODULE_MAP` · `SO_ORGS`→`useAmsPersist('serviceorgs.<engId>')` (tambah/edit/hapus) · **sign-off fiktif (Dimas/Anindya/Hartono)→`WpPanel`** · narasi+reliance hardcoded→dinamis. **Gotcha penting:** SO-01 disinkron dari `socEngine()` (SJAH 3402 SSOT) — IIFE mutasi-const diubah jadi **`applySocSync()` derive-on-render** (jaga SSOT, SO-01 field read-only di form). `view_serviceorg` ratchet mengetat (prune→11).

**ICFR (SA 315/330/265) SELESAI → [PR#11](https://github.com/ari1945/Asseris/pull/11)** (branch `sa315-icfr-auditable`, 3 commit+PRD, CI hijau). Modul TERBESAR (724 baris). Bounded scope: **dua store stateful** dipersist — Store A RCM `useStateIC(IC_CYCLES)`→`useAmsPersist('icfrMatrix.<engId>')` (kesimpulan Test of Controls + walkthrough); Store B evaluasi defisiensi `assess`→`useAmsPersist('icfrDef.<engId>')` (klasifikasi severity SA 265, akses defensif `DEFAULT_DEF_A`). `WpPanel` di tab Evaluasi Defisiensi; lock LUNAK gate ToC/klasifikasi; Export Matriks wire; "Kontrol Baru" disembunyikan. Tipe `ICCycle`/`ICControl`; ratchet prune→60. **Non-scope sengaja:** CRUD kontrol penuh, editing COSO 17 prinsip, mesin ToC.

**SEMUA modul display-only besar TUNTAS: SA 250/260/265/315(ICFR)/402/620.**

**SA-08 GOING CONCERN substantif SELESAI → [PR#12](https://github.com/ari1945/Asseris/pull/12)** (branch `sa570-goingconcern-substantive`, 2 commit+PRD, CI hijau). Track kedalaman substantif (BEDA: memperdalam logika, bukan persist UI). `useAmsPersist('goingconcern.<engId>')`: proyeksi kas dari **asumsi editable** (kas awal/inflow/outflow/utang JT — ganti konstanta hardcoded), **register covenant** (headroom/status, breach→bobot verdict), **evaluasi mitigasi ¶16** (kelayakan+bukti). WpPanel inline. Tipe `GCState`/`Covenant`/`Mitigation`; prune→22. **✅ VERIFIED LIVE** (lihat koreksi TOTP di bawah).

**⚠️ KOREKSI BESAR (PR#12): smoke UI live TERNYATA BISA.** Asumsi "auth W7+TOTP menghalangi smoke" yang dipakai sbg alasan gate-statis di PR#6–#11 **KELIRU** — dev user login TANPA TOTP. Recipe terbukti: `preview_start('dev-all')` (vite+backend) → boot bersih → isi form login `hartono.w@whr-cpa.id`/`Partner#2025!` via native-setter+dispatch `input` event → klik Masuk (lolos, no TOTP) → `localStorage['ams.route']='<modul>'`+reload → drive. Persist diuji: edit→`ams.v1.firm.FIRM-WHR.<key>.<engId>`→reload→bertahan. (preview_screenshot sering timeout → pakai preview_eval baca `body.innerText`/localStorage.) Lihat handoff 2026-07-03 (dihapus).

**FRAUD SA 240 substantif SELESAI → [PR#13](https://github.com/ari1945/Asseris/pull/13)** (branch `sa240-fraud-substantive`, 2 commit+PRD, CI hijau, **VERIFIED LIVE**). `useAmsPersist('fraud.<engId>')`: register risiko editable (tambah/edit/hapus ber-jejak), faktor segitiga toggle interaktif (¶24), prosedur override ¶32 toggle. Sign-off fiktif (Putri/Anindya)→`WpPanel`. Tipe `FraudState`; prune→6. **Bukti live: Tambah→FR-06 ber-jejak "Hartono Wijaya" (user login)→reload→bertahan.** Pola `view_sa530`/`view_sa540` IDENTIK (pure-display, 0 useState) — tinggal ulang.

**SA 540 ESTIMASI substantif SELESAI → [PR#14](https://github.com/ari1945/Asseris/pull/14)** (branch `sa540-estimate-substantive`, 2 commit+PRD, CI hijau, **VERIFIED LIVE**). `useAmsPersist('estimates.<engId>')`: inventaris estimasi editable (titik mgmt/rentang lo-hi/asumsi via textarea) + indikator bias ¶32 editable. WpPanel inline tab Bias (tanpa sign-off fiktif — sa540 sudah di WP_MODULE_MAP). Tipe `EstState`; prune→1. **Bukti: Tambah E-06 ber-jejak user login→reload→bertahan.**

**SA 530 SAMPLING substantif SELESAI → [PR#15](https://github.com/ari1945/Asseris/pull/15)** (branch `sa530-sampling-substantive`, 2 commit+PRD, CI hijau, **VERIFIED LIVE**). `useAmsPersist('sampling.<engId>')`: parameter kalkulator MUS (bv/conf/tm/em) persist (ukuran sampel recompute) + temuan salah saji editable (tainting/proyeksi recompute). WpPanel inline tab Evaluasi. Tipe `SamplingState`; prune→6. **Bukti: ubah bv 245k→100k persist + Tambah S-130 ber-jejak user login → reload → bertahan.** **TRIO SUBSTANTIF fraud/estimasi/sampling TUNTAS.**

**GOING CONCERN LANJUTAN (rasio/Altman canon-SSOT + multi-skenario) SELESAI → [PR#16](https://github.com/ari1945/Asseris/pull/16) MERGED ke master `c599737` (24 Jun 2026)** (branch `sa570-goingconcern-ratios-canon`, CI 3/3 hijau, **VERIFIED LIVE**). Follow-up eksplisit PR#12 (2 item Non-Scope). **Canon baru `canon_part5.ts` `goingConcern(wtb?)`** — rasio (CR/QR/DER/ICR/OCF) + Altman Z (X1–X5+zone) dari WTB by-code-prefix utk `adj`&`ly`; OCF=metode tak-langsung murni-WTB; daftar AMS_CANON + selektor `goingConcernFor` + tipe `GcAggregates/AltmanZ/GoingConcernResult` (canon_types) + test `canon_part5.test.ts` mengunci figur=nilai hardcoded yg digantikan (CR1.60/QR0.79/DER0.97/ICR3.96/Z2.69 grey); **snapshot regresi di-update sengaja** (`npm run test -- -u`, aditif). **View:** `GC_RATIOS`&`z` literal DIHAPUS→`goingConcernFor(useAudit().wtb)` reaktif; sparkline `[py,cy]` nyata. **Multi-skenario:** `scenarios[]`+`activeScenarioId` persist (migrasi mulus dari `scenario` lama via `normScenarios`), chip pilih/tambah/rename/hapus, `projectCash()` murni, tabel banding (kas terendah+defisit/skenario). Ratchet view_goingconcern 22→10. **Bukti live:** Z 2.69 grey, CR1.60/PY1.72, OCF16.09M; 2 skenario −30%/−35% bertahan reload, defisit APR vs MAR. **GOTCHA:** `preview_click` MCP kadang tak memicu React onClick (timing) → pakai `el.click()` via preview_eval. Verifikasi grup/agregat WTB pakai by-code-prefix (1-1/1-2/2-1/2-2/3-/4-/5-); X4 Altman = nilai buku ekuitas (proksi pasar).

**JET/eksepsi SA 240 SELESAI (commit `cd2253e`, masuk master via PR#16 merge `c599737`).** `view_jet.tsx`: skoring inline→canon `AMS_FORENSIC.score(JE_POP,active)` (SSOT, populasi+kriteria SAMA dgn Forensic Cash Flow); kriteria/ambang/disposisi-per-jurnal (clear/eksepsi) persist + textarea "Tindak Lanjut" ber-jejak (by/at) utk eksepsi; lock LUNAK; `WpPanel moduleId="jet"` inline (sudah di WP_MODULE_MAP). Tipe `JetState/JetTest/Ev`; ratchet view_jet 16→1 (prune). **VERIFIED LIVE** sbg Audit Manager (anindya.p/Manager#2025!): eksepsi JV-24-08841 ber-jejak→clear localStorage→reload→**rehydrate dari SERVER** (badge "Eksepsi" + by Anindya).

**🐞 BUG NYATA DITEMUKAN saat JET (high-confidence, di-spawn_task `task_85a92ff4`):** `useAmsPersist(key,init)` **default scope='firm'** bila key tak terdaftar di `AMS_PERSIST_SCOPE` (contexts.tsx:55). Pola `'<key>.'+engId` (SA530 `sampling.`, SA540 `estimates.`, SA240 `fraud.`) → firm scope → `capForWrite('firm',key)` (rbac.ts:69-84) butuh **FIRM_ADMIN** → hanya Engagement Partner punya. Manager/Senior/Junior: `state.set` lempar FORBIDDEN, `flush` catch menelan diam → temuan HANYA di localStorage, hilang saat clear-cache/device/user lain. **Inilah kenapa "VERIFIED LIVE" trio fraud/estimasi/sampling MENIPU: diuji sbg `hartono.w` (Partner, PUNYA FIRM_ADMIN) → tulisan firm-scope lolos & MENUTUPI bug utk non-partner.** **Fix (dipakai di JET):** key statis `'jet.v1'` + daftar di `AMS_PERSIST_SCOPE` scope `'engagement'` (precedent `diagnostics.v1`/`aiInsights.v1`) → `capForWrite('engagement',...)`=WP_EDIT (semua auditor punya) + isolasi W7.5. **Pelajaran: substantive findings WAJIB engagement-scope (key statis `.v1`), JANGAN firm-scope `'+engId'`; & verifikasi live pakai role NON-partner (Manager) agar gate RBAC ketahuan.**

**🐞 FIX FIRM-SCOPE PERSIST TUNTAS untuk SEMUA modul sisa → [PR#17](https://github.com/ari1945/Asseris/pull/17) MERGED ke master `75b03ea` (24 Jun 2026; bersama [PR#16](https://github.com/ari1945/Asseris/pull/16) merge `c599737` yg membawa going-concern-ratios + JET/SA240). Branch `persist-scope-canon-remaining6`, 2 commit `513c46a`+`357ef13`, CI 3/3 hijau.** Catatan riwayat: branch ini sempat dibuat percobaan sebelumnya (commit dangling `c1bb438` = hanya `contexts.tsx`, lalu `reset HEAD~1`); commit final saya superset ketat — tak ada yg hilang. **Gotcha PR stacked:** branch off `sa570…` yg PR-nya belum merge → base PR awalnya salah (master, double-diff); fix = set base ke branch induk, GitHub TAK auto-retarget bila branch induk tak dihapus → setelah induk merge, set base balik ke master manual. Lanjutan `task_85a92ff4`. **9 situs** key statis `.v1` scope engagement: ICFR matrix+defisiensi, going concern, opini, NOCLAR/TCWG/defisiensi (SA250/260/265), service org, pakar. **+3 situs lolos grep awal** karena pakai `eng.id`/`activeEngagement.id` bukan literal `engId`: strategi tab+pendekatan (SA300), memo analitis (SA520) → `strategyTab.v1`/`strategyApproach.v1`/`arMemo.v1`. Total 12 entri `AMS_PERSIST_SCOPE` baru. **Positif palsu (JANGAN migrasi):** `firmAttest.'+attestKey` (firm_attest.tsx) sengaja firm-scope by design. **Pelajaran grep:** enumerasi `+engId` literal MELEWATKAN ekspresi engId lain — sweep juga `eng.id`/`activeEngagement.id`/template-literal.
**🐞 TEMUAN BARU (commit `357ef13`): 3 pembaca gerbang P5 baca cacheKey W6 SALAH (basi sejak W6).** `opinionFinalized`+`eqrReviewsLS` (wp_signoff.tsx) & `prLoadLS('opinionDoc'…)` (view_presentasi.tsx) menunjuk key **legacy pra-W6** `ams.v1.<key>` yg TAK PERNAH ditulis pasca-W6 (W6 nulis cacheKey berlingkup `ams.v1.<scope>.<scopeId>.<key>`) → gerbang "Arsip butuh opini final" & gerbang EQR baca data basi. Fix: repoint ke `ams.v1.engagement.<engId>.opinionDoc.v1` & `ams.v1.firm.FIRM-WHR.eqrReviews.v2`. **Pelajaran: tiap rename key persist → grep pembaca localStorage LANGSUNG lintas-modul (bukan cuma view pemilik); gerbang P5 baca cache sinkron, hanya terisi bila view pemilik pernah hidrasi di browser ybs (server-backed gate = arsitektur tersendiri).**

**TAB ANALITIK SA 530 & SA 540 SELESAI → [PR#18](https://github.com/ari1945/Asseris/pull/18) MERGED ke master `57b0bf0` (24 Jun 2026)** (branch `sa530-540-analytical-tabs`, 3 commit+PRD, CI 3/3 hijau, **VERIFIED LIVE role Manager**). PRD-first (sign-off Ari). Temuan kunci: tab "risiko/respons/sensitivitas" (540) & "desain/seleksi" (530) **sudah ada tapi statis hardcoded + menduplikasi register** → bukan "bikin tab baru", tapi derive-SSOT + fungsionalkan. **Fungsi murni baru (util lokal, bukan canon — spesifik modul):** `sampling_select.ts` `selectMus()` (MUS sistematis deterministik seed+interval; populasi ilustratif 100 item bobot + `scalePopulation` total=bv SSOT) + `estimate_sensitivity.ts` `estimateSensitivity()` (driver Δ%×perPct → titik baru + verdict rentang+breach); 11 uji unit. **SA530:** Desain ambang/rasional editable+persist + stratifikasi derive; Seleksi **generator item MUS nyata** (seedFrac persist, "Acak ulang"). **SA540:** Risiko cplx/subj editable+persist derive register; Respons rentang independen derive; **sensitivitas interaktif** (driver persist per estimasi). State diperluas backward-compat (default seed bila absen). **Bukti live (Manager anindya.p):** generator 60 item; **edit ambang→clear cache→reload→rehydrate dari SERVER** (RBAC non-Partner OK); sensitivitas 630→titik baru 5.500 dalam rentang. Ratchet sa530 :any 6→4 (prune), sa540 tetap 1; gate tsc 0/eslint 0/vitest 98/98. **Gotcha verify:** route via `localStorage['ams.route']='sa530'` **plain string** (BUKAN JSON.stringify → quotes bocor jadi tooltip); AMS bukan lagi window-global (ESM) → baca state via localStorage/DOM, bukan `window.AMS`.

**AK-01 PENOMORAN PSAK SELESAI → [PR#19](https://github.com/ari1945/Asseris/pull/19) MERGED ke master `fbad386` (24 Jun 2026)** (branch `ak01-psak-renumber`, 3 commit+PRD, CI 3/3 hijau, VERIFIED LIVE). PRD-first. **Sumber mapping = dokumen RESMI IAI** "Perubahan Penomoran PSAK dan ISAK" (web.iaiglobal.or.id) — penomoran berubah efektif 1 Jan 2024 (1xx=IFRS, 2xx=IAS, 3xx=lokal, 4xx=syariah), substansi tetap. **GOTCHA RISET: ringkasan AI WebSearch garbled** (klaim "PSAK 109=d/h PSAK 72" SALAH) → WAJIB ambil dari PDF resmi (WebFetch saved → Read PDF langsung). Peta `PSAK_RENUMBER` (41 PSAK+13 ISAK, SSOT di data_sakroadmap, ekspos `AMS_CANON.psakRenumber`, tipe `PsakRenumberRow`, uji unit kunci 71→109/73→116/22→103/25→208/46→212/48→236/65→110). Surface: tab "Padanan Penomoran" di view_sakroadmap + anotasi label modul icons ("PSAK 71 → 109 ·…"). **🐞 BUG DITEMUKAN & DIKOREKSI: kode salah melabeli IFRS 18="PSAK 207" & IFRS 19="PSAK 208"** — padahal resmi PSAK 207=Arus Kas (d/h PSAK 2), 208=Kebijakan Akuntansi (d/h PSAK 25); **IFRS 18 benar=PSAK 118** (ED PSAK 118). Tersebar 4 file (data_sakroadmap, view_sakroadmap fitur "Kesiapan PSAK 207", related_modules_data2 "PSAK 1→207" jg salah, harusnya 201). compmatrix SUDAH benar (207=IAS7) → dibiarkan; IFRS 19 → label netral (nomor belum ditetapkan IAI). Gate tsc 0/eslint 0/vitest 103. **PSAK lama→baru inti: 1→201, 22→103, 24→219, 25→208, 46→212, 48→236, 57→237, 58→105, 65→110, 71→109, 72→115, 73→116, 74→117.**

**SA-01 SEMPIT SELESAI → [PR#20](https://github.com/ari1945/Asseris/pull/20) MERGED ke master `fd3132c` (24 Jun 2026, CI 3/3)** (branch `sa01-acceptance-letter-signoff`, 1 commit+PRD). PRD-first. **Koreksi klaim matriks lagi:** "tidak ada generator surat perikatan" USANG — `StepLetter` (view_onboarding2: generator berversi+preview+TTE PSrE/e-Meterai+esign log+export) & `StepAcceptance` (view_onboarding: skor/verdict/sign-off) SUDAH ADA. Gap nyata sempit = sign-off auditor tak ber-jejak/tanpa RBAC/bukan append-only. Fix (pola Q-03a): approver=`useAuth().user` (bukan p.partner hardcode); RBAC `CAP.FIRM_ADMIN` gate tombol Setujui/Buka-kembali (akseptasi) & Buat/Kirim/Tandatangani (surat); `trail[]` append-only {action,by,at} approve+reopen. StepLetter peristiwa KAP who=login; **tanda tangan KLIEN+TTE/Meterai TAK disentuh**. **Selaras server:** `capForWrite('firm','prospects')=FIRM_ADMIN` → UI dulu tanpa gate = non-Partner ditolak diam-diam; kini selaras. **Temuan terpisah → DITANGANI [PR#21](https://github.com/ari1945/Asseris/pull/21)** (branch `prospects-intake-rbac`, 1 commit+PRD, Opsi A): `capForWrite('firm','prospects')` FIRM_ADMIN→**ENGAGEMENT_MANAGE** (SSOT migration/src/rbac.ts, di-re-export server) → Partner+Manager intake; Senior/Junior ditolak; persetujuan tetap Partner via gate klien (PR#20) — pola intra-doc gating terdokumentasi. Trade-off Opsi A diterima (server granularitas dokumen; tak split persist). Uji authz router nyata +Manager-CAN/+Senior-CANNOT; server 118/118. **Gotcha:** `useAuth()` WAJIB unconditional (rules-of-hooks; null-safe spt firm_attest) — pola defensif `typeof useAuth==='function'?…` KENA lint react-hooks. Gate tsc 0/eslint 0/vitest 103; render bersih; drawer onboarding multi-langkah tak didorong penuh via preview (logika gate deterministik Manager≠FIRM_ADMIN). **DAFTAR GAP INTI AUDITOR PADA DASARNYA TERTUTUP.**

**Roadmap sisa (tiap track = PRD terpisah):**
- ~~JET/eksepsi SA 240~~ TUNTAS · ~~SA530/540/240 fix scope firm→engagement (`task_85a92ff4`)~~ · ~~sisa 9+3 situs firm-scope→engagement + 3 pembaca gerbang P5 (`persist-scope-canon-remaining6`)~~ TUNTAS · ~~tab analitik SA 540 (risiko/respons/sensitivitas) & SA 530 (desain/seleksi/generator item)~~ TUNTAS PR#18 · ~~AK-01 penomoran PSAK~~ TUNTAS PR#19 · ~~(opsional) SA-01 sempit~~ TUNTAS PR#20. (~~Going concern lanjutan~~ TUNTAS PR#16.)
- **Kedalaman substantif:** going concern kuantitatif (SA-08), persist domain fraud/sampling/estimasi.
- **Penomoran PSAK baru (AK-01).**
- **(opsional) SA-01 remediasi sempit:** sign-off acceptance/surat ber-jejak ke user login + RBAC + append-only (pola Q-03a) — Ari belum pilih ini.
Lihat handoff 2026-07-03 (dihapus).
