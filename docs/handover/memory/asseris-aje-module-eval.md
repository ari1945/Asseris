---
name: asseris-aje-module-eval
description: "Evaluasi modul AJE (2026-07-26) — konstanta PBT/likuiditas modul 3x meleset dari WTB dan menjadi sumber benchmark materialitas; entri baru lahir 'Posted' melewati gerbang ISQM 1 yang diklaim modulnya sendiri."
metadata: 
  node_type: memory
  type: project
  originSessionId: 85f57879-597a-4425-b603-612b408c57a5
  modified: 2026-07-27T07:13:30.875Z
---

Evaluasi `migration/src/view_aje.tsx` pada 2026-07-26 (branch `feat/audit-context-value-types`, master `5a81dd2`). Belum ada PR.

**ARC SA 600 DIMULAI — Ari memilih OPSI A (populasi konsolidasian) + A-1.** Bukti pemicu: `view_opinion.tsx:84` menerbitkan "laporan keuangan {client} (**Perusahaan**)" — kata "konsolidasian" NOL kemunculan di view_opinion & fsgen_model, padahal entitas Tbk dgn 4 anak yang `GROUP_CONTROL` simpulkan Dikonsolidasi. Jadi sistem menerbitkan opini STANDALONE untuk entitas yang WAJIB publikasi konsolidasian — cacat lebih besar dari materialitas.
**SA 600 PR-1 MERGED `c65d677`** (#146) — paket komponen membawa `pbt`+`tax` (masuk `PKG_NUMF` & `PKG_FIELDS`), `psak65` meroll-up `consolRev/consolPbt/consolTax`. Identitas `consolPbt−consolTax===consolNpat` tertutup ALJABAR. Seed: CP-02/03/04 tarif 22%, **CP-05 17% (Singapura)** — itu alasan field-nya harus ada. Snapshot: 22 baris DITAMBAH, 0 berubah.
**SA 600 PR-2 MERGED `c7766db`** (#147) — `groupMateriality()` di canon_part3. GROUP_MAT 6,2 M/GROUP_PM/GROUP_CTT + 5 mat komponen + faktor `*0.35` DIBUANG. **LIVE: grup Rp 2,74 M · PM 2,06 M · komponen 1.371/1.371/960/—/960 jt.** Kebijakan alokasi SATU tempat: ukuran 50% · risiko 35% · Analytical 0; batas 50% menjamin SA 600 ¶21 SECARA STRUKTURAL (diuji 4 basis PBT). **Kontradiksi inti tertutup: CP-01 (= entitas ENG-2025-014) kini 1,371 M < OM standalone 1,4845 M**, dulu 4,25 M (selisih 2,9×).
**GOTCHA:** kanon punya kontrak **setiap fungsi harus bisa dipanggil TANPA argumen** (`canon_regression` menjaganya) → beri default `= {}`. Snapshot kanon WAJIB diperiksa `-`/`+` sebelum `-u`.
**MASTER `f3d11ae`** — #145 (`4c1f4a3` titik henti `.split` + `.gitattributes` snap) & #144 (`f3d11ae` PRD) juga MERGED. NOL PR arc tersisa; hanya 2 Dependabot.
**PSAK 46 FISKAL — BELUM dikerjakan, dan lebih besar dari tukar-konstanta. DUA TEMUAN DESAIN:** (1) **Pajak dinilai PER ENTITAS HUKUM** (SPT per entitas), jadi rekonsiliasi fiskal tetap **STANDALONE induk** meski materialitas kini konsolidasian — dua populasi untuk dua tujuan, KEDUANYA benar; ini MENGUATKAN basis = PBT dilaporkan induk 25.750. (2) `FISCAL.pbt/pkp` dipakai di `canon_base.ts:277-278` di dalam builder FIG yang memanggil **`figuresFromWTB()` TANPA argumen** — dan fungsi itu **tidak mengekspos `pbt`**; "dilaporkan" juga butuh status posting AJE yang TIDAK tersedia di builder murni itu. Jadi perlu menjahitkan `entityFigures(...,'unadj')` + `ajeEffect(aje)` ke jalur fiskal → perubahan desain, bukan 2 baris. Konsekuensi angka: pajak kini 11.770 → ~6.765 (−43%), dan snapshot akan menunjukkan baris BERUBAH (bukan hanya ditambah) — periksa satu per satu.
**ARC SA 600 TUNTAS — Opsi A TERPASANG. Master `eb686ce`.** PR-3a `ab84b23` (#148): laporan audit menyatakan **LK konsolidasian** — paragraf menyebut entitas anak & menamai pelapor "Grup"; `OPINION_PARA_O`/`OP_TXT` diekspor utk diuji (teks opini = DELIVERABLE audit). PR-3b `eb686ce` (#149): **`psak65` menerima parameter `basis`** (default 'adj' → nol pergeseran konsumen lama) sehingga konsolidasi dapat dihitung pada 'unadj' — WAJIB, karena SA 320 menetapkan ambang atas figur DILAPORKAN dan basis 'adj' menghidupkan sirkularitas yg ditolak PR-A. `engagementBenchmarks()` jadi SATU pintu.
**DAMPAK TERUKUR LIVE: OM 1,4845 M → 3,0876 M** (PBT konsolidasian 61.751 jt = induk unadj 29.690 + dividen 2.100 + Σanak 32.701 − elim 2.740). **Agregat SAD 220% → 106% OM — MASIH >100%, peringatan SA 705.7/.8 TIDAK padam.** Drift tetap menyala (27%).
**PELAJARAN BESAR — verifikasi live menangkap yg uji tak bisa:** setelah perubahan pertama, KPI sudah konsolidasian sementara TABEL benchmark masih standalone, karena `contexts` & `view_materiality` masing-masing memanggil benchmark sendiri. Modul membantah dirinya di layar yg sama, 731 test tetap hijau. **Selalu cek KPI vs tabel di modul yg sama setelah mengubah sumber angka.**
**Yg SENGAJA dihilangkan, bukan dikarang:** Laba Bruto tak ada di tabel grup (paket komponen tak bawa beban pokok) · komparatif TA-1 disembunyikan ("komparatif TA-1 tak tersedia") karena kolom `ly` WTB standalone → membandingkannya memberi "+150,1%" yg murni artefak perpindahan populasi. Mengaktifkan keduanya = tambah `cogs`/figur komparatif ke paket komponen.
**SISA: SA 600 PR-3 (SUDAH SELESAI — abaikan baris di bawah)** — basis OM perikatan, teks opini, FS Generator naik ke konsolidasian. Akan MENGGESER kesimpulan SA 450 (agregat 220% OM bisa turun <100% → peringatan opini modifikasian padam). Plus **PRD fiskal PSAK 46** (#144) masih menunggu basis PBT.
**PR terbuka:** #144 (PRD) · #145 (`.split` titik henti + `.gitattributes` snap EOL; **175 tempat di 71 berkas lain masih rentan** → PR sapuan tersendiri).
**PR-E MERGED `dd9d689`** (#143). Master kini: #140·#139·#141·#142·#138·#143. PRD di #144 (`cf4e8c9`).
**⚠️ KOREKSI KETIGA — prasyarat Opsi A SA 600 diperiksa, `psak65` TERNYATA SEHAT.** `canon_part3.ts:387-528`: `psak65(wtb)` menarik induk LANGSUNG dari WTB (basis `adj` lewat `aj()`), mengonsolidasi 4 anak + 5 jurnal eliminasi (INTERCO), **neraca menutup** (`balCheck === 0` diuji), goodwill di-tie ke `AMS_CANON.GOODWILL`, dan `consolNpat = indukSeparate + subsNpat − elimLaba` lengkap dgn NCI/ownersProfit. Bukan seed beku.
**Klaim PRD draf-1 saya SALAH:** "Σ npat komponen 40,41 M tak pernah direkonsiliasi dgn laba WTB 23,158 M". **`GROUP_SUBS` = CP-02..CP-05 SAJA — induk sengaja TIDAK di sana** karena berasal dari WTB. Saya menjumlahkan CP-01 ke komponen padahal CP-01 ADALAH entitas WTB → menghitung induk dua kali. Σ npat anak sebenarnya = 25.750.
**Prasyarat yang BENAR-BENAR kurang:** paket komponen membawa `rev` & `npat` tapi **TIDAK PBT/beban pajak per anak** → benchmark PBT tak dapat dihitung utk grup tanpa mengasumsikan tarif seragam (salah utk entitas SGD). Q3 baru utk Ari: **A-1** perluas paket komponen dgn PBT+pajak (rekomendasi) vs **A-2** pindah benchmark grup ke Total Aset/Pendapatan konsolidasian (sudah derivable hari ini: `totals.aset.konsol`; pendapatan = induk + Σ anak − ELM-01).
**Cacat tambahan:** `view_groupaudit.tsx` `GA_COMPONENTS` menyimpan **CP-01 hardcode** (`npat: 14_660_000_000`) padahal induk hidup di WTB via psak65 — salinan mati, sekelas `AJE_META.pbt` yang dibuang PR-D.
**POLA KESALAHAN SAYA HARI INI (3x): menyatakan sesuatu mustahil/hilang tanpa memeriksa sumber paling dasar** — "mustahil menurut SA 600" (tak cek populasi mana yang diatur), "kredensial Manager mustahil" (ada di README:150), "npat tak direkonsiliasi" (tak cek isi GROUP_SUBS). Verifikasi dulu, vonis kemudian.
**⚠️ KOREKSI KEDUA (2026-07-27) — "verifikasi live peran Manager MUSTAHIL tanpa kredensial" itu SALAH.** Kredensial seluruh peran ada di **README.md:150 & BUILD.md:87** repo sendiri. `setRole` memang mati sejak W7 tapi tak pernah relevan: cukup logout lalu login akun lain. **Audit Manager = `anindya.p@whr-cpa.id` / `Manager#2025!`** — ia bahkan pengguna seed UTAMA (`seed.ts:38` PRIMARY_PASSWORD). Lainnya: Rekan Pemimpin `hartono.w@whr-cpa.id`/`Partner#2025!` · Senior `dimas.r@whr-cpa.id`/`Senior#2025!` · Junior `fajar.n@whr-cpa.id`/`Junior#2025!`. **Pelajaran: cek README/BUILD.md sebelum menyatakan sesuatu tak terverifikasi.** (Agen tak boleh mengetik kata sandi ke form — minta Ari yang login, lalu baca DOM.)
**VERIFIKASI MANAGER TUNTAS — gerbang per-langkah PR-B TERBUKTI DI LAYAR.** Sebagai Anindya P. (Audit Manager) di modul Approvals: **AJE-05** (langkah menunggu = Audit Manager) → tombol ADA, berlabel "Setujui Langkah Saya" (kontrol POSITIF — tanpa ini "semua ditolak" bisa berarti gerbang rusak); **AJE-03** (langkah = Engagement Partner) → TERKUNCI, nol tombol; **AJE-01** (langkah = EQR) → TERKUNCI, nol tombol. Resep uji tiga-arah ini reusable untuk gerbang peran mana pun. CATATAN: ini membuktikan lapis KLIEN (`rbac.ts` statis); lapis server (`can()` DB-backed) tetap butuh re-seed + restart, dan seed itu DESTRUKTIF (hapus 11 tabel).

**2026-07-27 — ARC TUNTAS, KELIMA PR MERGED.** Urutan: #140 `93ebd96` → #139 `197a2ab` → #141 `a28b384` → #142 `5970f23` → #138 `4f98fdb`. Master diverifikasi SETELAH gabung (tiap CI hanya menguji pra-merge): typecheck 0 · lint 0 · **713 test** · 0 PR arc tersisa.
**RESEP RETARGET yang TERBUKTI (pakai ini, jangan `git rebase origin/master` polos):** `git rebase --onto origin/master <tip-base-LAMA>` — mis. untuk PR-C `git rebase --onto origin/master 85eabee`, PR-D `... 895096e`. Rebase polos akan memutar ulang commit yang sudah tersquash. Lalu `gh pr edit <n> --base master`, `git push --force-with-lease`.
**Konflik NYATA hanya 2, keduanya instruktif:** (1) `eslint-suppressions.json` konflik di SETIAP rebase — berkas GENERATED, JANGAN dijahit tangan: `git checkout --ours`, lanjutkan rebase, lalu `npx eslint src --prune-suppressions` + `npm run lint` (exit 2 = masih ada suppression basi), lalu `git commit --amend`. Ratchet `any` turun **75→73→65** — PR-C & PR-D ternyata mengetik lebih banyak `any` daripada yang diklaim PRD-nya, ketahuan HANYA karena konflik memaksa regenerasi. (2) #138 vs master: `tsc` menolak `view_platform.tsx:167` — PR-B memanggil `toggleAjeStatus(id, meta)` 2-argumen, tipe PR-6d `(id: string) => void`. Implementasi master `(id, meta?: {by?, approvalId?})` → **tipe yang basi, diselaraskan ke implementasi**. Cacat integrasi yang mustahil terlihat sebelum kelimanya dipertemukan.
**GOTCHA BARU (muncul 3x):** `canon_regression.test.ts.snap` berubah akhiran baris LF↔CRLF tiap `npm test` → pohon kotor & `git checkout` DIBLOKIR. Selalu `git diff --ignore-all-space` dulu (isi = 0 baris) sebelum `git checkout -- <file>`. Perbaikan permanen: satu baris `.gitattributes`.
**2026-07-27 — TINJAUAN PIKSEL AKHIRNYA JALAN (utang 4 sesi lunas untuk A/C/D).** Ari membuka panel Browser; login dilakukan Ari sendiri (Hartono Wijaya · Rekan Pemimpin · ENG-2025-014). Dev server `dev-all` port 5180 milik SESI CHAT LAIN — jangan `preview_start`, cukup `preview_start {url:'http://localhost:5180'}`; ganti branch memang mengubah kode di bawah kaki sesi itu (Ari mengizinkan, dikembalikan setelah selesai). **CI kelima PR (#138-#142) 6/6 hijau semua.** Verifikasi anti-basi yang MURAH & WAJIB: `fetch('/src/view_aje.tsx')` lalu cek simbol penanda PR — membuktikan server menyajikan branch yang benar tanpa menebak.
**Terverifikasi di layar:** OM 1.484.500.000 · drift **MENYALA 65%** (kriteria sukses PRD PR-A, dulu dibungkam di 0,235%) · agregat SAD 3.270 jt = **220% OM** memicu SA 705.7/.8 (dgn OM lama 4,26 M hanya 77% → alarm mustahil menyala) · faktor kualitatif kovenan **NONAKTIF** (dibaca dari `getComputedStyle`, bukan teks) · jejak audit ISO terurut · AJE-01 tampil **3/4 dgn EQR MENUNGGU**.
**Dua "cacat" yang saya kejar dan TERNYATA BENAR — jangan diulang:** (a) selisih nilai M-01/M-04 tak beralarm karena `canon_validation.ts:261` hanya mengeskalasi selisih **residual** (disp=dikoreksi & AJE=Posted); item uncorrected sudah utuh di agregat → alarm = hitung ganda; (b) AJE 1,62→1,60× vs SAD 1,62→1,52× BUKAN kontradiksi — cakupan beda & masing-masing berlabel; 1,60× tie dgn rasio WTB adjusted.
**TEMUAN BARU (nyata, belum diperbaiki) — Partner dapat menjadi EQR bagi dirinya sendiri.** `stepAuthority()` (`view_platform.tsx:89`) mencocokkan KAPABILITAS, bukan IDENTITAS: `rbac.ts:99` memberi `EQR_REVIEW` ke `PARTNER_BASE` dan uji baris 138 memang memakunya. Cek self-approval hanya `user.name === it.from` (**penyusun**) — tak pernah bertanya "sudahkah orang ini menandatangani langkah lebih awal pada rantai yang sama", dan tak membandingkan `user.name` dgn nama penerima tugas langkah (Rudi Gunawan). Bukti live: Hartono yang sudah tandatangan AJE-01 sbg Engagement Partner 08 Mei mendapati tombol "Setujui & Finalkan" langkah **EQR** aktif penuh. Melanggar ISQM 2 / SA 220.36. **BUKAN regresi** — pra-PR-B gerbangnya `role.includes(...)` + stempel otomatis, jauh lebih longgar → jangan blokir #140 karenanya. Perbaikan: 2 pemeriksaan identitas + uji → digabung ke **PR-E**.
**Keputusan Ari 2026-07-27:** urutan merge **#140 → #139 → #141 → #142 → #138**; AJEForm capture jadi **PR-E terpisah dari master** (bukan digabung ke #140). Cacat lain tercatat: `view_platform.tsx:217` menyuruh "Ganti peran di menu pengguna" padahal `setRole` mati sejak W7; tab Dampak AJE tumpang-tindih di <1100px (`view_aje.tsx:486` kolom 340px tetap, ditulis `d60199d` 17 Jun 2026 — pre-existing, sudah jadi chip tugas).

**PR-A SUDAH DIIMPLEMENTASI & DI-PR: [#139](https://github.com/ari1945/Asseris/pull/139)** — branch `feat/aje-pr-a-entity-figures`, commit `db41dca` + `85eabee` (dari master `31e2ef2`; #138/PR-6d masih terbuka tapi footprint-nya NOL overlap dengan PR-A → §6 PRD terlalu hati-hati, tak perlu menunggu). 683 test hijau · typecheck 0 · lint bersih. Terverifikasi LIVE: benchmark PBT 29.690.000.000, OM 1.484.500.000, PM 1.113.375.000, CTT 74.225.000; AJE & SAD sama-sama menyebut PBT dilaporkan 25.750 jt; rasio lancar 1,64x→1,62x. Yang dibangun: `entityFigures(wtb,basis)` + `benchmarksFromWTB()` + `ajeEffect()` di canon_base (murni, TANPA fallback AMS.WTB), `materiality(opts.benchmarks)` + `benchSource` di canon_part4, `useMateriality` jadi satu pintu. GOTCHA saat kerja: (a) ratchet ESLint menyala karena SATU `(s: any, a: any)` baru di view_aje → un-suppress 78 error se-berkas, ketik eksplisit; (b) `ENGAGEMENTS` di app hidup datang dari SERVER, jadi ubah `data_part1.ts` TIDAK terlihat tanpa reseed; (c) screenshot tetap mustahil tanpa panel Browser dibuka Ari — piksel belum ditinjau manusia (utang ketiga kali).

**REGIME MATERIALITAS KELIMA — ⚠️ PERUMUSAN LAMA DI BARIS INI KELIRU, JANGAN DIPAKAI.** Klaim lama: materialitas komponen "SELURUHNYA di ATAS OM grup 1,4845 M, mustahil menurut SA 600". **SALAH, dikoreksi 2026-07-27:** (a) 1,4845 M BUKAN materialitas grup — ia turunan WTB, dan `view_groupaudit.tsx:47` menyatakan WTB = **saldo STANDALONE entitas induk**; materialitas grup berlaku atas LK konsolidasian yang populasinya lebih besar, jadi melebihi OM induk itu WAJAR; (b) syarat SA 600 sesungguhnya (komponen < grup) justru TERPENUHI: 4,25 · 2,1 · 1,65 · 1,4 semuanya di bawah GROUP_MAT 6,2 M. Pelajaran: "mustahil menurut standar" diucapkan tanpa memeriksa POPULASI mana yang diatur tiap ambang.
**Cacat yang NYATA (PRD ditulis: `PRD - Materialitas Grup SA 600 (populasi, derivasi & entitas yang sama dua materialitas).md`):** P-1 CP-01 = PT Sentosa Makmur = **entitas yang sama dengan ENG-2025-014** namun membawa DUA materialitas: 1,4845 M sbg perikatan vs **4,25 M** sbg komponen — selisih 2,9×, satu entitas satu periode. P-2 angka 4,25 M itu warisan OM fantasi pra-PR-A (4,26 M) yang sudah didiskreditkan. P-3 `GROUP_MAT 6,2 M` konstanta telanjang tanpa basis apa pun; GROUP_PM/CTT rapi 75%/5% DI ATAS fondasi yang tak pernah dihitung — kelas sama dgn PBT 85,2 M. P-4 `view_groupaudit.tsx:137` default `GROUP_MAT * 0.35`, angka ajaib.
**MEMBLOKIR — keputusan Ari:** populasi ENG-2025-014 = **Opsi A** (audit atas LK konsolidasian → OM harus dari figur konsolidasi, GROUP_MAT jadi turunannya; REKOMENDASI saya) atau **Opsi B** (audit induk-standalone; modul grup milik perikatan lain). Jangan menebak — itu mengarang metodologi audit.

**2026-07-27 — TINJAUAN PIKSEL AKHIRNYA JALAN (utang 4 sesi lunas untuk A/C/D).** [lanjutan catatan lama di bawah]
**PR-E TERIMPLEMENTASI & DI-PR: [#143](https://github.com/ari1945/Asseris/pull/143)** — `8d2e7eb`, dari master `4f98fdb`, CI 6/6 hijau, DIBIARKAN TERBUKA untuk tinjauan Ari (kode baru yang mengubah gerbang persetujuan). typecheck 0 · lint 0 · **719 test** (713 + 6). Isi: (a) `stepAuthority` cek IDENTITAS — langkah ber-status `approved` dgn nama sama menolak penandatangan berikutnya (nama pada langkah MENUNGGU tak dihitung, kalau tidak penerima tugas memblokir dirinya sendiri); (b) `AJEForm` menangkap `kind`/`mis`/`assertions`/`preparer`; (c) header modal ke token + banner peran berhenti menyuruh "ganti peran di menu pengguna". `stepAuthority` kini DIEKSPOR agar dapat diuji langsung.
**GOTCHA PR-E (3 kesalahan saya, semua tertangkap gerbang):** (1) `--on-solid` & `--blue-bg` adalah token KARANGAN — yang ada hanya `green/amber/red/purple/teal`-bg; di atas isian solid repo memakai `#fff` literal (`.pbubble.self`). **Grep `styles_base.css` sebelum memakai nama token.** (2) dua `any` baru meng-un-suppress SELURUH `view_execution.tsx` → 78 error. (3) `React.ChangeEvent` DITOLAK tsc (`Namespace 'react' has no exported member`); konvensi repo = tipe struktural `(e: { target: { value: string } })` — lihat `firm_attest.tsx:98`. Juga: `useStateX<T>()` tak boleh (untyped fn), pakai `useStateX([] as T)`. **LINGKAR: `view_aje` mengimpor `AJEForm` dari `view_execution` → jangan impor balik.**

**Cacat kedua yang ikut tertutup di `85eabee`:** `view_opinion.tsx` memakai DUA nilai materialitas dalam SATU modul — baris 178 ikut perikatan, baris 250 (`OpinionFlowBar`) memaku `4_250_000_000` TANPA fallback. Kelas PR-6·0 (dua PM satu perikatan), kali ini di modul penentu OPINI. Plus `activeEngagement.materiality` dipakai sebagai OM padahal sejak PR-6·0 itu angka administratif. Ketiganya kini `useMateriality().omFull`. Juga `priorOM = 3_900_000_000` (luput dari grep semula) → turunan kolom `ly` WTB; YoY live berubah −14,9% karangan → +20,3% nyata.

**PR-B TERIMPLEMENTASI & DI-PR: [#140](https://github.com/ari1945/Asseris/pull/140)** — branch `feat/aje-pr-b-posting-authority`, commit `4aa6acd`, dicabang dari **master** (bukan dari branch PR-A; berkas beririsan di region berjauhan). Klien 670 + server 299 hijau, typecheck 0 dua-duanya, lint bersih. Kunci perbaikan = SATU pembalikan arah: sumber kebenaran persetujuan `aje.status` → keputusan tercatat; `status` jurnal jadi KELUARAN rantai, bukan masukan. Yang dibangun: `chainFromDecisions()` + `AJE_DECISIONS_SEED` di data_platform, `stepAuthority()` per-langkah di view_platform, `CAP.AJE_POST` (Partner-only, terpisah dari OPINION_APPROVE & AJE_EDIT), `aje`+`approvals_ov_v4` masuk SIGNOFF_KEYS, `approvals_ov_v4` naik ke lingkup perikatan.
**Eksepsi nyata tersingkap:** AJE-01 (Rp 2,34 M) lewat ambang EQR tapi jejaknya hanya Manager+Partner → kini tampil 3/4, EQR MENUNGGU, ditandai `postedWithoutFullChain`.
**GOTCHA baru:** (a) `chain()` dipakai 4 jenis persetujuan lain → JANGAN ubah tanda tangannya, buat fungsi kedua; (b) server `can()` DB-backed (`roleStore` ← `GRANTS` via `bootstrapFirm`) → kapabilitas BARU tak berlaku di instalasi yang tabel Role-nya sudah terisi tanpa re-seed; (c) `setRole` dinonaktifkan sejak W7 → **verifikasi live peran Manager MUSTAHIL tanpa kredensial**; jalur Manager-ditolak hanya terbukti di uji unit, bukan layar (utang, tercatat di PR); (d) badge status innerText UPPERCASE via CSS `.upper` — regex `/^Posted$/` gagal, cek `innerHTML`. **KOREKSI PENTING atas evaluasi awal: alur persetujuan AJE SUDAH ADA dan benar** — `buildApprovals()` (`data_platform.ts:74`) membangun rantai Penyusun→Manager→Partner(+EQR bila >=2 M) sesuai `ROUTING_RULES`, dan `decide()` (`view_platform.tsx:92`) mencatat keputusan, memanggil `logActivity`, lalu menulis balik lewat `toggleAjeStatus`. PR-B bukan membangun mesin, melainkan menutup bypass-nya. Jadi jangan mulai dengan "bikin gerbang".

**P-0 PR-B lebih berat dari dugaan: sistem MEMALSUKAN persetujuan.** `addAje` lahir 'Posted' → `data_platform.ts:88` `doneTo = posted ? steps.length : 1` → `chain()` menandai Manager, Partner, DAN EQR sebagai `approved` dengan `ts: NOW` + note "Disetujui.". Senior Auditor menekan satu tombol, sistem menerbitkan jejak bahwa tiga orang menyetujui. Itu bukti audit palsu, bukan sekadar kontrol hilang.
**P-2:** `SIGNOFF_KEYS` (`server/src/signoff.ts:67`) = {wpState, opinionDoc.v1, reviewNotes, prospects, strategyApproved.v1, mat.memo.signoff} — **`aje` maupun `approvals_ov_v3` TIDAK ada** → `guardSignoffWrite` tak pernah jalan utk jurnal; satu-satunya gerbang server = `capForWrite AJE_EDIT`.
**P-3:** `canApprove = user.role.includes('Partner')||includes('Manager')` — cocok STRING bukan `can()`, dan NOL otoritas per-langkah: Manager bisa menyelesaikan langkah Partner maupun EQR. Tak ada tolak self-approval.
**P-4:** `approvals_ov_v3` pakai `useAmsPersist` & TIDAK terdaftar di `AMS_PERSIST_SCOPE` → lingkup FIRMA, bocor lintas-perikatan. Plus `buildApprovals` memaku `engById('ENG-2025-014')` → setiap AJE diatribusikan ke manager/partner perikatan itu.
**Q1 memblokir:** pakai `OPINION_APPROVE` untuk posting jurnal salah semantik (menyatukan wewenang posting jurnal dgn persetujuan opini) → rekomendasi `CAP.AJE_POST` baru.

**Sisa yang SENGAJA di luar PR-A:** `FISCAL.pbt` 48.500 (PKP hanya tie di situ) → sistem masih punya DUA PBT · `view_sad` masih menarasikan "current ratio 1,38x → 1,19x" hardcode, kini bertentangan dengan 1,64x di modul AJE (PR-C) · seed perikatan butuh reseed.

**Temuan baru saat verifikasi live:** `mat.pmPct` tersimpan `null` di ENG-2025-040 → PM Rp 0 (default 75 tak pernah dipakai) + warning React `value` null di SliderRow. Pre-existing, sudah di-spawn jadi task terpisah.

**PRD PR-A ditulis** → `PRD - AJE PR-A SSOT Figur Entitas (benchmark SA 320 dari WTB).md`. Menunggu "Proceed."
**Q1 diputuskan Ari: KOHERENSI** — konstanta modul yang dikoreksi, seed WTB TIDAK diskalakan (menolak pola PR-6c). OM turun; pergeseran demo diterima.
**Q2 direvisi jadi `unadj`** (draf pertama merekomendasikan `adj`) karena dua alasan: (a) sirkularitas — basis `adj` membuat posting AJE menggerakkan ambang yang menilai AJE itu sendiri (PBT 29.690→OM 1.484; posting 3 AJE −3.940 → OM 1.287, mengetat 13% akibat koreksi auditor sendiri); (b) kolom `aje` seed WTB memasukkan AJE-03 & AJE-05 yang **Proposed** → `adj` belum tepercaya sampai PR-B/PR-C. Basis `adj` tetap dihitung sebagai pemicu revisi SA 320 ¶12-13, tak mengikat OM.
Q3 (FISCAL.pbt) = utang tercatat. Q4 = nilai administratif ditampilkan tapi berlabel "TB belum diimpor", `basis:'none'`.

**Empat PBT untuk satu entitas** (bukan dua seperti dugaan awal): 29.690 (WTB unadj) · 22.780 (WTB adj) · **48.500 (`canon_base.ts:112` FISCAL.pbt, dipakai PSAK 46)** · 85.200 (BENCHMARKS materialitas). PR-A menurunkannya jadi **dua**, bukan satu — `FISCAL.pbt` sengaja di luar scope karena PKP hanya tie di 48.500 (`48.500+1.200−3.000+6.800=53.500` ✅; dengan PBT WTB → 27.780 ❌ → PSAK 46 pecah).

**Tabel benchmark SA 320 mencampur basis** (temuan saat menulis PRD): Ekuitas 160.456 tie ✅ · Pendapatan 331.900 tie ke **unadjusted** · Total Aset 316.558 tie ke **adjusted** · Laba Bruto 99.420 dan PBT 85.200 tak tie ke mana pun. Satu tabel, tiga basis. Cacat metodologi SA 320 ¶A5, bukan sekadar salah angka.
**Alarmnya dibungkam:** `ENG-2025-014.materiality = 4.250` (`data_part1.ts:42`) ditala persis ke `calcOM` fantasi 4.260 → detektor `drift` (`canon_part4.ts:382`, ambang 0,5%) melihat 0,235% → tak pernah menyala. Kriteria sukses PRD menuntut drift **menyala dulu lalu padam**.
**Bukti WTB yang koheren:** pergerakan saldo laba LY→CY = 18,094 M vs laba neto unadj turunan WTB 29,690−11,240 = 18,450 M (Δ 2%, wajar untuk dividen/OCI). Sedangkan PBT 85,200 atas pendapatan 331,900 = marjin 25,7% pada entitas bermarjin bruto 30,7% → menuntut opex ≈ 0.
`window.BENCHMARKS` masih kontrak window (`view_materiality.tsx:315` → `canon_part4.ts:372`) padahal WTB per-perikatan (`contexts.tsx:772,785`) → benchmark buta-perikatan.

**Temuan terdalam — konstanta modul membantah WTB, lalu menjalar ke materialitas.**
`AJE_PBT_UNADJ = 89_140_000_000` (view_aje.tsx:26) vs PBT unadjusted turunan WTB = **Rp 29,69 M**. `AJE_FS.curAssets 112,3 / curLiab 81,4` vs WTB adj **151,998 / 94,901**. Hanya `equity 160_456` yang tie (= 3-1100 + 3-2100). Rantai: konstanta AJE → `view_sad.tsx:57 FS.pbt = 85_200_000_000` → `view_materiality.tsx:19 BENCHMARKS.pbt = 85_200_000_000` → OM 5% = Rp 4,26 M padahal dari WTB audited (22,78 M) semestinya ~Rp 1,14 M. **Materialitas kelebihan ~3,7x.** Uji koherensi: marjin bruto WTB 30,7%, PBT 8,9% dari pendapatan — wajar untuk manufaktur; PBT 85,2/331,9 = 25,7% menuntut opex nyaris nol. Jadi **WTB koheren, konstanta modul yang salah** — tapi memperbaikinya menurunkan OM 3,7x dan menggeser kesimpulan SAD di seluruh demo (keputusan Ari: turunkan OM, atau skalakan seed WTB naik seperti PR-6c).
`figuresFromWTB()` di `canon_base.ts:38` **tidak** mengekspos pbt/curAssets/curLiab — jadi memang belum ada selector kanonik; itu yang harus dibuat.

**Modul membantah kontrolnya sendiri.** Tab 3 menyatakan "Jurnal hanya boleh diposting ke WTB setelah disetujui Engagement Partner (ISQM 1)" (view_aje.tsx:617), tapi `addAje` default `status: 'Posted'` (contexts.tsx:869) dan tombol form berbunyi "Posting ke WTB" — entri baru lahir terposting. Toggle status = satu klik `<span onClick>` tanpa konfirmasi/alasan/`logActivity`, tersedia bagi Senior Auditor (`AJE_EDIT`, rbac.ts:108). Matriks otorisasi berjenjang sudah tertulis di `data_platform.ts:40` (Manager <500jt · Partner 0,5–2 M · EQR >2 M) — tak ditegakkan di modul. Kelas sama dengan [[asseris-opinion-signoff-sod-defect]] dan lubang `can()` di [[asseris-materiality-om-split]].

**PR-C TERIMPLEMENTASI & DI-PR: [#141](https://github.com/ari1945/Asseris/pull/141)** — branch `feat/aje-pr-c-sa450-reconciliation`, commit `895096e`, **BERTUMPUK di atas branch PR-A** (base PR diarahkan ke sana; setelah #139 squash-merge WAJIB retarget ke master + rebase — resep 6 langkah di [[asseris-wtb-eval-pr1-pr2]]). 692 test hijau, typecheck 0, lint bersih. Ari mendelegasikan Q1/Q2/Q3 ke saya: `bsEffect` ditambahkan, residu jadi anotasi, `disp` tetap 3 keadaan.
**Temuan terbesar PR-C: klaim pelanggaran covenant pada seed KELIRU.** Terhitung: rasio kini **1,62×** → proyeksi **1,52×**, ambang 1,20× **TIDAK tertembus** (seed mengklaim 1,38×→1,19× tertembus). Klaim palsu itu menyalakan faktor kualitatif `covenant`, baris aksi "Komunikasikan dampak covenant ke TCWG (SA 260)", dan entri log komunikasi TCWG — **tiga kesimpulan audit di atas satu angka karangan**.
**Bonus:** `view_aje` kini pakai `ajeEffect` alih-alih `curEff` → SAD & AJE menyebut SATU rasio (1,62×), efek liabilitas lancar ikut terhitung, dan bug `curEff` AJE-02 (CKPN −620 jt ditulis 0) lenyap — itu memakan sebagian pekerjaan PR-D.
**GOTCHA:** menginisialisasi `sadItems.v1` dengan `[]` di modul kedua membuat banner mengklaim "belum ada di ledger SAD" untuk SETIAP item → `SAD_SEED` harus diekspor & dipakai bersama (dua default berbeda atas satu kunci = kelas cacat yang sama dgn yang diperbaiki). Temuan baru saat menulisnya: **item SAD TIDAK punya field efek neraca** — hanya `pbt` & `na` (efek laba). M-07 (reklas utang bank ke liabilitas lancar, yang paling menentukan rasio lancar) tercatat `pbt: 0, na: 0` dan besarannya (Rp 4,1 M) hanya hidup di dalam STRING catatan kualitatif. **Rasio covenant proyeksi karena itu tak dapat dihitung dari data yang ada** — itulah sebabnya ia di-hardcode. Q1 PR-C memblokir: tambah `bsEffect` ke item SAD, atau buang proyeksinya. Juga: `disp` hanya 3 keadaan (uncorrected/corrected/passed), tak ada "dikoreksi sebagian" → residu koreksi-sebagian tak punya tempat. PR-C bergantung PR-A (#139) untuk bagian covenant DAN berkonflik dengannya di `view_sad.tsx` → merge #139 dulu.

**Rekonsiliasi SA 450 membaca seed beku.** `view_sad.tsx:81` memberi `aje: AMS.AJE` ke `reconcileUncorrectedMisstatements`, bukan `useAudit().aje` — cache-dingin persis kelas PR-6b. Fungsinya sendiri sehat (`ajeRefKey` menormalkan `PAJE-03`→`AJE-03`) tapi **tak memeriksa nilai**: SAD M-01 = 1.950 jt vs AJE-03 = 1.850 jt; M-04 = 680 jt vs AJE-02 = 620 jt — selisih lolos senyap. Modul AJE juga tak punya banner balikan: drill mengklaim "Salah saji ini telah berstatus dikoreksi" (baris 365) tanpa membaca `disp` SAD.

**Covenant: tiga jawaban satu perikatan.** AJE 1,38x→1,36x (hijau, cabang amber baris 442 tak pernah tampil dengan seed) · SAD 1,38x→1,19x menembus 1,20x (view_sad.tsx:37,359) · WTB sebenarnya 1,60x (aset lancar adj 151,998 / liab lancar adj 94,901 — terverifikasi lewat entityFigures(), bukan hitung tangan).

**PR-D TERIMPLEMENTASI & DI-PR: [#142](https://github.com/ari1945/Asseris/pull/142)** — `99c0b1d`, bertumpuk di atas #141 (yang di atas #139). 700 test hijau. `AJE_META.pbt` dibuang (uji memakai nilai lama sbg oracle — nol angka bergeser), `curEff` dibuang (nol konsumen sejak PR-C), `ajeDeriveKind` → `ajeKindSuggestion` (saran, bukan penentuan; uji memaku saran 'reclass' selalu berefek laba nol), tanggal ISO + urutan jejak eksplisit, `reviewer`/`partner` berhenti dipalsukan jadi 'Anindya/Hartono' untuk entri auditor, 3 gradient + warna heksa → token (NOL heksa tersisa), "Telaah AI" & ref WP jadi hidup.
**SISA PR-D yang SENGAJA tak dikerjakan:** penangkapan `kind`/`mis`/`assertions` di AJEForm — berkasnya `view_execution.tsx` disentuh PR-B sedangkan PR-D di jalur PR-A→PR-C, jadi akan konflik. Model SUDAH menerima `a.kind`/`a.mis`/`a.preparer`; formulirnya paling tepat digabung ke PR-B.

**RANTAI PR BERTUMPUK (retarget wajib setelah tiap merge):** #139 (PR-A, dari master) ← #141 (PR-C) ← #142 (PR-D). #140 (PR-B) BERDIRI SENDIRI dari master.
**GOTCHA:** `read_console_messages` mengembalikan RIWAYAT akumulatif lintas-branch — error `CAP is not defined` dari berkas milik PR-B muncul saat bekerja di branch PR-D meski berkasnya versi master. Verifikasi dgn `grep` berkas + cek app benar-benar merender, jangan percaya konsol.

**Duplikasi turunan.** Kelima `AJE_META[].pbt` persis sama dengan `ajeDerivePbt(ajeLines(a))` — duplikasi murni. `curEff` di-hardcode switch (baris 89) dan **salah untuk AJE-02** (CKPN 1-1210 kredit 620 jt = −620 jt aset lancar, ditulis 0); laten karena AJE-02 Posted.

Lain-lain: `ajeDeriveKind` salah dua arah (reklas antar akun beban dilabeli Penyesuaian; koreksi neraca-saja dilabeli Reklasifikasi) padahal legenda menjanjikan "Reklasifikasi tidak berdampak pada laba" · AJEForm tak menangkap kind/mis/assertions/preparer → entri user tak pernah masuk SAD maupun Lensa Asersi, dan reviewer/partner dipalsukan ke 'Anindya Pramesti'/'Hartono Wijaya' (baris 93) · `allCoded` dihitung tak dipakai (view_execution.tsx:1491) · `'ENG-2025-014'` hardcode (view_execution.tsx:1509) · `ord()` jejak audit hanya kenal 'Mei'/'Jun' dan entri baru ('baru saja') tenggelam ke dasar · gradient `#013a52/#005085` + `#fff/#bcd6e4/#9fc0d2` hardcode 3x → mematahkan tema gelap · tombol "Telaah AI" tanpa onClick · **nol test AJE**.

Yang sudah benar dan jangan dirusak: gerbang `AJE_EDIT` + banner peran (baris 200) — lebih baik dari `view_materiality_parts.tsx`; `ajeAssertionIds` sebagai SSOT lintas-modul; export XLSX tersegel; `DiagnosticPanel area="aje"` tersambung ke uji jurnal SA 240.

Terkait: [[asseris-wtb-eval-pr1-pr2]] · [[asseris-materiality-om-split]] · [[asseris-wtb-pr3-pr4-sa520-spine]]
