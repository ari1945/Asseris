# W6 dev datasource. SQLite single-file (zero-ops). No secret — safe to track.
# Prod (W10): flip schema.prisma provider to "postgresql" + set DATABASE_URL to the PG DSN.
DATABASE_URL="file:./dev.db"
