{
  "//": "NeoSuite AMS — W6 server. TS strict (extends the W5 typecheck gate to server/). Bundler resolution = extensionless imports; tsx (esbuild) runs the .ts at runtime, tsc only typechecks (noEmit).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],

    "strict": true,

    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "verbatimModuleSyntax": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
